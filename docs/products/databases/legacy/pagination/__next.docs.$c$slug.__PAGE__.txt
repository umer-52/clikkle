1:"$Sreact.fragment"
2:I[2650755,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsCopyPage"]
c:I[885369,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsArticleGridContent"]
d:I[4522016,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],""]
e:I[6725962,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsProse"]
f:I[4673752,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsHeading"]
10:I[155313,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"MultiCode"]
11:I[9023822,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"MarkdocFence"]
1d:I[9595551,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsOnThisPage"]
1e:I[3897367,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js"],"OutletBoundary"]
1f:"$Sreact.suspense"
22:I[5587564,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsFeedback"]
3:T2063,---
layout: article
title: Pagination
description: Implement pagination for large data sets in Clikkle Databases. Explore techniques for splitting and displaying data across multiple pages.
---

As your database grows in size, you'll need to paginate results returned.
Pagination improves performance by returning a subset of results that match a query at a time, called a page.

By default, list operations return 25 items per page, which can be changed using the `Query.limit(25)` operator.
There is no hard limit on the number of items you can request. However, beware that **large pages can degrade performance**.

# Offset pagination {% #offset-pagination %}

Offset pagination works by dividing documents into `M` pages containing `N` documents.
Every page is retrieved by skipping `offset = M * (N - 1)` items and reading the following `M` pages.

Using `Query.limit()` and `Query.offset()` you can achieve offset pagination.
With `Query.limit()` you can define how many documents can be returned from one request.
The `Query.offset()` is number of records you wish to skip before selecting records.

{% multicode %}
```client-web
import { Client, Databases, Query } from "clikkle";

const client = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const databases = new Databases(client);

// Page 1
const page1 = await databases.listDocuments(
    '<DATABASE_ID>',
    '<COLLECTION_ID>',
    [
        Query.limit(25),
        Query.offset(0)
    ]
);

// Page 2
const page2 = await databases.listDocuments(
    '<DATABASE_ID>',
    '<COLLECTION_ID>',
    [
        Query.limit(25),
        Query.offset(25)
    ]
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

void main() async {
    final client = Client()
        .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
        .setProject('<PROJECT_ID>');

    final databases = Databases(client);

    final page1 = await databases.listDocuments(
        databaseId: '<DATABASE_ID>',
        collectionId: '<COLLECTION_ID>',
        queries: [
            Query.limit(25),
            Query.offset(0)
        ]
    );

    final page2 = await databases.listDocuments(
        databaseId: '<DATABASE_ID>',
        collectionId: '<COLLECTION_ID>',
        queries: [
            Query.limit(25),
            Query.offset(25)
        ]
    );
}
```
```client-apple
import Clikkle
import ClikkleModels

func main() async throws {
    let client = Client()
        .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
        .setProject("<PROJECT_ID>")

    let databases = Databases(client)

    let page1 = try await databases.listDocuments(
        databaseId: "<DATABASE_ID>",
        collectionId: "<COLLECTION_ID>",
        queries: [
            Query.limit(25),
            Query.offset(0)
        ]
    )

    let page2 = try await databases.listDocuments(
        databaseId: "<DATABASE_ID>",
        collectionId: "<COLLECTION_ID>",
        queries: [
            Query.limit(25),
            Query.offset(25)
        ]
    )
}
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.Query
import io.clikkle.services.Databases

suspend fun main() {
    val client = Client(applicationContext)
        .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
        .setProject("<PROJECT_ID>")

    val databases = Databases(client)

    val page1 = databases.listDocuments(
        databaseId = "<DATABASE_ID>",
        collectionId = "<COLLECTION_ID>",
        queries = [
            Query.limit(25),
            Query.offset(0)
        ]
    )

    val page2 = databases.listDocuments(
        databaseId = "<DATABASE_ID>",
        collectionId = "<COLLECTION_ID>",
        queries = [
            Query.limit(25),
            Query.offset(25)
        ]
    )
}
```

{% /multicode %}

{% info title="Drawbacks" %}
While traditional offset pagination is familiar, it comes with some drawbacks.
The request gets slower as the number of records increases because the database has to read up to the offset number `M * (N - 1)` of rows to know where it should start selecting data.
If the data changes frequently, offset pagination will also produce **missing and duplicate** results.
{% /info %}

# Cursor pagination {% #cursor-pagination %}

The cursor is a unique identifier for a document that points to where the next page should start.
After reading a page of documents, pass the last document's ID into the `Query.cursorAfter(lastId)` query method to get the next page of documents.
Pass the first document's ID into the `Query.cursorBefore(firstId)` query method to retrieve the previous page.

{% multicode %}

```client-web
import { Databases, Query } from "clikkle";

const client = new Client()
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>");

const databases = new Databases(client);

// Page 1
const page1 = await databases.listDocuments(
    '<DATABASE_ID>',
    '<COLLECTION_ID>',
    [
        Query.limit(25),
    ]
);

const lastId = page1.documents[page1.documents.length - 1].$id;

// Page 2
const page2 = await databases.listDocuments(
    '<DATABASE_ID>',
    '<COLLECTION_ID>',
    [
        Query.limit(25),
        Query.cursorAfter(lastId),
    ]
);
```

```client-flutter
import 'package:clikkle/clikkle.dart';

void main() async {
    final client = Client()
        .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
        .setProject('<PROJECT_ID>');

    final databases = Databases(client);

    final page1 = await databases.listDocuments(
        databaseId: '<DATABASE_ID>',
        collectionId: '<COLLECTION_ID>',
        queries: [
            Query.limit(25)
        ]
    );

    final lastId = page1.documents[page1.documents.length - 1].$id;

    final page2 = await databases.listDocuments(
        databaseId: '<DATABASE_ID>',
        collectionId: '<COLLECTION_ID>',
        queries: [
            Query.limit(25),
            Query.cursorAfter(lastId)
        ]
    );

}
```
```client-apple
import Clikkle
import ClikkleModels

func main() async throws {
    let client = Client()
      .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
      .setProject("<PROJECT_ID>")

    let databases = Databases(client)

    let page1 = try await databases.listDocuments(
        databaseId: "<DATABASE_ID>",
        collectionId: "<COLLECTION_ID>",
        queries: [
            Query.limit(25)
        ]
    )

    let lastId = page1.documents[page1.documents.count - 1].$id

    let page2 = try await databases.listDocuments(
        databaseId: "<DATABASE_ID>",
        collectionId: "<COLLECTION_ID>",
        queries: [
            Query.limit(25),
            Query.cursorAfter(lastId)
        ]
    )
}
```
```client-android-kotlin
import android.util.Log
import io.clikkle.ClikkleException
import io.clikkle.Client
import io.clikkle.Query
import io.clikkle.services.Databases

suspend fun main() {
    val client = Client(applicationContext)
        .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
        .setProject("<PROJECT_ID>")

    val databases = Databases(client)

    val page1 = databases.listDocuments(
        databaseId = "<DATABASE_ID>",
        collectionId = "<COLLECTION_ID>",
        queries = [
            Query.limit(25)
        ]
    )

    val lastId = page1.documents[page1.documents.size - 1].$id

    val page2 = databases.listDocuments(
        databaseId = "<DATABASE_ID>",
        collectionId = "<COLLECTION_ID>",
        queries = [
            Query.limit(25),
            Query.cursorAfter(lastId)
        ]
    )
}
```

{% /multicode %}

# When to use what? {% #when-to-use %}
Offset pagination should be used for collections that rarely change.
Offset paginations allow you to create indicator of the current page number and total page number.
For example, a list with up to 20 pages or static data like a list of countries or currencies.
Using offset pagination on large collections and frequently updated collections may result in slow performance and **missing and duplicate** results.

Cursor pagination should be used for frequently updated collections.
It is best suited for lazy-loaded pages with infinite scrolling.
For example, a feed, comment section, chat history, or high volume datasets.

0:{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"contents","children":["$","article",null,{"className":"web-article contents","children":[["$","header",null,{"className":"web-article-header docs-article-header-stack","children":[["$","div",null,{"className":"docs-article-mobile-header-row mb-2 flex w-full items-center lg:hidden","children":[null,["$","div",null,{"className":"copy-button-wrapper-mobile ml-auto","children":["$","$L2",null,{"markdown":"$3","className":"ml-0"}]}]]}],null,"$L4"]}],"$L5","$L6"]}]}],["$L7","$L8","$L9"],"$La"]}],"isPartial":false,"staleTime":300,"varyParams":null,"buildId":"siKNZ69LNgpmcXavwmUTs"}
b:T2063,---
layout: article
title: Pagination
description: Implement pagination for large data sets in Clikkle Databases. Explore techniques for splitting and displaying data across multiple pages.
---

As your database grows in size, you'll need to paginate results returned.
Pagination improves performance by returning a subset of results that match a query at a time, called a page.

By default, list operations return 25 items per page, which can be changed using the `Query.limit(25)` operator.
There is no hard limit on the number of items you can request. However, beware that **large pages can degrade performance**.

# Offset pagination {% #offset-pagination %}

Offset pagination works by dividing documents into `M` pages containing `N` documents.
Every page is retrieved by skipping `offset = M * (N - 1)` items and reading the following `M` pages.

Using `Query.limit()` and `Query.offset()` you can achieve offset pagination.
With `Query.limit()` you can define how many documents can be returned from one request.
The `Query.offset()` is number of records you wish to skip before selecting records.

{% multicode %}
```client-web
import { Client, Databases, Query } from "clikkle";

const client = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const databases = new Databases(client);

// Page 1
const page1 = await databases.listDocuments(
    '<DATABASE_ID>',
    '<COLLECTION_ID>',
    [
        Query.limit(25),
        Query.offset(0)
    ]
);

// Page 2
const page2 = await databases.listDocuments(
    '<DATABASE_ID>',
    '<COLLECTION_ID>',
    [
        Query.limit(25),
        Query.offset(25)
    ]
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

void main() async {
    final client = Client()
        .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
        .setProject('<PROJECT_ID>');

    final databases = Databases(client);

    final page1 = await databases.listDocuments(
        databaseId: '<DATABASE_ID>',
        collectionId: '<COLLECTION_ID>',
        queries: [
            Query.limit(25),
            Query.offset(0)
        ]
    );

    final page2 = await databases.listDocuments(
        databaseId: '<DATABASE_ID>',
        collectionId: '<COLLECTION_ID>',
        queries: [
            Query.limit(25),
            Query.offset(25)
        ]
    );
}
```
```client-apple
import Clikkle
import ClikkleModels

func main() async throws {
    let client = Client()
        .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
        .setProject("<PROJECT_ID>")

    let databases = Databases(client)

    let page1 = try await databases.listDocuments(
        databaseId: "<DATABASE_ID>",
        collectionId: "<COLLECTION_ID>",
        queries: [
            Query.limit(25),
            Query.offset(0)
        ]
    )

    let page2 = try await databases.listDocuments(
        databaseId: "<DATABASE_ID>",
        collectionId: "<COLLECTION_ID>",
        queries: [
            Query.limit(25),
            Query.offset(25)
        ]
    )
}
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.Query
import io.clikkle.services.Databases

suspend fun main() {
    val client = Client(applicationContext)
        .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
        .setProject("<PROJECT_ID>")

    val databases = Databases(client)

    val page1 = databases.listDocuments(
        databaseId = "<DATABASE_ID>",
        collectionId = "<COLLECTION_ID>",
        queries = [
            Query.limit(25),
            Query.offset(0)
        ]
    )

    val page2 = databases.listDocuments(
        databaseId = "<DATABASE_ID>",
        collectionId = "<COLLECTION_ID>",
        queries = [
            Query.limit(25),
            Query.offset(25)
        ]
    )
}
```

{% /multicode %}

{% info title="Drawbacks" %}
While traditional offset pagination is familiar, it comes with some drawbacks.
The request gets slower as the number of records increases because the database has to read up to the offset number `M * (N - 1)` of rows to know where it should start selecting data.
If the data changes frequently, offset pagination will also produce **missing and duplicate** results.
{% /info %}

# Cursor pagination {% #cursor-pagination %}

The cursor is a unique identifier for a document that points to where the next page should start.
After reading a page of documents, pass the last document's ID into the `Query.cursorAfter(lastId)` query method to get the next page of documents.
Pass the first document's ID into the `Query.cursorBefore(firstId)` query method to retrieve the previous page.

{% multicode %}

```client-web
import { Databases, Query } from "clikkle";

const client = new Client()
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>");

const databases = new Databases(client);

// Page 1
const page1 = await databases.listDocuments(
    '<DATABASE_ID>',
    '<COLLECTION_ID>',
    [
        Query.limit(25),
    ]
);

const lastId = page1.documents[page1.documents.length - 1].$id;

// Page 2
const page2 = await databases.listDocuments(
    '<DATABASE_ID>',
    '<COLLECTION_ID>',
    [
        Query.limit(25),
        Query.cursorAfter(lastId),
    ]
);
```

```client-flutter
import 'package:clikkle/clikkle.dart';

void main() async {
    final client = Client()
        .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
        .setProject('<PROJECT_ID>');

    final databases = Databases(client);

    final page1 = await databases.listDocuments(
        databaseId: '<DATABASE_ID>',
        collectionId: '<COLLECTION_ID>',
        queries: [
            Query.limit(25)
        ]
    );

    final lastId = page1.documents[page1.documents.length - 1].$id;

    final page2 = await databases.listDocuments(
        databaseId: '<DATABASE_ID>',
        collectionId: '<COLLECTION_ID>',
        queries: [
            Query.limit(25),
            Query.cursorAfter(lastId)
        ]
    );

}
```
```client-apple
import Clikkle
import ClikkleModels

func main() async throws {
    let client = Client()
      .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
      .setProject("<PROJECT_ID>")

    let databases = Databases(client)

    let page1 = try await databases.listDocuments(
        databaseId: "<DATABASE_ID>",
        collectionId: "<COLLECTION_ID>",
        queries: [
            Query.limit(25)
        ]
    )

    let lastId = page1.documents[page1.documents.count - 1].$id

    let page2 = try await databases.listDocuments(
        databaseId: "<DATABASE_ID>",
        collectionId: "<COLLECTION_ID>",
        queries: [
            Query.limit(25),
            Query.cursorAfter(lastId)
        ]
    )
}
```
```client-android-kotlin
import android.util.Log
import io.clikkle.ClikkleException
import io.clikkle.Client
import io.clikkle.Query
import io.clikkle.services.Databases

suspend fun main() {
    val client = Client(applicationContext)
        .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
        .setProject("<PROJECT_ID>")

    val databases = Databases(client)

    val page1 = databases.listDocuments(
        databaseId = "<DATABASE_ID>",
        collectionId = "<COLLECTION_ID>",
        queries = [
            Query.limit(25)
        ]
    )

    val lastId = page1.documents[page1.documents.size - 1].$id

    val page2 = databases.listDocuments(
        databaseId = "<DATABASE_ID>",
        collectionId = "<COLLECTION_ID>",
        queries = [
            Query.limit(25),
            Query.cursorAfter(lastId)
        ]
    )
}
```

{% /multicode %}

# When to use what? {% #when-to-use %}
Offset pagination should be used for collections that rarely change.
Offset paginations allow you to create indicator of the current page number and total page number.
For example, a list with up to 20 pages or static data like a list of countries or currencies.
Using offset pagination on large collections and frequently updated collections may result in slow performance and **missing and duplicate** results.

Cursor pagination should be used for frequently updated collections.
It is best suited for lazy-loaded pages with infinite scrolling.
For example, a feed, comment section, chat history, or high volume datasets.

4:["$","div",null,{"className":"docs-article-title-actions-row","children":[["$","div",null,{"className":"relative flex min-w-0 flex-1 items-center","children":[null,["$","h1",null,{"className":"text-title m-0 font-aeonik-pro tracking-tight text-primary","children":"Pagination"}]]}],["$","div",null,{"className":"web-article-header-end copy-button-wrapper hidden shrink-0 lg:block","children":["$","$L2",null,{"markdown":"$b","className":"ml-0"}]}]]}]
5:["$","$Lc",null,{"className":"prose max-w-none","children":[null,["$","div",null,{"className":"docs-article-after-header pt-8","children":[["$","div",null,{"className":"web-inline-info not-prose my-12!","children":[["$","span",null,{"className":"icon-info","aria-hidden":"true"}],["$","h5",null,{"className":"text-sub-body text-primary font-medium","children":"Deprecated API"}],["$","p",null,{"className":"text-caption mt-2 leading-relaxed text-[var(--color-text-secondary)] dark:text-white/70","children":["This API uses outdated terminology and is no longer maintained. Refer to the"," ",["$","$Ld",null,{"href":"/docs/products/databases/pagination","className":"web-link underline","children":"updated documentation"}]," ","for improved compatibility and migration guidance."]}]]}],null,["$","$Le",null,{"isArticleLayout":true,"contentOnly":true,"children":["$","article",null,{"children":[["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["As your database grows in size, you'll need to paginate results returned."," ","Pagination improves performance by returning a subset of results that match a query at a time, called a page."]}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["By default, list operations return 25 items per page, which can be changed using the ",["$","code",null,{"children":"Query.limit(25)"}]," operator."," ","There is no hard limit on the number of items you can request. However, beware that ",["$","strong",null,{"children":"large pages can degrade performance"}],"."]}],["$","$Lf",null,{"id":"offset-pagination","level":1,"children":"Offset pagination "}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Offset pagination works by dividing documents into ",["$","code",null,{"children":"M"}]," pages containing ",["$","code",null,{"children":"N"}]," documents."," ","Every page is retrieved by skipping ",["$","code",null,{"children":"offset = M * (N - 1)"}]," items and reading the following ",["$","code",null,{"children":"M"}]," pages."]}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Using ",["$","code",null,{"children":"Query.limit()"}]," and ",["$","code",null,{"children":"Query.offset()"}]," you can achieve offset pagination."," ","With ",["$","code",null,{"children":"Query.limit()"}]," you can define how many documents can be returned from one request."," ","The ",["$","code",null,{"children":"Query.offset()"}]," is number of records you wish to skip before selecting records."]}],["$","$L10",null,{"children":[["$","$L11",null,{"language":"client-web","content":"import { Client, Databases, Query } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst databases = new Databases(client);\n\n// Page 1\nconst page1 = await databases.listDocuments(\n    '<DATABASE_ID>',\n    '<COLLECTION_ID>',\n    [\n        Query.limit(25),\n        Query.offset(0)\n    ]\n);\n\n// Page 2\nconst page2 = await databases.listDocuments(\n    '<DATABASE_ID>',\n    '<COLLECTION_ID>',\n    [\n        Query.limit(25),\n        Query.offset(25)\n    ]\n);\n","children":"import { Client, Databases, Query } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst databases = new Databases(client);\n\n// Page 1\nconst page1 = await databases.listDocuments(\n    '<DATABASE_ID>',\n    '<COLLECTION_ID>',\n    [\n        Query.limit(25),\n        Query.offset(0)\n    ]\n);\n\n// Page 2\nconst page2 = await databases.listDocuments(\n    '<DATABASE_ID>',\n    '<COLLECTION_ID>',\n    [\n        Query.limit(25),\n        Query.offset(25)\n    ]\n);\n"}],"$L12","$L13","$L14"]}],"$L15","$L16","$L17","$L18","$L19","$L1a","$L1b"]}]}]]}],"$L1c"]}]
6:["$","aside",null,{"className":"web-references-menu ps-6","children":["$","$L1d",null,{"items":[{"id":"offset-pagination","title":"Offset pagination","level":1},{"id":"cursor-pagination","title":"Cursor pagination","level":1},{"id":"when-to-use","title":"When to use what?","level":1}]}]}]
7:["$","script","script-0",{"src":"/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","async":true}]
8:["$","script","script-1",{"src":"/clikkle/_next/static/chunks/10iz33s~n0gnf.js","async":true}]
9:["$","script","script-2",{"src":"/clikkle/_next/static/chunks/17akn4e9sundm.js","async":true}]
a:["$","$L1e",null,{"children":["$","$1f",null,{"name":"Next.MetadataOutlet","children":"$@20"}]}]
12:["$","$L11",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart';\n\nvoid main() async {\n    final client = Client()\n        .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n        .setProject('<PROJECT_ID>');\n\n    final databases = Databases(client);\n\n    final page1 = await databases.listDocuments(\n        databaseId: '<DATABASE_ID>',\n        collectionId: '<COLLECTION_ID>',\n        queries: [\n            Query.limit(25),\n            Query.offset(0)\n        ]\n    );\n\n    final page2 = await databases.listDocuments(\n        databaseId: '<DATABASE_ID>',\n        collectionId: '<COLLECTION_ID>',\n        queries: [\n            Query.limit(25),\n            Query.offset(25)\n        ]\n    );\n}\n","children":"import 'package:clikkle/clikkle.dart';\n\nvoid main() async {\n    final client = Client()\n        .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n        .setProject('<PROJECT_ID>');\n\n    final databases = Databases(client);\n\n    final page1 = await databases.listDocuments(\n        databaseId: '<DATABASE_ID>',\n        collectionId: '<COLLECTION_ID>',\n        queries: [\n            Query.limit(25),\n            Query.offset(0)\n        ]\n    );\n\n    final page2 = await databases.listDocuments(\n        databaseId: '<DATABASE_ID>',\n        collectionId: '<COLLECTION_ID>',\n        queries: [\n            Query.limit(25),\n            Query.offset(25)\n        ]\n    );\n}\n"}]
13:["$","$L11",null,{"language":"client-apple","content":"import Clikkle\nimport ClikkleModels\n\nfunc main() async throws {\n    let client = Client()\n        .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n        .setProject(\"<PROJECT_ID>\")\n\n    let databases = Databases(client)\n\n    let page1 = try await databases.listDocuments(\n        databaseId: \"<DATABASE_ID>\",\n        collectionId: \"<COLLECTION_ID>\",\n        queries: [\n            Query.limit(25),\n            Query.offset(0)\n        ]\n    )\n\n    let page2 = try await databases.listDocuments(\n        databaseId: \"<DATABASE_ID>\",\n        collectionId: \"<COLLECTION_ID>\",\n        queries: [\n            Query.limit(25),\n            Query.offset(25)\n        ]\n    )\n}\n","children":"import Clikkle\nimport ClikkleModels\n\nfunc main() async throws {\n    let client = Client()\n        .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n        .setProject(\"<PROJECT_ID>\")\n\n    let databases = Databases(client)\n\n    let page1 = try await databases.listDocuments(\n        databaseId: \"<DATABASE_ID>\",\n        collectionId: \"<COLLECTION_ID>\",\n        queries: [\n            Query.limit(25),\n            Query.offset(0)\n        ]\n    )\n\n    let page2 = try await databases.listDocuments(\n        databaseId: \"<DATABASE_ID>\",\n        collectionId: \"<COLLECTION_ID>\",\n        queries: [\n            Query.limit(25),\n            Query.offset(25)\n        ]\n    )\n}\n"}]
14:["$","$L11",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.Query\nimport io.clikkle.services.Databases\n\nsuspend fun main() {\n    val client = Client(applicationContext)\n        .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n        .setProject(\"<PROJECT_ID>\")\n\n    val databases = Databases(client)\n\n    val page1 = databases.listDocuments(\n        databaseId = \"<DATABASE_ID>\",\n        collectionId = \"<COLLECTION_ID>\",\n        queries = [\n            Query.limit(25),\n            Query.offset(0)\n        ]\n    )\n\n    val page2 = databases.listDocuments(\n        databaseId = \"<DATABASE_ID>\",\n        collectionId = \"<COLLECTION_ID>\",\n        queries = [\n            Query.limit(25),\n            Query.offset(25)\n        ]\n    )\n}\n","children":"import io.clikkle.Client\nimport io.clikkle.Query\nimport io.clikkle.services.Databases\n\nsuspend fun main() {\n    val client = Client(applicationContext)\n        .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n        .setProject(\"<PROJECT_ID>\")\n\n    val databases = Databases(client)\n\n    val page1 = databases.listDocuments(\n        databaseId = \"<DATABASE_ID>\",\n        collectionId = \"<COLLECTION_ID>\",\n        queries = [\n            Query.limit(25),\n            Query.offset(0)\n        ]\n    )\n\n    val page2 = databases.listDocuments(\n        databaseId = \"<DATABASE_ID>\",\n        collectionId = \"<COLLECTION_ID>\",\n        queries = [\n            Query.limit(25),\n            Query.offset(25)\n        ]\n    )\n}\n"}]
15:["$","div",null,{"className":"web-inline-info not-prose my-12!","children":[["$","span",null,{"className":"icon-info","aria-hidden":"true"}],["$","h5",null,{"className":"text-sub-body text-primary font-medium","children":"Drawbacks"}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["While traditional offset pagination is familiar, it comes with some drawbacks."," ","The request gets slower as the number of records increases because the database has to read up to the offset number ",["$","code",null,{"children":"M * (N - 1)"}]," of rows to know where it should start selecting data."," ","If the data changes frequently, offset pagination will also produce ",["$","strong",null,{"children":"missing and duplicate"}]," results."]}]]}]
16:["$","$Lf",null,{"id":"cursor-pagination","level":1,"children":"Cursor pagination "}]
17:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["The cursor is a unique identifier for a document that points to where the next page should start."," ","After reading a page of documents, pass the last document's ID into the ",["$","code",null,{"children":"Query.cursorAfter(lastId)"}]," query method to get the next page of documents."," ","Pass the first document's ID into the ",["$","code",null,{"children":"Query.cursorBefore(firstId)"}]," query method to retrieve the previous page."]}]
18:["$","$L10",null,{"children":[["$","$L11",null,{"language":"client-web","content":"import { Databases, Query } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\");\n\nconst databases = new Databases(client);\n\n// Page 1\nconst page1 = await databases.listDocuments(\n    '<DATABASE_ID>',\n    '<COLLECTION_ID>',\n    [\n        Query.limit(25),\n    ]\n);\n\nconst lastId = page1.documents[page1.documents.length - 1].$id;\n\n// Page 2\nconst page2 = await databases.listDocuments(\n    '<DATABASE_ID>',\n    '<COLLECTION_ID>',\n    [\n        Query.limit(25),\n        Query.cursorAfter(lastId),\n    ]\n);\n","children":"import { Databases, Query } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\");\n\nconst databases = new Databases(client);\n\n// Page 1\nconst page1 = await databases.listDocuments(\n    '<DATABASE_ID>',\n    '<COLLECTION_ID>',\n    [\n        Query.limit(25),\n    ]\n);\n\nconst lastId = page1.documents[page1.documents.length - 1].$id;\n\n// Page 2\nconst page2 = await databases.listDocuments(\n    '<DATABASE_ID>',\n    '<COLLECTION_ID>',\n    [\n        Query.limit(25),\n        Query.cursorAfter(lastId),\n    ]\n);\n"}],["$","$L11",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart';\n\nvoid main() async {\n    final client = Client()\n        .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n        .setProject('<PROJECT_ID>');\n\n    final databases = Databases(client);\n\n    final page1 = await databases.listDocuments(\n        databaseId: '<DATABASE_ID>',\n        collectionId: '<COLLECTION_ID>',\n        queries: [\n            Query.limit(25)\n        ]\n    );\n\n    final lastId = page1.documents[page1.documents.length - 1].$id;\n\n    final page2 = await databases.listDocuments(\n        databaseId: '<DATABASE_ID>',\n        collectionId: '<COLLECTION_ID>',\n        queries: [\n            Query.limit(25),\n            Query.cursorAfter(lastId)\n        ]\n    );\n\n}\n","children":"import 'package:clikkle/clikkle.dart';\n\nvoid main() async {\n    final client = Client()\n        .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n        .setProject('<PROJECT_ID>');\n\n    final databases = Databases(client);\n\n    final page1 = await databases.listDocuments(\n        databaseId: '<DATABASE_ID>',\n        collectionId: '<COLLECTION_ID>',\n        queries: [\n            Query.limit(25)\n        ]\n    );\n\n    final lastId = page1.documents[page1.documents.length - 1].$id;\n\n    final page2 = await databases.listDocuments(\n        databaseId: '<DATABASE_ID>',\n        collectionId: '<COLLECTION_ID>',\n        queries: [\n            Query.limit(25),\n            Query.cursorAfter(lastId)\n        ]\n    );\n\n}\n"}],["$","$L11",null,{"language":"client-apple","content":"import Clikkle\nimport ClikkleModels\n\nfunc main() async throws {\n    let client = Client()\n      .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n      .setProject(\"<PROJECT_ID>\")\n\n    let databases = Databases(client)\n\n    let page1 = try await databases.listDocuments(\n        databaseId: \"<DATABASE_ID>\",\n        collectionId: \"<COLLECTION_ID>\",\n        queries: [\n            Query.limit(25)\n        ]\n    )\n\n    let lastId = page1.documents[page1.documents.count - 1].$id\n\n    let page2 = try await databases.listDocuments(\n        databaseId: \"<DATABASE_ID>\",\n        collectionId: \"<COLLECTION_ID>\",\n        queries: [\n            Query.limit(25),\n            Query.cursorAfter(lastId)\n        ]\n    )\n}\n","children":"import Clikkle\nimport ClikkleModels\n\nfunc main() async throws {\n    let client = Client()\n      .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n      .setProject(\"<PROJECT_ID>\")\n\n    let databases = Databases(client)\n\n    let page1 = try await databases.listDocuments(\n        databaseId: \"<DATABASE_ID>\",\n        collectionId: \"<COLLECTION_ID>\",\n        queries: [\n            Query.limit(25)\n        ]\n    )\n\n    let lastId = page1.documents[page1.documents.count - 1].$id\n\n    let page2 = try await databases.listDocuments(\n        databaseId: \"<DATABASE_ID>\",\n        collectionId: \"<COLLECTION_ID>\",\n        queries: [\n            Query.limit(25),\n            Query.cursorAfter(lastId)\n        ]\n    )\n}\n"}],"$L21"]}]
19:["$","$Lf",null,{"id":"when-to-use","level":1,"children":"When to use what? "}]
1a:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Offset pagination should be used for collections that rarely change."," ","Offset paginations allow you to create indicator of the current page number and total page number."," ","For example, a list with up to 20 pages or static data like a list of countries or currencies."," ","Using offset pagination on large collections and frequently updated collections may result in slow performance and ",["$","strong",null,{"children":"missing and duplicate"}]," results."]}]
1b:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Cursor pagination should be used for frequently updated collections."," ","It is best suited for lazy-loaded pages with infinite scrolling."," ","For example, a feed, comment section, chat history, or high volume datasets."]}]
1c:["$","$L22",null,{}]
20:null
21:["$","$L11",null,{"language":"client-android-kotlin","content":"import android.util.Log\nimport io.clikkle.ClikkleException\nimport io.clikkle.Client\nimport io.clikkle.Query\nimport io.clikkle.services.Databases\n\nsuspend fun main() {\n    val client = Client(applicationContext)\n        .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n        .setProject(\"<PROJECT_ID>\")\n\n    val databases = Databases(client)\n\n    val page1 = databases.listDocuments(\n        databaseId = \"<DATABASE_ID>\",\n        collectionId = \"<COLLECTION_ID>\",\n        queries = [\n            Query.limit(25)\n        ]\n    )\n\n    val lastId = page1.documents[page1.documents.size - 1].$id\n\n    val page2 = databases.listDocuments(\n        databaseId = \"<DATABASE_ID>\",\n        collectionId = \"<COLLECTION_ID>\",\n        queries = [\n            Query.limit(25),\n            Query.cursorAfter(lastId)\n        ]\n    )\n}\n","children":"import android.util.Log\nimport io.clikkle.ClikkleException\nimport io.clikkle.Client\nimport io.clikkle.Query\nimport io.clikkle.services.Databases\n\nsuspend fun main() {\n    val client = Client(applicationContext)\n        .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n        .setProject(\"<PROJECT_ID>\")\n\n    val databases = Databases(client)\n\n    val page1 = databases.listDocuments(\n        databaseId = \"<DATABASE_ID>\",\n        collectionId = \"<COLLECTION_ID>\",\n        queries = [\n            Query.limit(25)\n        ]\n    )\n\n    val lastId = page1.documents[page1.documents.size - 1].$id\n\n    val page2 = databases.listDocuments(\n        databaseId = \"<DATABASE_ID>\",\n        collectionId = \"<COLLECTION_ID>\",\n        queries = [\n            Query.limit(25),\n            Query.cursorAfter(lastId)\n        ]\n    )\n}\n"}]
