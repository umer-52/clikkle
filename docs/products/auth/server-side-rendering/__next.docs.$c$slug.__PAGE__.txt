1:"$Sreact.fragment"
2:I[2650755,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsCopyPage"]
c:I[885369,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsArticleGridContent"]
d:I[6725962,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsProse"]
e:I[4673752,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsHeading"]
f:I[4522016,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],""]
3f:I[9595551,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsOnThisPage"]
40:I[3897367,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js"],"OutletBoundary"]
41:"$Sreact.suspense"
43:I[394047,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsTable"]
44:I[155313,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"MultiCode"]
45:I[9023822,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"MarkdocFence"]
47:I[394047,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"TableTag"]
49:I[5587564,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsFeedback"]
3:T5489,---
layout: article
title: SSR login
description: How to implement SSR authentication with Clikkle
---
Server-side rendering (SSR) is fully supported with Clikkle. You can use Clikkle with many SSR-oriented frameworks, such as Next.js, SvelteKit, Nuxt, Gatsby, Remix, and more.

SSR is a technique where the server renders a web page and sending the fully rendered page to the client's web browser. This is in contrast to client-side rendering (CSR), where the client's web browser renders the page using JavaScript.

This guide will walk you through the process of implementing an SSR application with Clikkle.

# SSR authentication flow {% #ssr-auth-flow %}

In client-side rendered web apps, a [Client SDK](/docs/sdks#client) is used to perform authentication directly from the client's web browser.

With server-side rendered web apps, a [Server SDK](/docs/sdks#server) is used to handle authentication against Clikkle. Authentication data is passed from the client's web browser to your server, and then your server makes requests to Clikkle on behalf of the client.

Here's a high-level overview of the authentication flow:

1. The user enters their credentials in their web browser.
2. The browser sends the credentials to your server.
3. Your server uses the Server SDK to authenticate the user with Clikkle.
4. If the authentication is successful, your server sends a session cookie to the client's web browser.
5. The client's web browser sends the session cookie to your server with subsequent request.
6. Your server uses the session cookie to make authenticated requests to Clikkle on behalf of the client.

{% only_dark %}
![CSR vs SSR flow diagram](/clikkle/images/docs/auth/ssr/dark/ssr.png)
{% /only_dark %}
{% only_light %}
![CSR vs SSR flow diagram](/clikkle/images/docs/auth/ssr/ssr.png)
{% /only_light %}

# Initialize clients {% #initialize-clients %}
{% info title="Server SDK required" %}
Server-side rendering requires a [Server SDK](/docs/sdks#server) instead of a Client SDK.
{% /info %}
In SSR, your server-side application will be making authentication requests to Clikkle and passing session cookies to your client-side app on the browser.

We'll need to initialize two Clikkle clients, one for admin requests and one for session-based requests.

## Admin client {% #admin-client %}

{% info title="Admin clients" %}
Admin clients should only be used if you need to perform admin actions that bypass permissions
or [unauthenticated requests that bypass rate limits](#rate-limits).
{% /info %}

To initialize the admin client, we'll need to first [generate an API key](/docs/advanced/platform/api-keys#create-api-key).
The API key should have the following scope in order to perform authentication:

| Category  {% width=120 %} | Required scopes    | Purpose |
|-----------|---------------------|---------|
| Sessions  | `sessions.write`    | Allows API key to create, update, and delete sessions. |

{% multicode %}
```server-nodejs
import { Client } from "node-clikkle"; // Using the server SDK

const adminClient = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    .setProject('<PROJECT_ID>')             // Your project ID
    .setKey('<YOUR_API_KEY>');                   // Your secret API key
```
```php
use Clikkle\Client;
use Clikkle\Services\Account;

$adminClient = (new Client())
    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    ->setProject('<PROJECT_ID>')             // Your project ID
    ->setKey('<YOUR_API_KEY>');                   // Your secret API key


```
```python
from clikkle.client import Client

admin_client = (Client()
                .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint \
                .set_project('<PROJECT_ID>') # Your project ID
                .set_key('<YOUR_API_KEY>') # Your secret API key
            )


```
{% /multicode %}

It is important to use an API key, as this will allow your server requests to bypass [rate limits](/docs/advanced/platform/rate-limits). If you don't use an API key, your server will be rate limited as if it were a client from a single IP address.

## Session client {% #session-client %}

The session client will be used to make requests to Clikkle on behalf of the end-user.
It will be initialized with the session, usually stored within a cookie.

You should create a new client for each request and **never** share the client between requests.

Use `a_session_<PROJECT_ID>` as the [cookie name](/docs/apis/rest#client-integration) and a [custom domain](/docs/advanced/platform/custom-domains) for your Clikkle endpoint if you want the session to work client-side as well.

{% multicode %}
```server-nodejs
const sessionClient = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    .setProject('<PROJECT_ID>');            // Your project ID

const session = req.cookies['a_session_<PROJECT_ID>']; // Get the session cookie from the request
if (session) {
    sessionClient.setSession(session);
}
```
```php
$sessionClient = (new Client())
    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    ->setProject('<PROJECT_ID>');            // Your project ID

$session = $_COOKIE['a_session_<PROJECT_ID>']; // Get the session cookie from the request
if ($session) {
    $sessionClient->setSession($session);
}
```

```python
from flask import request
from clikkle.client import Client

session_client = (Client()
                  .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint
                  .set_project('<PROJECT_ID>') # Your project ID
                )

# Get the session cookie from the request
session = request.cookies.get('a_session_<PROJECT_ID>')
if session:
    session_client.set_session(session)

```
{% /multicode %}

# Creating email/password sessions {% #creating-sessions %}

The most straightforward type of session to integrate is email/password.

Create an endpoint using your server's framework of choice that accepts a username and password, and then makes a request to Clikkle to create a session.
Once you have a session object, you can store it in a cookie. This will allow your users make authenticated requests to the Clikkle API from your server.

Use the `secret` property of the session object as the cookie value. The `expire` property of the session object should be used as the cookie's max age.
Here's an example with Express and PHP, but the same concepts apply to most frameworks.
{% multicode %}
```server-nodejs
import express from 'express';

// Initialize admin client here
// ...

app.post('/login', async (req, res) => {
    // Get email and password from request
    const { email, password } = req.body;

    const account = new Account(adminClient);

    try {
        // Create the session using the Clikkle client
        const session = await account.createEmailPasswordSession({
            email,
            password
        });

        // Set the session cookie
        res.cookie('a_session_<PROJECT_ID>', session.secret, { // use the session secret as the cookie value
            httpOnly: true,
            secure: true,
            sameSite: 'strict',
            expires: new Date(session.expire),
            path: '/',
        });

        res.status(200).json({ success: true });
    } catch (e) {
        res.status(400).json({ success: false, error: e.message });
    }
});
```

```php
<?php
// Initialize admin client here
// ...

// Get email and password from request
$email = $_POST['email'];
$password = $_POST['password'];

try {
    $account = new Account($adminClient);

    // Create the session using the Clikkle client
    $session = $account->createEmailPasswordSession($email, $password);

    // Set the session cookie
    setcookie('a_session_<PROJECT_ID>', $session['secret'], [
        'httpOnly' => true,
        'secure' => true,
        'sameSite' => 'strict',
        'expires' => strtotime($session['expire']),
        'path' => '/',
    ]);

    echo json_encode(['success' => true]);
} catch (Exception $e) {
    echo json_encode(['success' => false, 'error' => $e->getMessage()]);
}
```
```python
from flask import Flask, request, jsonify, make_response

# Initialize admin client here
# ...

@app.post('/login')
def login():
    body = request.json
    # Get email and password from request
    email = body['email']
    password = body['password']

    try:
        account = Account(admin_client)

        # Create the session using the Clikkle client
        session = account.create_email_password_session(email, password)
        resp = make_response(jsonify({'success': True}))

        # Set the session cookie
        resp.set_cookie('a_session_<PROJECT_ID>',
                        session['secret'],
                        httponly=True,
                        secure=True,
                        samesite='Strict',
                        expires=session['expire'],
                        path='/'
                    )
        return resp
    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 400
```
{% /multicode %}

We also recommend using the `httpOnly`, `secure`, and `sameSite` cookie options to ensure that the cookie is only sent over HTTPS,
and is not accessible to JavaScript. This will prevent XSS attacks.

# Making authenticated requests {% #making-authenticated-requests %}

Once a user has a session cookie, which will be set by the browser when it receives the `/login` endpoint's response, they can use it to make authenticated requests to your server.

To enable this, you will need to read the cookie value from the request, and then pass it to the Clikkle client, using the `setSession` helper.
When the browser makes a request to your domain's endpoints, it will automatically include session cookies.

{% multicode %}

```server-nodejs
// Initialize the session client here

app.get('/user', async (req, res) => {
    // First, read the session cookie from the request
    const session = req.cookies['a_session_<PROJECT_ID>'];

    // If the session cookie is not present, return an error
    if (!session) {
        return res.status(401).json({ success: false, error: 'Unauthorized' });
    }

    // Pass the session cookie to the Clikkle client
    sessionClient.setSession(session);

    // Now, you can make authenticated requests to the Clikkle API
    const account = new Account(sessionClient);
    try {
        const user = await account.get();

        res.status(200).json({ success: true, user });
    } catch (e) {
        res.status(400).json({ success: false, error: e.message });
    }
});
```
```php
<?php
// Initialize the session client here

// First, read the session cookie from the request
$session = $_COOKIE['a_session_<PROJECT_ID>'];

// If the session cookie is not present, return an error
if (!$session) {
    return http_response_code(401);
}

// Pass the session cookie to the Clikkle client
$sessionClient->setSession($session);
$account = new Account($sessionClient);

// Now, you can make authenticated requests to the Clikkle API
try {
    $user = $account->get();

    echo json_encode(['success' => true, 'user' => $user]);
} catch (Exception $e) {
    echo json_encode(['success' => false, 'error' => $e->getMessage()]);
}
```
```python
#  Initialize the session client here

@app.get('/user')
def get_user():
    #  First, read the session cookie from the request
    session = request.cookies.get('a_session_<PROJECT_ID>')

    # If the session cookie is not present, return an error
    if not session:
        return jsonify({'success': False, 'error': 'Unauthorized'}), 401

    # pass the session cookie to the Clikkle client
    session_client.set_session(session)
    account = Account(session_client)

    # Now, you can make authenticated requests to the Clikkle API
    try:
        user = account.get()
        return jsonify({'success': True, 'user': user})
    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 400

```
{% /multicode %}

# Rate limits {% #rate-limits %}
Unauthenticated requests are subject to [rate limits](/docs/advanced/platform/rate-limits).
Normally, rate limits are applied by an abuse key, which is usually a combination of IP and another factor like user ID.
When you make unauthenticated requests from your server, however, all requests originate from the same IP and no user ID is provided.
This means that all unauthenticated requests from your server will be **subject to the same rate limits**.

These rate limits protect your Clikkle server from abuse, if you need to make unauthenticated requests from your server,
there are ways to bypass rate limits.

# Making unauthenticated requests {% #making-unauthenticated-requests %}
Unauthenticated requests are used for displaying information to users before they log in.
For example some apps may display all public posts on the home page, and only show private posts to logged-in users.

There are two ways to make unauthenticated requests:

{% table %}
* Guest sessions
* Admin clients
---
* Uses the `createAnonymousSession` method to create a guest session.
* Uses an API key to bypass rate limits.
---
* Creates a session for unauthenticated users so each user has their own rate limit.
* Bypasses rate limits completely because API keys are not limited.
---
* Still respects access permissions.
* Also bypasses access permissions.
---
* Can be turned into a full session later by creating an account.
* Cannot be later turned into a full session.
{% /table %}

You can create a guest session using the `createAnonymousSession` method.
This will create a session for unauthenticated users, and each user will have their own rate limit.

{% multicode %}
```server-nodejs
const sdk = require('node-clikkle');

// Init SDK
const client = new sdk.Client();

const account = new sdk.Account(client);

client
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    .setProject('<PROJECT_ID>') // Your project ID
;

const promise = account.createAnonymousSession();

promise.then(function (response) {
    console.log(response);
}, function (error) {
    console.log(error);
});
```
```php
<?php

use Clikkle\Client;
use Clikkle\Services\Account;

$client = new Client();

$client
    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    ->setProject('<PROJECT_ID>') // Your project ID
;

$account = new Account($client);

$result = $account->createAnonymousSession();
```
```python
from clikkle.client import Client
from clikkle.services.account import Account

client = (Client()
            .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint
            .set_project('<PROJECT_ID>') # Your project ID
        )

account = Account(client)

result = account.create_anonymous_session()
```
{% /multicode %}

# Forwarding user agent {% #forwarding-user-agent %}

Clikkle sessions record some information about the client. To set this information in a server-side context use the `setForwardedUserAgent` to set the end-user's user agent. While optional, these can be useful for debugging and security purposes.

{% multicode %}
```server-nodejs
client.setForwardedUserAgent(req.headers['user-agent']);
```
```php
<?php
$client->setForwardedUserAgent($_SERVER['HTTP_USER_AGENT']);
```
```python
client.set_forwarded_user_agent(request.headers.get('user-agent'))
```
{% /multicode %}

# OAuth2 {% #oauth2 %}

Server-side OAuth2 authentication requires two server endpoints:

Create an initial endpoint that redirects the user to the OAuth2 provider's authentication page using Clikkle's `createOAuth2Token` method. After authenticating with the provider, the user will be redirected to the `success` URL with `userId` and `secret` URL parameters.

{% multicode %}
```server-nodejs
import { Client, Account, OAuthProvider } from "node-clikkle"; // Using the server SDK

const adminClient = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    .setProject('<PROJECT_ID>')             // Your project ID
    .setKey('<API_KEY>');                        // Your secret API key

app.get('/oauth', async (req, res) => {
    const account = new Account(adminClient);

    const redirectUrl = await account.createOAuth2Token({
        provider: OAuthProvider.Github,                // Provider
        success: 'https://example.com/oauth/success', // Success URL
        failure: 'https://example.com/oauth/failure', // Failure URL
    });

    res.redirect(redirectUrl);
});
```
```php
<?php
use Clikkle\Client;
use Clikkle\Services\Account;
use Clikkle\Enums\OAuthProvider;

$adminClient = (new Client())
    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    ->setProject('<PROJECT_ID>')             // Your project ID
    ->setKey('<API_KEY>');                        // Your secret API key

$account = new Account($adminClient);

$redirectUrl = $account->createOAuth2Token(
    OAuthProvider::GITHUB(),
    'https://example.com/oauth/success', // Success URL
    'https://example.com/oauth/failure', // Failure URL
);

header('Location' . $redirectUrl);
```
```python
from clikkle.client import Client
from clikkle.services.account import Account, OAuthProvider
from flask import Flask, request ,redirect, make_response, jsonify

admin_client = (Client()
                .set_endpoint('https://<REGION>.cloud.clikkle.io/v1')
                .set_project('<PROJECT_ID>')
                .set_key('<API_KEY>')
            )

@app.get('/oauth')
def oauth():
    account = Account(admin_client)

    redirect_url = account.create_o_auth2_token(
        OAuthProvider.Github,                # Provider
        'https://example.com/oauth/success', # Success URL
        'https://example.com/oauth/failure', # Failure URL
    )

    return redirect(redirect_url)
```
{% /multicode %}

Next, create a success callback endpoint that receives the `userId` and `secret` URL parameters, and then calls `createSession` on the server side. This endpoint returns a session object, which you can store in a cookie.

{% multicode %}
```server-nodejs
app.get('/oauth/success', async (req, res) => {
    const account = new Account(adminClient);

    // Get the userId and secret from the URL parameters
    const { userId, secret } = req.query;

    try {
        // Create the session using the Clikkle client
        const session = await account.createSession({
            userId,
            secret
        });

        // Set the session cookie
        res.cookie('a_session_<PROJECT_ID>', session.secret, { // Use the session secret as the cookie value
            httpOnly: true,
            secure: true,
            sameSite: 'strict',
            maxAge: sesion.expire
            path: '/',
        });

        res.status(200).json({ success: true });
    } catch (e) {
        res.status(400).json({ success: false, error: e.message });
    }
});
```
```php
<?php
use Clikkle\Client;
use Clikkle\Services\Account;

$adminClient = (new Client())
    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    ->setProject('<PROJECT_ID>')             // Your project ID
    ->setKey('<API_KEY>');                        // Your secret API key

$account = new Account($adminClient);

// Get the userId and secret from the URL parameters
$userId = $_GET['userId'];
$secret = $_GET['secret'];

try {
    // Create the session using the Clikkle client
    $session = $account->createSession($userId, $secret);

    // Set the session cookie
    setcookie('a_session_<PROJECT_ID>', $session['secret'], [
        'httpOnly' => true,
        'secure' => true,
        'sameSite' => 'strict',
        'expires' => strtotime($session['expire']),
        'path' => '/',
    ]);

    echo json_encode(['success' => true]);
} catch (Exception $e) {
    echo json_encode(['success' => false, 'error' => $e->getMessage()]);
}
```
```python
@app.get('/oauth/success')
def oauth_success():
    account = Account(admin_client)

    # Get the userId and secret from the URL parameters
    user_id = request.args.get('userId')
    secret = request.args.get('secret')

    try:
        # Create the session using the Clikkle client
        session = account.create_session(user_id, secret)

        # Set the session cookie
        res = make_response(jsonify({'success': True}))

        #  Set session cookie
        res.set_cookie(
            'a_session_<PROJECT_ID>',
            session['secret'],
            httponly=True,
            secure=True,
            samesite='Strict',
            max_age=session['expire'],
            path='/'
        )

        return res

    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 400
```
{% /multicode %}

Now the cookie is set, it will be passed to the server with subsequent requests, and you can use it to make authenticated requests to the Clikkle API on behalf of the end-user.

# Tutorials {% #tutorials %}
If you'd like to see SSR authentication implemented in a full auth example, see these tutorials.

{% cards %}
{% cards_item href="/docs/tutorials/nextjs-ssr-auth" title="Next.js SSR" icon="icon-nextjs"%}
{% /cards_item %}
{% cards_item href="/docs/tutorials/sveltekit-ssr-auth" title="SvelteKit SSR" icon="icon-svelte" %}
{% /cards_item %}
{% cards_item href="/docs/tutorials/nuxt-ssr-auth" title="Nuxt SSR" icon="icon-nuxt" %}
{% /cards_item %}
{% cards_item href="/docs/tutorials/astro-ssr-auth" title="Astro SSR" icon="icon-astro" %}
{% /cards_item %}
{% /cards %}

0:{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"contents","children":["$","article",null,{"className":"web-article contents","children":[["$","header",null,{"className":"web-article-header docs-article-header-stack","children":[["$","div",null,{"className":"docs-article-mobile-header-row mb-2 flex w-full items-center lg:hidden","children":[null,["$","div",null,{"className":"copy-button-wrapper-mobile ml-auto","children":["$","$L2",null,{"markdown":"$3","className":"ml-0"}]}]]}],null,"$L4"]}],"$L5","$L6"]}]}],["$L7","$L8","$L9"],"$La"]}],"isPartial":false,"staleTime":300,"varyParams":null,"buildId":"siKNZ69LNgpmcXavwmUTs"}
b:T5489,---
layout: article
title: SSR login
description: How to implement SSR authentication with Clikkle
---
Server-side rendering (SSR) is fully supported with Clikkle. You can use Clikkle with many SSR-oriented frameworks, such as Next.js, SvelteKit, Nuxt, Gatsby, Remix, and more.

SSR is a technique where the server renders a web page and sending the fully rendered page to the client's web browser. This is in contrast to client-side rendering (CSR), where the client's web browser renders the page using JavaScript.

This guide will walk you through the process of implementing an SSR application with Clikkle.

# SSR authentication flow {% #ssr-auth-flow %}

In client-side rendered web apps, a [Client SDK](/docs/sdks#client) is used to perform authentication directly from the client's web browser.

With server-side rendered web apps, a [Server SDK](/docs/sdks#server) is used to handle authentication against Clikkle. Authentication data is passed from the client's web browser to your server, and then your server makes requests to Clikkle on behalf of the client.

Here's a high-level overview of the authentication flow:

1. The user enters their credentials in their web browser.
2. The browser sends the credentials to your server.
3. Your server uses the Server SDK to authenticate the user with Clikkle.
4. If the authentication is successful, your server sends a session cookie to the client's web browser.
5. The client's web browser sends the session cookie to your server with subsequent request.
6. Your server uses the session cookie to make authenticated requests to Clikkle on behalf of the client.

{% only_dark %}
![CSR vs SSR flow diagram](/clikkle/images/docs/auth/ssr/dark/ssr.png)
{% /only_dark %}
{% only_light %}
![CSR vs SSR flow diagram](/clikkle/images/docs/auth/ssr/ssr.png)
{% /only_light %}

# Initialize clients {% #initialize-clients %}
{% info title="Server SDK required" %}
Server-side rendering requires a [Server SDK](/docs/sdks#server) instead of a Client SDK.
{% /info %}
In SSR, your server-side application will be making authentication requests to Clikkle and passing session cookies to your client-side app on the browser.

We'll need to initialize two Clikkle clients, one for admin requests and one for session-based requests.

## Admin client {% #admin-client %}

{% info title="Admin clients" %}
Admin clients should only be used if you need to perform admin actions that bypass permissions
or [unauthenticated requests that bypass rate limits](#rate-limits).
{% /info %}

To initialize the admin client, we'll need to first [generate an API key](/docs/advanced/platform/api-keys#create-api-key).
The API key should have the following scope in order to perform authentication:

| Category  {% width=120 %} | Required scopes    | Purpose |
|-----------|---------------------|---------|
| Sessions  | `sessions.write`    | Allows API key to create, update, and delete sessions. |

{% multicode %}
```server-nodejs
import { Client } from "node-clikkle"; // Using the server SDK

const adminClient = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    .setProject('<PROJECT_ID>')             // Your project ID
    .setKey('<YOUR_API_KEY>');                   // Your secret API key
```
```php
use Clikkle\Client;
use Clikkle\Services\Account;

$adminClient = (new Client())
    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    ->setProject('<PROJECT_ID>')             // Your project ID
    ->setKey('<YOUR_API_KEY>');                   // Your secret API key


```
```python
from clikkle.client import Client

admin_client = (Client()
                .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint \
                .set_project('<PROJECT_ID>') # Your project ID
                .set_key('<YOUR_API_KEY>') # Your secret API key
            )


```
{% /multicode %}

It is important to use an API key, as this will allow your server requests to bypass [rate limits](/docs/advanced/platform/rate-limits). If you don't use an API key, your server will be rate limited as if it were a client from a single IP address.

## Session client {% #session-client %}

The session client will be used to make requests to Clikkle on behalf of the end-user.
It will be initialized with the session, usually stored within a cookie.

You should create a new client for each request and **never** share the client between requests.

Use `a_session_<PROJECT_ID>` as the [cookie name](/docs/apis/rest#client-integration) and a [custom domain](/docs/advanced/platform/custom-domains) for your Clikkle endpoint if you want the session to work client-side as well.

{% multicode %}
```server-nodejs
const sessionClient = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    .setProject('<PROJECT_ID>');            // Your project ID

const session = req.cookies['a_session_<PROJECT_ID>']; // Get the session cookie from the request
if (session) {
    sessionClient.setSession(session);
}
```
```php
$sessionClient = (new Client())
    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    ->setProject('<PROJECT_ID>');            // Your project ID

$session = $_COOKIE['a_session_<PROJECT_ID>']; // Get the session cookie from the request
if ($session) {
    $sessionClient->setSession($session);
}
```

```python
from flask import request
from clikkle.client import Client

session_client = (Client()
                  .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint
                  .set_project('<PROJECT_ID>') # Your project ID
                )

# Get the session cookie from the request
session = request.cookies.get('a_session_<PROJECT_ID>')
if session:
    session_client.set_session(session)

```
{% /multicode %}

# Creating email/password sessions {% #creating-sessions %}

The most straightforward type of session to integrate is email/password.

Create an endpoint using your server's framework of choice that accepts a username and password, and then makes a request to Clikkle to create a session.
Once you have a session object, you can store it in a cookie. This will allow your users make authenticated requests to the Clikkle API from your server.

Use the `secret` property of the session object as the cookie value. The `expire` property of the session object should be used as the cookie's max age.
Here's an example with Express and PHP, but the same concepts apply to most frameworks.
{% multicode %}
```server-nodejs
import express from 'express';

// Initialize admin client here
// ...

app.post('/login', async (req, res) => {
    // Get email and password from request
    const { email, password } = req.body;

    const account = new Account(adminClient);

    try {
        // Create the session using the Clikkle client
        const session = await account.createEmailPasswordSession({
            email,
            password
        });

        // Set the session cookie
        res.cookie('a_session_<PROJECT_ID>', session.secret, { // use the session secret as the cookie value
            httpOnly: true,
            secure: true,
            sameSite: 'strict',
            expires: new Date(session.expire),
            path: '/',
        });

        res.status(200).json({ success: true });
    } catch (e) {
        res.status(400).json({ success: false, error: e.message });
    }
});
```

```php
<?php
// Initialize admin client here
// ...

// Get email and password from request
$email = $_POST['email'];
$password = $_POST['password'];

try {
    $account = new Account($adminClient);

    // Create the session using the Clikkle client
    $session = $account->createEmailPasswordSession($email, $password);

    // Set the session cookie
    setcookie('a_session_<PROJECT_ID>', $session['secret'], [
        'httpOnly' => true,
        'secure' => true,
        'sameSite' => 'strict',
        'expires' => strtotime($session['expire']),
        'path' => '/',
    ]);

    echo json_encode(['success' => true]);
} catch (Exception $e) {
    echo json_encode(['success' => false, 'error' => $e->getMessage()]);
}
```
```python
from flask import Flask, request, jsonify, make_response

# Initialize admin client here
# ...

@app.post('/login')
def login():
    body = request.json
    # Get email and password from request
    email = body['email']
    password = body['password']

    try:
        account = Account(admin_client)

        # Create the session using the Clikkle client
        session = account.create_email_password_session(email, password)
        resp = make_response(jsonify({'success': True}))

        # Set the session cookie
        resp.set_cookie('a_session_<PROJECT_ID>',
                        session['secret'],
                        httponly=True,
                        secure=True,
                        samesite='Strict',
                        expires=session['expire'],
                        path='/'
                    )
        return resp
    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 400
```
{% /multicode %}

We also recommend using the `httpOnly`, `secure`, and `sameSite` cookie options to ensure that the cookie is only sent over HTTPS,
and is not accessible to JavaScript. This will prevent XSS attacks.

# Making authenticated requests {% #making-authenticated-requests %}

Once a user has a session cookie, which will be set by the browser when it receives the `/login` endpoint's response, they can use it to make authenticated requests to your server.

To enable this, you will need to read the cookie value from the request, and then pass it to the Clikkle client, using the `setSession` helper.
When the browser makes a request to your domain's endpoints, it will automatically include session cookies.

{% multicode %}

```server-nodejs
// Initialize the session client here

app.get('/user', async (req, res) => {
    // First, read the session cookie from the request
    const session = req.cookies['a_session_<PROJECT_ID>'];

    // If the session cookie is not present, return an error
    if (!session) {
        return res.status(401).json({ success: false, error: 'Unauthorized' });
    }

    // Pass the session cookie to the Clikkle client
    sessionClient.setSession(session);

    // Now, you can make authenticated requests to the Clikkle API
    const account = new Account(sessionClient);
    try {
        const user = await account.get();

        res.status(200).json({ success: true, user });
    } catch (e) {
        res.status(400).json({ success: false, error: e.message });
    }
});
```
```php
<?php
// Initialize the session client here

// First, read the session cookie from the request
$session = $_COOKIE['a_session_<PROJECT_ID>'];

// If the session cookie is not present, return an error
if (!$session) {
    return http_response_code(401);
}

// Pass the session cookie to the Clikkle client
$sessionClient->setSession($session);
$account = new Account($sessionClient);

// Now, you can make authenticated requests to the Clikkle API
try {
    $user = $account->get();

    echo json_encode(['success' => true, 'user' => $user]);
} catch (Exception $e) {
    echo json_encode(['success' => false, 'error' => $e->getMessage()]);
}
```
```python
#  Initialize the session client here

@app.get('/user')
def get_user():
    #  First, read the session cookie from the request
    session = request.cookies.get('a_session_<PROJECT_ID>')

    # If the session cookie is not present, return an error
    if not session:
        return jsonify({'success': False, 'error': 'Unauthorized'}), 401

    # pass the session cookie to the Clikkle client
    session_client.set_session(session)
    account = Account(session_client)

    # Now, you can make authenticated requests to the Clikkle API
    try:
        user = account.get()
        return jsonify({'success': True, 'user': user})
    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 400

```
{% /multicode %}

# Rate limits {% #rate-limits %}
Unauthenticated requests are subject to [rate limits](/docs/advanced/platform/rate-limits).
Normally, rate limits are applied by an abuse key, which is usually a combination of IP and another factor like user ID.
When you make unauthenticated requests from your server, however, all requests originate from the same IP and no user ID is provided.
This means that all unauthenticated requests from your server will be **subject to the same rate limits**.

These rate limits protect your Clikkle server from abuse, if you need to make unauthenticated requests from your server,
there are ways to bypass rate limits.

# Making unauthenticated requests {% #making-unauthenticated-requests %}
Unauthenticated requests are used for displaying information to users before they log in.
For example some apps may display all public posts on the home page, and only show private posts to logged-in users.

There are two ways to make unauthenticated requests:

{% table %}
* Guest sessions
* Admin clients
---
* Uses the `createAnonymousSession` method to create a guest session.
* Uses an API key to bypass rate limits.
---
* Creates a session for unauthenticated users so each user has their own rate limit.
* Bypasses rate limits completely because API keys are not limited.
---
* Still respects access permissions.
* Also bypasses access permissions.
---
* Can be turned into a full session later by creating an account.
* Cannot be later turned into a full session.
{% /table %}

You can create a guest session using the `createAnonymousSession` method.
This will create a session for unauthenticated users, and each user will have their own rate limit.

{% multicode %}
```server-nodejs
const sdk = require('node-clikkle');

// Init SDK
const client = new sdk.Client();

const account = new sdk.Account(client);

client
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    .setProject('<PROJECT_ID>') // Your project ID
;

const promise = account.createAnonymousSession();

promise.then(function (response) {
    console.log(response);
}, function (error) {
    console.log(error);
});
```
```php
<?php

use Clikkle\Client;
use Clikkle\Services\Account;

$client = new Client();

$client
    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    ->setProject('<PROJECT_ID>') // Your project ID
;

$account = new Account($client);

$result = $account->createAnonymousSession();
```
```python
from clikkle.client import Client
from clikkle.services.account import Account

client = (Client()
            .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint
            .set_project('<PROJECT_ID>') # Your project ID
        )

account = Account(client)

result = account.create_anonymous_session()
```
{% /multicode %}

# Forwarding user agent {% #forwarding-user-agent %}

Clikkle sessions record some information about the client. To set this information in a server-side context use the `setForwardedUserAgent` to set the end-user's user agent. While optional, these can be useful for debugging and security purposes.

{% multicode %}
```server-nodejs
client.setForwardedUserAgent(req.headers['user-agent']);
```
```php
<?php
$client->setForwardedUserAgent($_SERVER['HTTP_USER_AGENT']);
```
```python
client.set_forwarded_user_agent(request.headers.get('user-agent'))
```
{% /multicode %}

# OAuth2 {% #oauth2 %}

Server-side OAuth2 authentication requires two server endpoints:

Create an initial endpoint that redirects the user to the OAuth2 provider's authentication page using Clikkle's `createOAuth2Token` method. After authenticating with the provider, the user will be redirected to the `success` URL with `userId` and `secret` URL parameters.

{% multicode %}
```server-nodejs
import { Client, Account, OAuthProvider } from "node-clikkle"; // Using the server SDK

const adminClient = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    .setProject('<PROJECT_ID>')             // Your project ID
    .setKey('<API_KEY>');                        // Your secret API key

app.get('/oauth', async (req, res) => {
    const account = new Account(adminClient);

    const redirectUrl = await account.createOAuth2Token({
        provider: OAuthProvider.Github,                // Provider
        success: 'https://example.com/oauth/success', // Success URL
        failure: 'https://example.com/oauth/failure', // Failure URL
    });

    res.redirect(redirectUrl);
});
```
```php
<?php
use Clikkle\Client;
use Clikkle\Services\Account;
use Clikkle\Enums\OAuthProvider;

$adminClient = (new Client())
    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    ->setProject('<PROJECT_ID>')             // Your project ID
    ->setKey('<API_KEY>');                        // Your secret API key

$account = new Account($adminClient);

$redirectUrl = $account->createOAuth2Token(
    OAuthProvider::GITHUB(),
    'https://example.com/oauth/success', // Success URL
    'https://example.com/oauth/failure', // Failure URL
);

header('Location' . $redirectUrl);
```
```python
from clikkle.client import Client
from clikkle.services.account import Account, OAuthProvider
from flask import Flask, request ,redirect, make_response, jsonify

admin_client = (Client()
                .set_endpoint('https://<REGION>.cloud.clikkle.io/v1')
                .set_project('<PROJECT_ID>')
                .set_key('<API_KEY>')
            )

@app.get('/oauth')
def oauth():
    account = Account(admin_client)

    redirect_url = account.create_o_auth2_token(
        OAuthProvider.Github,                # Provider
        'https://example.com/oauth/success', # Success URL
        'https://example.com/oauth/failure', # Failure URL
    )

    return redirect(redirect_url)
```
{% /multicode %}

Next, create a success callback endpoint that receives the `userId` and `secret` URL parameters, and then calls `createSession` on the server side. This endpoint returns a session object, which you can store in a cookie.

{% multicode %}
```server-nodejs
app.get('/oauth/success', async (req, res) => {
    const account = new Account(adminClient);

    // Get the userId and secret from the URL parameters
    const { userId, secret } = req.query;

    try {
        // Create the session using the Clikkle client
        const session = await account.createSession({
            userId,
            secret
        });

        // Set the session cookie
        res.cookie('a_session_<PROJECT_ID>', session.secret, { // Use the session secret as the cookie value
            httpOnly: true,
            secure: true,
            sameSite: 'strict',
            maxAge: sesion.expire
            path: '/',
        });

        res.status(200).json({ success: true });
    } catch (e) {
        res.status(400).json({ success: false, error: e.message });
    }
});
```
```php
<?php
use Clikkle\Client;
use Clikkle\Services\Account;

$adminClient = (new Client())
    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint
    ->setProject('<PROJECT_ID>')             // Your project ID
    ->setKey('<API_KEY>');                        // Your secret API key

$account = new Account($adminClient);

// Get the userId and secret from the URL parameters
$userId = $_GET['userId'];
$secret = $_GET['secret'];

try {
    // Create the session using the Clikkle client
    $session = $account->createSession($userId, $secret);

    // Set the session cookie
    setcookie('a_session_<PROJECT_ID>', $session['secret'], [
        'httpOnly' => true,
        'secure' => true,
        'sameSite' => 'strict',
        'expires' => strtotime($session['expire']),
        'path' => '/',
    ]);

    echo json_encode(['success' => true]);
} catch (Exception $e) {
    echo json_encode(['success' => false, 'error' => $e->getMessage()]);
}
```
```python
@app.get('/oauth/success')
def oauth_success():
    account = Account(admin_client)

    # Get the userId and secret from the URL parameters
    user_id = request.args.get('userId')
    secret = request.args.get('secret')

    try:
        # Create the session using the Clikkle client
        session = account.create_session(user_id, secret)

        # Set the session cookie
        res = make_response(jsonify({'success': True}))

        #  Set session cookie
        res.set_cookie(
            'a_session_<PROJECT_ID>',
            session['secret'],
            httponly=True,
            secure=True,
            samesite='Strict',
            max_age=session['expire'],
            path='/'
        )

        return res

    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 400
```
{% /multicode %}

Now the cookie is set, it will be passed to the server with subsequent requests, and you can use it to make authenticated requests to the Clikkle API on behalf of the end-user.

# Tutorials {% #tutorials %}
If you'd like to see SSR authentication implemented in a full auth example, see these tutorials.

{% cards %}
{% cards_item href="/docs/tutorials/nextjs-ssr-auth" title="Next.js SSR" icon="icon-nextjs"%}
{% /cards_item %}
{% cards_item href="/docs/tutorials/sveltekit-ssr-auth" title="SvelteKit SSR" icon="icon-svelte" %}
{% /cards_item %}
{% cards_item href="/docs/tutorials/nuxt-ssr-auth" title="Nuxt SSR" icon="icon-nuxt" %}
{% /cards_item %}
{% cards_item href="/docs/tutorials/astro-ssr-auth" title="Astro SSR" icon="icon-astro" %}
{% /cards_item %}
{% /cards %}

4:["$","div",null,{"className":"docs-article-title-actions-row","children":[["$","div",null,{"className":"relative flex min-w-0 flex-1 items-center","children":[null,["$","h1",null,{"className":"text-title m-0 font-aeonik-pro tracking-tight text-primary","children":"SSR login"}]]}],["$","div",null,{"className":"web-article-header-end copy-button-wrapper hidden shrink-0 lg:block","children":["$","$L2",null,{"markdown":"$b","className":"ml-0"}]}]]}]
5:["$","$Lc",null,{"className":"prose max-w-none","children":[null,["$","div",null,{"className":"docs-article-after-header pt-8","children":[null,null,["$","$Ld",null,{"isArticleLayout":true,"contentOnly":true,"children":["$","article",null,{"children":[["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Server-side rendering (SSR) is fully supported with Clikkle. You can use Clikkle with many SSR-oriented frameworks, such as Next.js, SvelteKit, Nuxt, Gatsby, Remix, and more."}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"SSR is a technique where the server renders a web page and sending the fully rendered page to the client's web browser. This is in contrast to client-side rendering (CSR), where the client's web browser renders the page using JavaScript."}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"This guide will walk you through the process of implementing an SSR application with Clikkle."}],["$","$Le",null,{"id":"ssr-auth-flow","level":1,"children":"SSR authentication flow "}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["In client-side rendered web apps, a ",["$","$Lf",null,{"href":"/docs/sdks#client","children":"Client SDK"}]," is used to perform authentication directly from the client's web browser."]}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["With server-side rendered web apps, a ",["$","$Lf",null,{"href":"/docs/sdks#server","children":"Server SDK"}]," is used to handle authentication against Clikkle. Authentication data is passed from the client's web browser to your server, and then your server makes requests to Clikkle on behalf of the client."]}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Here's a high-level overview of the authentication flow:"}],["$","ol",null,{"className":"web-numeric-list","children":[["$","li",null,{"children":"The user enters their credentials in their web browser."}],["$","li",null,{"children":"The browser sends the credentials to your server."}],["$","li",null,{"children":"Your server uses the Server SDK to authenticate the user with Clikkle."}],["$","li",null,{"children":"If the authentication is successful, your server sends a session cookie to the client's web browser."}],["$","li",null,{"children":"The client's web browser sends the session cookie to your server with subsequent request."}],["$","li",null,{"children":"Your server uses the session cookie to make authenticated requests to Clikkle on behalf of the client."}]]}],["$","span",null,{"className":"web-u-only-dark","children":["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["$","span",null,{"className":"web-media relative my-8 block","children":["$","img",null,{"src":"/clikkle/images/docs/auth/ssr/dark/ssr.png","alt":"CSR vs SSR flow diagram","className":"aspect-video w-full object-cover","decoding":"async","loading":"lazy"}]}]}]}],["$","span",null,{"className":"web-u-only-light","children":["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["$","span",null,{"className":"web-media relative my-8 block","children":["$","img",null,{"src":"/clikkle/images/docs/auth/ssr/ssr.png","alt":"CSR vs SSR flow diagram","className":"aspect-video w-full object-cover","decoding":"async","loading":"lazy"}]}]}]}],["$","$Le",null,{"id":"initialize-clients","level":1,"children":"Initialize clients "}],["$","div",null,{"className":"web-inline-info not-prose my-12!","children":[["$","span",null,{"className":"icon-info","aria-hidden":"true"}],["$","h5",null,{"className":"text-sub-body text-primary font-medium","children":"Server SDK required"}],"$L10"]}],"$L11","$L12","$L13","$L14","$L15","$L16","$L17","$L18","$L19","$L1a","$L1b","$L1c","$L1d","$L1e","$L1f","$L20","$L21","$L22","$L23","$L24","$L25","$L26","$L27","$L28","$L29","$L2a","$L2b","$L2c","$L2d","$L2e","$L2f","$L30","$L31","$L32","$L33","$L34","$L35","$L36","$L37","$L38","$L39","$L3a","$L3b","$L3c","$L3d"]}]}]]}],"$L3e"]}]
6:["$","aside",null,{"className":"web-references-menu ps-6","children":["$","$L3f",null,{"items":[{"id":"ssr-auth-flow","title":"SSR authentication flow","level":1},{"id":"initialize-clients","title":"Initialize clients","level":1},{"id":"admin-client","title":"Admin client","level":2},{"id":"session-client","title":"Session client","level":2},{"id":"creating-sessions","title":"Creating email/password sessions","level":1},{"id":"making-authenticated-requests","title":"Making authenticated requests","level":1},{"id":"rate-limits","title":"Rate limits","level":1},{"id":"making-unauthenticated-requests","title":"Making unauthenticated requests","level":1},{"id":"forwarding-user-agent","title":"Forwarding user agent","level":1},{"id":"oauth2","title":"OAuth2","level":1},{"id":"tutorials","title":"Tutorials","level":1}]}]}]
7:["$","script","script-0",{"src":"/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","async":true}]
8:["$","script","script-1",{"src":"/clikkle/_next/static/chunks/10iz33s~n0gnf.js","async":true}]
9:["$","script","script-2",{"src":"/clikkle/_next/static/chunks/17akn4e9sundm.js","async":true}]
a:["$","$L40",null,{"children":["$","$41",null,{"name":"Next.MetadataOutlet","children":"$@42"}]}]
10:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Server-side rendering requires a ",["$","$Lf",null,{"href":"/docs/sdks#server","children":"Server SDK"}]," instead of a Client SDK."]}]
11:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"In SSR, your server-side application will be making authentication requests to Clikkle and passing session cookies to your client-side app on the browser."}]
12:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"We'll need to initialize two Clikkle clients, one for admin requests and one for session-based requests."}]
13:["$","$Le",null,{"id":"admin-client","level":2,"children":"Admin client "}]
14:["$","div",null,{"className":"web-inline-info not-prose my-12!","children":[["$","span",null,{"className":"icon-info","aria-hidden":"true"}],["$","h5",null,{"className":"text-sub-body text-primary font-medium","children":"Admin clients"}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Admin clients should only be used if you need to perform admin actions that bypass permissions"," ","or ",["$","$Lf",null,{"href":"#rate-limits","children":"unauthenticated requests that bypass rate limits"}],"."]}]]}]
15:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["To initialize the admin client, we'll need to first ",["$","$Lf",null,{"href":"/docs/advanced/platform/api-keys#create-api-key","children":"generate an API key"}],"."," ","The API key should have the following scope in order to perform authentication:"]}]
16:["$","$L43",null,{"children":[["$","thead",null,{"className":"web-table-header","children":["$","tr",null,{"className":"web-table-row","children":[["$","td",null,{"role":"columnheader","className":"web-table-head-cell min-w-44 px-3 py-[0.5625rem] align-top","children":["$","span",null,{"className":"text-sm leading-[1.375rem] text-[var(--color-text-primary)] dark:text-white","children":"Category  "}]}],["$","td",null,{"role":"columnheader","className":"web-table-head-cell min-w-44 px-3 py-[0.5625rem] align-top","children":["$","span",null,{"className":"text-sm leading-[1.375rem] text-[var(--color-text-primary)] dark:text-white","children":"Required scopes"}]}],["$","td",null,{"role":"columnheader","className":"web-table-head-cell min-w-44 px-3 py-[0.5625rem] align-top","children":["$","span",null,{"className":"text-sm leading-[1.375rem] text-[var(--color-text-primary)] dark:text-white","children":"Purpose"}]}]]}]}],["$","tbody",null,{"className":"web-table-body","children":["$","tr",null,{"className":"web-table-row","children":[["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":"Sessions"}]}],["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":["$","code",null,{"children":"sessions.write"}]}]}],["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":"Allows API key to create, update, and delete sessions."}]}]]}]}]]}]
17:["$","$L44",null,{"children":[["$","$L45",null,{"language":"server-nodejs","content":"import { Client } from \"node-clikkle\"; // Using the server SDK\n\nconst adminClient = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    .setProject('<PROJECT_ID>')             // Your project ID\n    .setKey('<YOUR_API_KEY>');                   // Your secret API key\n","children":"import { Client } from \"node-clikkle\"; // Using the server SDK\n\nconst adminClient = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    .setProject('<PROJECT_ID>')             // Your project ID\n    .setKey('<YOUR_API_KEY>');                   // Your secret API key\n"}],["$","$L45",null,{"language":"php","content":"use Clikkle\\Client;\nuse Clikkle\\Services\\Account;\n\n$adminClient = (new Client())\n    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    ->setProject('<PROJECT_ID>')             // Your project ID\n    ->setKey('<YOUR_API_KEY>');                   // Your secret API key\n\n\n","children":"use Clikkle\\Client;\nuse Clikkle\\Services\\Account;\n\n$adminClient = (new Client())\n    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    ->setProject('<PROJECT_ID>')             // Your project ID\n    ->setKey('<YOUR_API_KEY>');                   // Your secret API key\n\n\n"}],["$","$L45",null,{"language":"python","content":"from clikkle.client import Client\n\nadmin_client = (Client()\n                .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint \\\n                .set_project('<PROJECT_ID>') # Your project ID\n                .set_key('<YOUR_API_KEY>') # Your secret API key\n            )\n\n\n","children":"from clikkle.client import Client\n\nadmin_client = (Client()\n                .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint \\\n                .set_project('<PROJECT_ID>') # Your project ID\n                .set_key('<YOUR_API_KEY>') # Your secret API key\n            )\n\n\n"}]]}]
18:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["It is important to use an API key, as this will allow your server requests to bypass ",["$","$Lf",null,{"href":"/docs/advanced/platform/rate-limits","children":"rate limits"}],". If you don't use an API key, your server will be rate limited as if it were a client from a single IP address."]}]
19:["$","$Le",null,{"id":"session-client","level":2,"children":"Session client "}]
1a:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["The session client will be used to make requests to Clikkle on behalf of the end-user."," ","It will be initialized with the session, usually stored within a cookie."]}]
1b:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["You should create a new client for each request and ",["$","strong",null,{"children":"never"}]," share the client between requests."]}]
1c:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Use ",["$","code",null,{"children":"a_session_<PROJECT_ID>"}]," as the ",["$","$Lf",null,{"href":"/docs/apis/rest#client-integration","children":"cookie name"}]," and a ",["$","$Lf",null,{"href":"/docs/advanced/platform/custom-domains","children":"custom domain"}]," for your Clikkle endpoint if you want the session to work client-side as well."]}]
1d:["$","$L44",null,{"children":[["$","$L45",null,{"language":"server-nodejs","content":"const sessionClient = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    .setProject('<PROJECT_ID>');            // Your project ID\n\nconst session = req.cookies['a_session_<PROJECT_ID>']; // Get the session cookie from the request\nif (session) {\n    sessionClient.setSession(session);\n}\n","children":"const sessionClient = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    .setProject('<PROJECT_ID>');            // Your project ID\n\nconst session = req.cookies['a_session_<PROJECT_ID>']; // Get the session cookie from the request\nif (session) {\n    sessionClient.setSession(session);\n}\n"}],["$","$L45",null,{"language":"php","content":"$$sessionClient = (new Client())\n    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    ->setProject('<PROJECT_ID>');            // Your project ID\n\n$session = $_COOKIE['a_session_<PROJECT_ID>']; // Get the session cookie from the request\nif ($session) {\n    $sessionClient->setSession($session);\n}\n","children":"$$sessionClient = (new Client())\n    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    ->setProject('<PROJECT_ID>');            // Your project ID\n\n$session = $_COOKIE['a_session_<PROJECT_ID>']; // Get the session cookie from the request\nif ($session) {\n    $sessionClient->setSession($session);\n}\n"}],["$","$L45",null,{"language":"python","content":"from flask import request\nfrom clikkle.client import Client\n\nsession_client = (Client()\n                  .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint\n                  .set_project('<PROJECT_ID>') # Your project ID\n                )\n\n# Get the session cookie from the request\nsession = request.cookies.get('a_session_<PROJECT_ID>')\nif session:\n    session_client.set_session(session)\n\n","children":"from flask import request\nfrom clikkle.client import Client\n\nsession_client = (Client()\n                  .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint\n                  .set_project('<PROJECT_ID>') # Your project ID\n                )\n\n# Get the session cookie from the request\nsession = request.cookies.get('a_session_<PROJECT_ID>')\nif session:\n    session_client.set_session(session)\n\n"}]]}]
1e:["$","$Le",null,{"id":"creating-sessions","level":1,"children":"Creating email/password sessions "}]
1f:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"The most straightforward type of session to integrate is email/password."}]
20:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Create an endpoint using your server's framework of choice that accepts a username and password, and then makes a request to Clikkle to create a session."," ","Once you have a session object, you can store it in a cookie. This will allow your users make authenticated requests to the Clikkle API from your server."]}]
21:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Use the ",["$","code",null,{"children":"secret"}]," property of the session object as the cookie value. The ",["$","code",null,{"children":"expire"}]," property of the session object should be used as the cookie's max age."," ","Here's an example with Express and PHP, but the same concepts apply to most frameworks."]}]
22:["$","$L44",null,{"children":[["$","$L45",null,{"language":"server-nodejs","content":"import express from 'express';\n\n// Initialize admin client here\n// ...\n\napp.post('/login', async (req, res) => {\n    // Get email and password from request\n    const { email, password } = req.body;\n\n    const account = new Account(adminClient);\n\n    try {\n        // Create the session using the Clikkle client\n        const session = await account.createEmailPasswordSession({\n            email,\n            password\n        });\n\n        // Set the session cookie\n        res.cookie('a_session_<PROJECT_ID>', session.secret, { // use the session secret as the cookie value\n            httpOnly: true,\n            secure: true,\n            sameSite: 'strict',\n            expires: new Date(session.expire),\n            path: '/',\n        });\n\n        res.status(200).json({ success: true });\n    } catch (e) {\n        res.status(400).json({ success: false, error: e.message });\n    }\n});\n","children":"import express from 'express';\n\n// Initialize admin client here\n// ...\n\napp.post('/login', async (req, res) => {\n    // Get email and password from request\n    const { email, password } = req.body;\n\n    const account = new Account(adminClient);\n\n    try {\n        // Create the session using the Clikkle client\n        const session = await account.createEmailPasswordSession({\n            email,\n            password\n        });\n\n        // Set the session cookie\n        res.cookie('a_session_<PROJECT_ID>', session.secret, { // use the session secret as the cookie value\n            httpOnly: true,\n            secure: true,\n            sameSite: 'strict',\n            expires: new Date(session.expire),\n            path: '/',\n        });\n\n        res.status(200).json({ success: true });\n    } catch (e) {\n        res.status(400).json({ success: false, error: e.message });\n    }\n});\n"}],["$","$L45",null,{"language":"php","content":"<?php\n// Initialize admin client here\n// ...\n\n// Get email and password from request\n$email = $_POST['email'];\n$password = $_POST['password'];\n\ntry {\n    $account = new Account($adminClient);\n\n    // Create the session using the Clikkle client\n    $session = $account->createEmailPasswordSession($email, $password);\n\n    // Set the session cookie\n    setcookie('a_session_<PROJECT_ID>', $session['secret'], [\n        'httpOnly' => true,\n        'secure' => true,\n        'sameSite' => 'strict',\n        'expires' => strtotime($session['expire']),\n        'path' => '/',\n    ]);\n\n    echo json_encode(['success' => true]);\n} catch (Exception $e) {\n    echo json_encode(['success' => false, 'error' => $e->getMessage()]);\n}\n","children":"<?php\n// Initialize admin client here\n// ...\n\n// Get email and password from request\n$email = $_POST['email'];\n$password = $_POST['password'];\n\ntry {\n    $account = new Account($adminClient);\n\n    // Create the session using the Clikkle client\n    $session = $account->createEmailPasswordSession($email, $password);\n\n    // Set the session cookie\n    setcookie('a_session_<PROJECT_ID>', $session['secret'], [\n        'httpOnly' => true,\n        'secure' => true,\n        'sameSite' => 'strict',\n        'expires' => strtotime($session['expire']),\n        'path' => '/',\n    ]);\n\n    echo json_encode(['success' => true]);\n} catch (Exception $e) {\n    echo json_encode(['success' => false, 'error' => $e->getMessage()]);\n}\n"}],"$L46"]}]
23:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["We also recommend using the ",["$","code",null,{"children":"httpOnly"}],", ",["$","code",null,{"children":"secure"}],", and ",["$","code",null,{"children":"sameSite"}]," cookie options to ensure that the cookie is only sent over HTTPS,"," ","and is not accessible to JavaScript. This will prevent XSS attacks."]}]
24:["$","$Le",null,{"id":"making-authenticated-requests","level":1,"children":"Making authenticated requests "}]
25:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Once a user has a session cookie, which will be set by the browser when it receives the ",["$","code",null,{"children":"/login"}]," endpoint's response, they can use it to make authenticated requests to your server."]}]
26:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["To enable this, you will need to read the cookie value from the request, and then pass it to the Clikkle client, using the ",["$","code",null,{"children":"setSession"}]," helper."," ","When the browser makes a request to your domain's endpoints, it will automatically include session cookies."]}]
27:["$","$L44",null,{"children":[["$","$L45",null,{"language":"server-nodejs","content":"// Initialize the session client here\n\napp.get('/user', async (req, res) => {\n    // First, read the session cookie from the request\n    const session = req.cookies['a_session_<PROJECT_ID>'];\n\n    // If the session cookie is not present, return an error\n    if (!session) {\n        return res.status(401).json({ success: false, error: 'Unauthorized' });\n    }\n\n    // Pass the session cookie to the Clikkle client\n    sessionClient.setSession(session);\n\n    // Now, you can make authenticated requests to the Clikkle API\n    const account = new Account(sessionClient);\n    try {\n        const user = await account.get();\n\n        res.status(200).json({ success: true, user });\n    } catch (e) {\n        res.status(400).json({ success: false, error: e.message });\n    }\n});\n","children":"// Initialize the session client here\n\napp.get('/user', async (req, res) => {\n    // First, read the session cookie from the request\n    const session = req.cookies['a_session_<PROJECT_ID>'];\n\n    // If the session cookie is not present, return an error\n    if (!session) {\n        return res.status(401).json({ success: false, error: 'Unauthorized' });\n    }\n\n    // Pass the session cookie to the Clikkle client\n    sessionClient.setSession(session);\n\n    // Now, you can make authenticated requests to the Clikkle API\n    const account = new Account(sessionClient);\n    try {\n        const user = await account.get();\n\n        res.status(200).json({ success: true, user });\n    } catch (e) {\n        res.status(400).json({ success: false, error: e.message });\n    }\n});\n"}],["$","$L45",null,{"language":"php","content":"<?php\n// Initialize the session client here\n\n// First, read the session cookie from the request\n$session = $_COOKIE['a_session_<PROJECT_ID>'];\n\n// If the session cookie is not present, return an error\nif (!$session) {\n    return http_response_code(401);\n}\n\n// Pass the session cookie to the Clikkle client\n$sessionClient->setSession($session);\n$account = new Account($sessionClient);\n\n// Now, you can make authenticated requests to the Clikkle API\ntry {\n    $user = $account->get();\n\n    echo json_encode(['success' => true, 'user' => $user]);\n} catch (Exception $e) {\n    echo json_encode(['success' => false, 'error' => $e->getMessage()]);\n}\n","children":"<?php\n// Initialize the session client here\n\n// First, read the session cookie from the request\n$session = $_COOKIE['a_session_<PROJECT_ID>'];\n\n// If the session cookie is not present, return an error\nif (!$session) {\n    return http_response_code(401);\n}\n\n// Pass the session cookie to the Clikkle client\n$sessionClient->setSession($session);\n$account = new Account($sessionClient);\n\n// Now, you can make authenticated requests to the Clikkle API\ntry {\n    $user = $account->get();\n\n    echo json_encode(['success' => true, 'user' => $user]);\n} catch (Exception $e) {\n    echo json_encode(['success' => false, 'error' => $e->getMessage()]);\n}\n"}],["$","$L45",null,{"language":"python","content":"#  Initialize the session client here\n\n@app.get('/user')\ndef get_user():\n    #  First, read the session cookie from the request\n    session = request.cookies.get('a_session_<PROJECT_ID>')\n\n    # If the session cookie is not present, return an error\n    if not session:\n        return jsonify({'success': False, 'error': 'Unauthorized'}), 401\n\n    # pass the session cookie to the Clikkle client\n    session_client.set_session(session)\n    account = Account(session_client)\n\n    # Now, you can make authenticated requests to the Clikkle API\n    try:\n        user = account.get()\n        return jsonify({'success': True, 'user': user})\n    except Exception as e:\n        return jsonify({'success': False, 'error': str(e)}), 400\n\n","children":"#  Initialize the session client here\n\n@app.get('/user')\ndef get_user():\n    #  First, read the session cookie from the request\n    session = request.cookies.get('a_session_<PROJECT_ID>')\n\n    # If the session cookie is not present, return an error\n    if not session:\n        return jsonify({'success': False, 'error': 'Unauthorized'}), 401\n\n    # pass the session cookie to the Clikkle client\n    session_client.set_session(session)\n    account = Account(session_client)\n\n    # Now, you can make authenticated requests to the Clikkle API\n    try:\n        user = account.get()\n        return jsonify({'success': True, 'user': user})\n    except Exception as e:\n        return jsonify({'success': False, 'error': str(e)}), 400\n\n"}]]}]
28:["$","$Le",null,{"id":"rate-limits","level":1,"children":"Rate limits "}]
29:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Unauthenticated requests are subject to ",["$","$Lf",null,{"href":"/docs/advanced/platform/rate-limits","children":"rate limits"}],"."," ","Normally, rate limits are applied by an abuse key, which is usually a combination of IP and another factor like user ID."," ","When you make unauthenticated requests from your server, however, all requests originate from the same IP and no user ID is provided."," ","This means that all unauthenticated requests from your server will be ",["$","strong",null,{"children":"subject to the same rate limits"}],"."]}]
2a:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["These rate limits protect your Clikkle server from abuse, if you need to make unauthenticated requests from your server,"," ","there are ways to bypass rate limits."]}]
2b:["$","$Le",null,{"id":"making-unauthenticated-requests","level":1,"children":"Making unauthenticated requests "}]
2c:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Unauthenticated requests are used for displaying information to users before they log in."," ","For example some apps may display all public posts on the home page, and only show private posts to logged-in users."]}]
2d:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"There are two ways to make unauthenticated requests:"}]
2e:["$","$L47",null,{"children":["$","$L43",null,{"children":[["$","thead",null,{"className":"web-table-header","children":["$","tr",null,{"className":"web-table-row","children":[["$","td",null,{"role":"columnheader","className":"web-table-head-cell min-w-44 px-3 py-[0.5625rem] align-top","children":["$","span",null,{"className":"text-sm leading-[1.375rem] text-[var(--color-text-primary)] dark:text-white","children":"Guest sessions"}]}],["$","td",null,{"role":"columnheader","className":"web-table-head-cell min-w-44 px-3 py-[0.5625rem] align-top","children":["$","span",null,{"className":"text-sm leading-[1.375rem] text-[var(--color-text-primary)] dark:text-white","children":"Admin clients"}]}]]}]}],["$","tbody",null,{"className":"web-table-body","children":[["$","tr",null,{"className":"web-table-row","children":[["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":["Uses the ",["$","code",null,{"children":"createAnonymousSession"}]," method to create a guest session."]}]}],["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":"Uses an API key to bypass rate limits."}]}]]}],["$","tr",null,{"className":"web-table-row","children":[["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":"Creates a session for unauthenticated users so each user has their own rate limit."}]}],["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":"Bypasses rate limits completely because API keys are not limited."}]}]]}],["$","tr",null,{"className":"web-table-row","children":[["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":"Still respects access permissions."}]}],["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":"Also bypasses access permissions."}]}]]}],["$","tr",null,{"className":"web-table-row","children":[["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":"Can be turned into a full session later by creating an account."}]}],["$","td",null,{"className":"web-table-col px-3 py-[0.5625rem] text-sm leading-[1.375rem] text-[var(--color-text-secondary)] dark:text-white/70","children":["$","div",null,{"className":"td-wrapper","children":"Cannot be later turned into a full session."}]}]]}]]}]]}]}]
2f:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["You can create a guest session using the ",["$","code",null,{"children":"createAnonymousSession"}]," method."," ","This will create a session for unauthenticated users, and each user will have their own rate limit."]}]
30:["$","$L44",null,{"children":[["$","$L45",null,{"language":"server-nodejs","content":"const sdk = require('node-clikkle');\n\n// Init SDK\nconst client = new sdk.Client();\n\nconst account = new sdk.Account(client);\n\nclient\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    .setProject('<PROJECT_ID>') // Your project ID\n;\n\nconst promise = account.createAnonymousSession();\n\npromise.then(function (response) {\n    console.log(response);\n}, function (error) {\n    console.log(error);\n});\n","children":"const sdk = require('node-clikkle');\n\n// Init SDK\nconst client = new sdk.Client();\n\nconst account = new sdk.Account(client);\n\nclient\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    .setProject('<PROJECT_ID>') // Your project ID\n;\n\nconst promise = account.createAnonymousSession();\n\npromise.then(function (response) {\n    console.log(response);\n}, function (error) {\n    console.log(error);\n});\n"}],["$","$L45",null,{"language":"php","content":"<?php\n\nuse Clikkle\\Client;\nuse Clikkle\\Services\\Account;\n\n$client = new Client();\n\n$client\n    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    ->setProject('<PROJECT_ID>') // Your project ID\n;\n\n$account = new Account($client);\n\n$result = $account->createAnonymousSession();\n","children":"<?php\n\nuse Clikkle\\Client;\nuse Clikkle\\Services\\Account;\n\n$client = new Client();\n\n$client\n    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    ->setProject('<PROJECT_ID>') // Your project ID\n;\n\n$account = new Account($client);\n\n$result = $account->createAnonymousSession();\n"}],["$","$L45",null,{"language":"python","content":"from clikkle.client import Client\nfrom clikkle.services.account import Account\n\nclient = (Client()\n            .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint\n            .set_project('<PROJECT_ID>') # Your project ID\n        )\n\naccount = Account(client)\n\nresult = account.create_anonymous_session()\n","children":"from clikkle.client import Client\nfrom clikkle.services.account import Account\n\nclient = (Client()\n            .set_endpoint('https://<REGION>.cloud.clikkle.io/v1') # Your API Endpoint\n            .set_project('<PROJECT_ID>') # Your project ID\n        )\n\naccount = Account(client)\n\nresult = account.create_anonymous_session()\n"}]]}]
31:["$","$Le",null,{"id":"forwarding-user-agent","level":1,"children":"Forwarding user agent "}]
32:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Clikkle sessions record some information about the client. To set this information in a server-side context use the ",["$","code",null,{"children":"setForwardedUserAgent"}]," to set the end-user's user agent. While optional, these can be useful for debugging and security purposes."]}]
33:["$","$L44",null,{"children":[["$","$L45",null,{"language":"server-nodejs","content":"client.setForwardedUserAgent(req.headers['user-agent']);\n","children":"client.setForwardedUserAgent(req.headers['user-agent']);\n"}],["$","$L45",null,{"language":"php","content":"<?php\n$client->setForwardedUserAgent($_SERVER['HTTP_USER_AGENT']);\n","children":"<?php\n$client->setForwardedUserAgent($_SERVER['HTTP_USER_AGENT']);\n"}],["$","$L45",null,{"language":"python","content":"client.set_forwarded_user_agent(request.headers.get('user-agent'))\n","children":"client.set_forwarded_user_agent(request.headers.get('user-agent'))\n"}]]}]
34:["$","$Le",null,{"id":"oauth2","level":1,"children":"OAuth2 "}]
35:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Server-side OAuth2 authentication requires two server endpoints:"}]
36:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Create an initial endpoint that redirects the user to the OAuth2 provider's authentication page using Clikkle's ",["$","code",null,{"children":"createOAuth2Token"}]," method. After authenticating with the provider, the user will be redirected to the ",["$","code",null,{"children":"success"}]," URL with ",["$","code",null,{"children":"userId"}]," and ",["$","code",null,{"children":"secret"}]," URL parameters."]}]
37:["$","$L44",null,{"children":[["$","$L45",null,{"language":"server-nodejs","content":"import { Client, Account, OAuthProvider } from \"node-clikkle\"; // Using the server SDK\n\nconst adminClient = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    .setProject('<PROJECT_ID>')             // Your project ID\n    .setKey('<API_KEY>');                        // Your secret API key\n\napp.get('/oauth', async (req, res) => {\n    const account = new Account(adminClient);\n\n    const redirectUrl = await account.createOAuth2Token({\n        provider: OAuthProvider.Github,                // Provider\n        success: 'https://example.com/oauth/success', // Success URL\n        failure: 'https://example.com/oauth/failure', // Failure URL\n    });\n\n    res.redirect(redirectUrl);\n});\n","children":"import { Client, Account, OAuthProvider } from \"node-clikkle\"; // Using the server SDK\n\nconst adminClient = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    .setProject('<PROJECT_ID>')             // Your project ID\n    .setKey('<API_KEY>');                        // Your secret API key\n\napp.get('/oauth', async (req, res) => {\n    const account = new Account(adminClient);\n\n    const redirectUrl = await account.createOAuth2Token({\n        provider: OAuthProvider.Github,                // Provider\n        success: 'https://example.com/oauth/success', // Success URL\n        failure: 'https://example.com/oauth/failure', // Failure URL\n    });\n\n    res.redirect(redirectUrl);\n});\n"}],["$","$L45",null,{"language":"php","content":"<?php\nuse Clikkle\\Client;\nuse Clikkle\\Services\\Account;\nuse Clikkle\\Enums\\OAuthProvider;\n\n$adminClient = (new Client())\n    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    ->setProject('<PROJECT_ID>')             // Your project ID\n    ->setKey('<API_KEY>');                        // Your secret API key\n\n$account = new Account($adminClient);\n\n$redirectUrl = $account->createOAuth2Token(\n    OAuthProvider::GITHUB(),\n    'https://example.com/oauth/success', // Success URL\n    'https://example.com/oauth/failure', // Failure URL\n);\n\nheader('Location' . $redirectUrl);\n","children":"<?php\nuse Clikkle\\Client;\nuse Clikkle\\Services\\Account;\nuse Clikkle\\Enums\\OAuthProvider;\n\n$adminClient = (new Client())\n    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    ->setProject('<PROJECT_ID>')             // Your project ID\n    ->setKey('<API_KEY>');                        // Your secret API key\n\n$account = new Account($adminClient);\n\n$redirectUrl = $account->createOAuth2Token(\n    OAuthProvider::GITHUB(),\n    'https://example.com/oauth/success', // Success URL\n    'https://example.com/oauth/failure', // Failure URL\n);\n\nheader('Location' . $redirectUrl);\n"}],["$","$L45",null,{"language":"python","content":"from clikkle.client import Client\nfrom clikkle.services.account import Account, OAuthProvider\nfrom flask import Flask, request ,redirect, make_response, jsonify\n\nadmin_client = (Client()\n                .set_endpoint('https://<REGION>.cloud.clikkle.io/v1')\n                .set_project('<PROJECT_ID>')\n                .set_key('<API_KEY>')\n            )\n\n@app.get('/oauth')\ndef oauth():\n    account = Account(admin_client)\n\n    redirect_url = account.create_o_auth2_token(\n        OAuthProvider.Github,                # Provider\n        'https://example.com/oauth/success', # Success URL\n        'https://example.com/oauth/failure', # Failure URL\n    )\n\n    return redirect(redirect_url)\n","children":"from clikkle.client import Client\nfrom clikkle.services.account import Account, OAuthProvider\nfrom flask import Flask, request ,redirect, make_response, jsonify\n\nadmin_client = (Client()\n                .set_endpoint('https://<REGION>.cloud.clikkle.io/v1')\n                .set_project('<PROJECT_ID>')\n                .set_key('<API_KEY>')\n            )\n\n@app.get('/oauth')\ndef oauth():\n    account = Account(admin_client)\n\n    redirect_url = account.create_o_auth2_token(\n        OAuthProvider.Github,                # Provider\n        'https://example.com/oauth/success', # Success URL\n        'https://example.com/oauth/failure', # Failure URL\n    )\n\n    return redirect(redirect_url)\n"}]]}]
38:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Next, create a success callback endpoint that receives the ",["$","code",null,{"children":"userId"}]," and ",["$","code",null,{"children":"secret"}]," URL parameters, and then calls ",["$","code",null,{"children":"createSession"}]," on the server side. This endpoint returns a session object, which you can store in a cookie."]}]
39:["$","$L44",null,{"children":[["$","$L45",null,{"language":"server-nodejs","content":"app.get('/oauth/success', async (req, res) => {\n    const account = new Account(adminClient);\n\n    // Get the userId and secret from the URL parameters\n    const { userId, secret } = req.query;\n\n    try {\n        // Create the session using the Clikkle client\n        const session = await account.createSession({\n            userId,\n            secret\n        });\n\n        // Set the session cookie\n        res.cookie('a_session_<PROJECT_ID>', session.secret, { // Use the session secret as the cookie value\n            httpOnly: true,\n            secure: true,\n            sameSite: 'strict',\n            maxAge: sesion.expire\n            path: '/',\n        });\n\n        res.status(200).json({ success: true });\n    } catch (e) {\n        res.status(400).json({ success: false, error: e.message });\n    }\n});\n","children":"app.get('/oauth/success', async (req, res) => {\n    const account = new Account(adminClient);\n\n    // Get the userId and secret from the URL parameters\n    const { userId, secret } = req.query;\n\n    try {\n        // Create the session using the Clikkle client\n        const session = await account.createSession({\n            userId,\n            secret\n        });\n\n        // Set the session cookie\n        res.cookie('a_session_<PROJECT_ID>', session.secret, { // Use the session secret as the cookie value\n            httpOnly: true,\n            secure: true,\n            sameSite: 'strict',\n            maxAge: sesion.expire\n            path: '/',\n        });\n\n        res.status(200).json({ success: true });\n    } catch (e) {\n        res.status(400).json({ success: false, error: e.message });\n    }\n});\n"}],["$","$L45",null,{"language":"php","content":"<?php\nuse Clikkle\\Client;\nuse Clikkle\\Services\\Account;\n\n$adminClient = (new Client())\n    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    ->setProject('<PROJECT_ID>')             // Your project ID\n    ->setKey('<API_KEY>');                        // Your secret API key\n\n$account = new Account($adminClient);\n\n// Get the userId and secret from the URL parameters\n$userId = $_GET['userId'];\n$secret = $_GET['secret'];\n\ntry {\n    // Create the session using the Clikkle client\n    $session = $account->createSession($userId, $secret);\n\n    // Set the session cookie\n    setcookie('a_session_<PROJECT_ID>', $session['secret'], [\n        'httpOnly' => true,\n        'secure' => true,\n        'sameSite' => 'strict',\n        'expires' => strtotime($session['expire']),\n        'path' => '/',\n    ]);\n\n    echo json_encode(['success' => true]);\n} catch (Exception $e) {\n    echo json_encode(['success' => false, 'error' => $e->getMessage()]);\n}\n","children":"<?php\nuse Clikkle\\Client;\nuse Clikkle\\Services\\Account;\n\n$adminClient = (new Client())\n    ->setEndpoint('https://<REGION>.cloud.clikkle.io/v1') // Your API Endpoint\n    ->setProject('<PROJECT_ID>')             // Your project ID\n    ->setKey('<API_KEY>');                        // Your secret API key\n\n$account = new Account($adminClient);\n\n// Get the userId and secret from the URL parameters\n$userId = $_GET['userId'];\n$secret = $_GET['secret'];\n\ntry {\n    // Create the session using the Clikkle client\n    $session = $account->createSession($userId, $secret);\n\n    // Set the session cookie\n    setcookie('a_session_<PROJECT_ID>', $session['secret'], [\n        'httpOnly' => true,\n        'secure' => true,\n        'sameSite' => 'strict',\n        'expires' => strtotime($session['expire']),\n        'path' => '/',\n    ]);\n\n    echo json_encode(['success' => true]);\n} catch (Exception $e) {\n    echo json_encode(['success' => false, 'error' => $e->getMessage()]);\n}\n"}],"$L48"]}]
3a:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Now the cookie is set, it will be passed to the server with subsequent requests, and you can use it to make authenticated requests to the Clikkle API on behalf of the end-user."}]
3b:["$","$Le",null,{"id":"tutorials","level":1,"children":"Tutorials "}]
3c:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"If you'd like to see SSR authentication implemented in a full auth example, see these tutorials."}]
3d:["$","div",null,{"className":"not-prose my-8 grid grid-cols-1 gap-8 md:grid-cols-2","children":[["$","$Lf",null,{"href":"/docs/tutorials/nextjs-ssr-auth","className":"bg-card border-smooth hover:bg-smooth !mb-0 flex flex-col gap-2 rounded-2xl border p-4 outline-0 transition-colors","children":[["$","header",null,{"className":"flex items-center gap-1","children":[["$","span",null,{"className":"icon-nextjs web-u-font-size-24","aria-hidden":"true"}],null,["$","h4",null,{"className":"text-sub-body text-primary font-medium","children":"Next.js SSR"}]]}],null]}],["$","$Lf",null,{"href":"/docs/tutorials/sveltekit-ssr-auth","className":"bg-card border-smooth hover:bg-smooth !mb-0 flex flex-col gap-2 rounded-2xl border p-4 outline-0 transition-colors","children":[["$","header",null,{"className":"flex items-center gap-1","children":[["$","span",null,{"className":"icon-svelte web-u-font-size-24","aria-hidden":"true"}],null,["$","h4",null,{"className":"text-sub-body text-primary font-medium","children":"SvelteKit SSR"}]]}],null]}],["$","$Lf",null,{"href":"/docs/tutorials/nuxt-ssr-auth","className":"bg-card border-smooth hover:bg-smooth !mb-0 flex flex-col gap-2 rounded-2xl border p-4 outline-0 transition-colors","children":[["$","header",null,{"className":"flex items-center gap-1","children":[["$","span",null,{"className":"icon-nuxt web-u-font-size-24","aria-hidden":"true"}],null,["$","h4",null,{"className":"text-sub-body text-primary font-medium","children":"Nuxt SSR"}]]}],null]}],["$","$Lf",null,{"href":"/docs/tutorials/astro-ssr-auth","className":"bg-card border-smooth hover:bg-smooth !mb-0 flex flex-col gap-2 rounded-2xl border p-4 outline-0 transition-colors","children":[["$","header",null,{"className":"flex items-center gap-1","children":[["$","span",null,{"className":"icon-astro web-u-font-size-24","aria-hidden":"true"}],null,["$","h4",null,{"className":"text-sub-body text-primary font-medium","children":"Astro SSR"}]]}],null]}]]}]
3e:["$","$L49",null,{}]
42:null
46:["$","$L45",null,{"language":"python","content":"from flask import Flask, request, jsonify, make_response\n\n# Initialize admin client here\n# ...\n\n@app.post('/login')\ndef login():\n    body = request.json\n    # Get email and password from request\n    email = body['email']\n    password = body['password']\n\n    try:\n        account = Account(admin_client)\n\n        # Create the session using the Clikkle client\n        session = account.create_email_password_session(email, password)\n        resp = make_response(jsonify({'success': True}))\n\n        # Set the session cookie\n        resp.set_cookie('a_session_<PROJECT_ID>',\n                        session['secret'],\n                        httponly=True,\n                        secure=True,\n                        samesite='Strict',\n                        expires=session['expire'],\n                        path='/'\n                    )\n        return resp\n    except Exception as e:\n        return jsonify({'success': False, 'error': str(e)}), 400\n","children":"from flask import Flask, request, jsonify, make_response\n\n# Initialize admin client here\n# ...\n\n@app.post('/login')\ndef login():\n    body = request.json\n    # Get email and password from request\n    email = body['email']\n    password = body['password']\n\n    try:\n        account = Account(admin_client)\n\n        # Create the session using the Clikkle client\n        session = account.create_email_password_session(email, password)\n        resp = make_response(jsonify({'success': True}))\n\n        # Set the session cookie\n        resp.set_cookie('a_session_<PROJECT_ID>',\n                        session['secret'],\n                        httponly=True,\n                        secure=True,\n                        samesite='Strict',\n                        expires=session['expire'],\n                        path='/'\n                    )\n        return resp\n    except Exception as e:\n        return jsonify({'success': False, 'error': str(e)}), 400\n"}]
48:["$","$L45",null,{"language":"python","content":"@app.get('/oauth/success')\ndef oauth_success():\n    account = Account(admin_client)\n\n    # Get the userId and secret from the URL parameters\n    user_id = request.args.get('userId')\n    secret = request.args.get('secret')\n\n    try:\n        # Create the session using the Clikkle client\n        session = account.create_session(user_id, secret)\n\n        # Set the session cookie\n        res = make_response(jsonify({'success': True}))\n\n        #  Set session cookie\n        res.set_cookie(\n            'a_session_<PROJECT_ID>',\n            session['secret'],\n            httponly=True,\n            secure=True,\n            samesite='Strict',\n            max_age=session['expire'],\n            path='/'\n        )\n\n        return res\n\n    except Exception as e:\n        return jsonify({'success': False, 'error': str(e)}), 400\n","children":"@app.get('/oauth/success')\ndef oauth_success():\n    account = Account(admin_client)\n\n    # Get the userId and secret from the URL parameters\n    user_id = request.args.get('userId')\n    secret = request.args.get('secret')\n\n    try:\n        # Create the session using the Clikkle client\n        session = account.create_session(user_id, secret)\n\n        # Set the session cookie\n        res = make_response(jsonify({'success': True}))\n\n        #  Set session cookie\n        res.set_cookie(\n            'a_session_<PROJECT_ID>',\n            session['secret'],\n            httponly=True,\n            secure=True,\n            samesite='Strict',\n            max_age=session['expire'],\n            path='/'\n        )\n\n        return res\n\n    except Exception as e:\n        return jsonify({'success': False, 'error': str(e)}), 400\n"}]
