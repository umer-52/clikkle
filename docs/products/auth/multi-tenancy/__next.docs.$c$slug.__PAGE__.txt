1:"$Sreact.fragment"
2:I[2650755,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsCopyPage"]
c:I[885369,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsArticleGridContent"]
d:I[6725962,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsProse"]
e:I[4522016,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],""]
f:I[4673752,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsHeading"]
10:I[155313,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"MultiCode"]
11:I[9023822,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"MarkdocFence"]
1f:I[9595551,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsOnThisPage"]
20:I[3897367,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js"],"OutletBoundary"]
21:"$Sreact.suspense"
27:I[5587564,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsFeedback"]
3:T2d0e,---
layout: article
title: Multi-tenancy with Teams
description: Learn how to implement multi-tenancy in your applications using Clikkle Teams.
---

Clikkle Teams provides an effective way to implement multi-tenancy in your applications. Create a team for each tenant to handle multi-tenant apps with built-in data isolation.

{% arrow_link href="/docs/products/auth/teams" %}
Learn more about Teams
{% /arrow_link %}

# What is multi-tenancy?

Multi-tenancy is a design pattern where a single instance of software serves multiple user groups (tenants). With Clikkle Teams, you can:

- Create a team for each tenant in your application
- Control access to resources using team-based permissions
- Define different roles within each tenant
- Scale to unlimited tenants without code changes

# Common use cases

- **SaaS applications**: Organizations that need isolated data and users
- **Collaborative tools**: Projects with different access levels
- **Educational platforms**: Schools with teachers and students
- **Business software**: Companies with department-based access control

# Create teams for tenants

When a new tenant signs up, create a dedicated team that serves as their isolated environment.

{% multicode %}
```client-web
import { Client, Teams, ID } from "clikkle";

const client = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

// Create team for a new tenant
const tenantTeam = await teams.create(
    'example_corp', // Team ID for tenant
    'Example Corp',   // Tenant name
    ['owner', 'admin', 'member'] // Tenant roles
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

// Create team for a new tenant
final tenantTeam = await teams.create(
    teamId: 'example_corp', // Team ID for tenant
    name: 'Example Corp',   // Tenant name
    roles: ['owner', 'admin', 'member'] // Tenant roles
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Create team for a new tenant
let tenantTeam = try await teams.create(
    teamId: "example_corp", // Team ID for tenant
    name: "Example Corp",   // Tenant name
    roles: ["owner", "admin", "member"] // Tenant roles
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.ID
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Create team for a new tenant
val tenantTeam = teams.create(
    teamId = "example_corp", // Team ID for tenant
    name = "Example Corp",   // Tenant name
    roles = listOf("owner", "admin", "member") // Tenant roles
)
```
{% /multicode %}

# Add members to tenants

Invite users to join a tenant using team memberships. Each member can be assigned different roles for access control.

{% multicode %}
```client-web
import { Client, Teams } from "clikkle";

const client = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

// Invite a member to the tenant
const membership = await teams.createMembership(
    'example_corp',      // Team/tenant ID
    ['admin'],           // Member's role in the tenant
    'user@example.com',  // User's email
    undefined,           // userId (optional)
    undefined,           // phone (optional)
    'https://example.com/accept-invite' // Redirect URL after accepting
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

// Invite a member to the tenant
final membership = await teams.createMembership(
    teamId: 'example_corp',   // Team/tenant ID
    roles: ['admin'],         // Member's role in the tenant
    email: 'user@example.com', // User's email
    url: 'https://example.com/accept-invite' // Redirect URL after accepting
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Invite a member to the tenant
let membership = try await teams.createMembership(
    teamId: "example_corp",    // Team/tenant ID
    roles: ["admin"],          // Member's role in the tenant
    email: "user@example.com", // User's email
    url: "https://example.com/accept-invite" // Redirect URL after accepting
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Invite a member to the tenant
val membership = teams.createMembership(
    teamId = "example_corp",    // Team/tenant ID
    roles = listOf("admin"),    // Member's role in the tenant
    email = "user@example.com", // User's email
    url = "https://example.com/accept-invite" // Redirect URL after accepting
)
```
{% /multicode %}

# Secure resources with team permissions

Control access to rows and resources using team-based permissions. This ensures data isolation between tenants.

{% multicode %}
```client-web
import { Client, TablesDB, ID, Permission, Role } from "clikkle";

const client = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const tablesDB = new TablesDB(client);

// Create a row that only members of "Example Corp" tenant can access
const row = await tablesDB.createRow(
    'invoices_db',
    'invoices',
    ID.unique(),
    {
        title: 'Q2 Invoice',
        amount: 2500.00,
        customer: 'Example Customer',
        status: 'pending',
        tenant_id: 'example_corp'
    },
    [
        // All Example Corp team members can read
        Permission.read(Role.team('example_corp')),

        // Only admins can update
        Permission.write(Role.team('example_corp', ['admin']))
    ]
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final tablesDB = TablesDB(client);

// Create a row that only members of "Example Corp" tenant can access
final row = await tablesDB.createRow(
    databaseId: 'invoices_db',
    tableId: 'invoices',
    rowId: ID.unique(),
    data: {
        'title': 'Q2 Invoice',
        'amount': 2500.00,
        'customer': 'Example Customer',
        'status': 'pending',
        'tenant_id': 'example_corp'
    },
    permissions: [
        // All Example Corp team members can read
        Permission.read(Role.team('example_corp')),

        // Only admins can update
        Permission.write(Role.team('example_corp', ['admin']))
    ]
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let tablesDB = TablesDB(client)

// Create a row that only members of "Example Corp" tenant can access
let row = try await tablesDB.createRow(
    databaseId: "invoices_db",
    tableId: "invoices",
    rowId: ID.unique(),
    data: [
        "title": "Q2 Invoice",
        "amount": 2500.00,
        "customer": "Example Customer",
        "status": "pending",
        "tenant_id": "example_corp"
    ],
    permissions: [
        // All Example Corp team members can read
        Permission.read(Role.team("example_corp")),

        // Only admins can update
        Permission.write(Role.team("example_corp", ["admin"]))
    ]
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.ID
import io.clikkle.services.TablesDB
import io.clikkle.models.Permission
import io.clikkle.models.Role

val client = Client(context)
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val tablesDB = TablesDB(client)

// Create a row that only members of "Example Corp" tenant can access
val row = tablesDB.createRow(
    databaseId = "invoices_db",
    tableId = "invoices",
    rowId = ID.unique(),
    data = mapOf(
        "title" to "Q2 Invoice",
        "amount" to 2500.00,
        "customer" to "Example Customer",
        "status" to "pending",
        "tenant_id" to "example_corp"
    ),
    permissions = listOf(
        // All Example Corp team members can read
        Permission.read(Role.team("example_corp")),

        // Only admins can update
        Permission.write(Role.team("example_corp", listOf("admin")))
    )
)
```
{% /multicode %}

# Query tenant data

When querying data, users will automatically only see rows they have permission to access based on their team memberships.

{% multicode %}
```client-web
import { Client, TablesDB, Query } from "clikkle";

const client = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const tablesDB = new TablesDB(client);

// Current user will only see invoices they have access to
const rows = await tablesDB.listRows(
    'invoices_db',
    'invoices'
);

// For specific tenant data, you can add a query filter
const tenantDocuments = await tablesDB.listRows(
    'invoices_db',
    'invoices',
    [
        Query.equal('tenant_id', 'example_corp')
    ]
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final tablesDB = TablesDB(client);

// Current user will only see invoices they have access to
final rows = await tablesDB.listRows(
    databaseId: 'invoices_db',
    tableId: 'invoices',
);

// For specific tenant data, you can add a query filter
final tenantDocuments = await tablesDB.listRows(
    databaseId: 'invoices_db',
    tableId: 'invoices',
    queries: [
        Query.equal('tenant_id', 'example_corp')
    ]
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let tablesDB = TablesDB(client)

// Current user will only see invoices they have access to
let rows = try await tablesDB.listRows(
    databaseId: "invoices_db",
    tableId: "invoices"
)

// For specific tenant data, you can add a query filter
let tenantDocuments = try await tablesDB.listRows(
    databaseId: "invoices_db",
    tableId: "invoices",
    queries: [
        Query.equal(key: "tenant_id", value: "example_corp")
    ]
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.TablesDB
import io.clikkle.Query

val client = Client(context)
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val tablesDB = TablesDB(client)

// Current user will only see invoices they have access to
val rows = tablesDB.listRows(
    databaseId = "invoices_db",
    tableId = "invoices"
)

// For specific tenant data, you can add a query filter
val tenantDocuments = tablesDB.listRows(
    databaseId = "invoices_db",
    tableId = "invoices",
    queries = listOf(
        Query.equal("tenant_id", "example_corp")
    )
)
```
{% /multicode %}

{% arrow_link href="/docs/products/auth/team-invites" %}
Learn how to manage team invitations
{% /arrow_link %}

0:{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"contents","children":["$","article",null,{"className":"web-article contents","children":[["$","header",null,{"className":"web-article-header docs-article-header-stack","children":[["$","div",null,{"className":"docs-article-mobile-header-row mb-2 flex w-full items-center lg:hidden","children":[null,["$","div",null,{"className":"copy-button-wrapper-mobile ml-auto","children":["$","$L2",null,{"markdown":"$3","className":"ml-0"}]}]]}],null,"$L4"]}],"$L5","$L6"]}]}],["$L7","$L8","$L9"],"$La"]}],"isPartial":false,"staleTime":300,"varyParams":null,"buildId":"siKNZ69LNgpmcXavwmUTs"}
b:T2d0e,---
layout: article
title: Multi-tenancy with Teams
description: Learn how to implement multi-tenancy in your applications using Clikkle Teams.
---

Clikkle Teams provides an effective way to implement multi-tenancy in your applications. Create a team for each tenant to handle multi-tenant apps with built-in data isolation.

{% arrow_link href="/docs/products/auth/teams" %}
Learn more about Teams
{% /arrow_link %}

# What is multi-tenancy?

Multi-tenancy is a design pattern where a single instance of software serves multiple user groups (tenants). With Clikkle Teams, you can:

- Create a team for each tenant in your application
- Control access to resources using team-based permissions
- Define different roles within each tenant
- Scale to unlimited tenants without code changes

# Common use cases

- **SaaS applications**: Organizations that need isolated data and users
- **Collaborative tools**: Projects with different access levels
- **Educational platforms**: Schools with teachers and students
- **Business software**: Companies with department-based access control

# Create teams for tenants

When a new tenant signs up, create a dedicated team that serves as their isolated environment.

{% multicode %}
```client-web
import { Client, Teams, ID } from "clikkle";

const client = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

// Create team for a new tenant
const tenantTeam = await teams.create(
    'example_corp', // Team ID for tenant
    'Example Corp',   // Tenant name
    ['owner', 'admin', 'member'] // Tenant roles
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

// Create team for a new tenant
final tenantTeam = await teams.create(
    teamId: 'example_corp', // Team ID for tenant
    name: 'Example Corp',   // Tenant name
    roles: ['owner', 'admin', 'member'] // Tenant roles
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Create team for a new tenant
let tenantTeam = try await teams.create(
    teamId: "example_corp", // Team ID for tenant
    name: "Example Corp",   // Tenant name
    roles: ["owner", "admin", "member"] // Tenant roles
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.ID
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Create team for a new tenant
val tenantTeam = teams.create(
    teamId = "example_corp", // Team ID for tenant
    name = "Example Corp",   // Tenant name
    roles = listOf("owner", "admin", "member") // Tenant roles
)
```
{% /multicode %}

# Add members to tenants

Invite users to join a tenant using team memberships. Each member can be assigned different roles for access control.

{% multicode %}
```client-web
import { Client, Teams } from "clikkle";

const client = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

// Invite a member to the tenant
const membership = await teams.createMembership(
    'example_corp',      // Team/tenant ID
    ['admin'],           // Member's role in the tenant
    'user@example.com',  // User's email
    undefined,           // userId (optional)
    undefined,           // phone (optional)
    'https://example.com/accept-invite' // Redirect URL after accepting
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

// Invite a member to the tenant
final membership = await teams.createMembership(
    teamId: 'example_corp',   // Team/tenant ID
    roles: ['admin'],         // Member's role in the tenant
    email: 'user@example.com', // User's email
    url: 'https://example.com/accept-invite' // Redirect URL after accepting
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Invite a member to the tenant
let membership = try await teams.createMembership(
    teamId: "example_corp",    // Team/tenant ID
    roles: ["admin"],          // Member's role in the tenant
    email: "user@example.com", // User's email
    url: "https://example.com/accept-invite" // Redirect URL after accepting
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Invite a member to the tenant
val membership = teams.createMembership(
    teamId = "example_corp",    // Team/tenant ID
    roles = listOf("admin"),    // Member's role in the tenant
    email = "user@example.com", // User's email
    url = "https://example.com/accept-invite" // Redirect URL after accepting
)
```
{% /multicode %}

# Secure resources with team permissions

Control access to rows and resources using team-based permissions. This ensures data isolation between tenants.

{% multicode %}
```client-web
import { Client, TablesDB, ID, Permission, Role } from "clikkle";

const client = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const tablesDB = new TablesDB(client);

// Create a row that only members of "Example Corp" tenant can access
const row = await tablesDB.createRow(
    'invoices_db',
    'invoices',
    ID.unique(),
    {
        title: 'Q2 Invoice',
        amount: 2500.00,
        customer: 'Example Customer',
        status: 'pending',
        tenant_id: 'example_corp'
    },
    [
        // All Example Corp team members can read
        Permission.read(Role.team('example_corp')),

        // Only admins can update
        Permission.write(Role.team('example_corp', ['admin']))
    ]
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final tablesDB = TablesDB(client);

// Create a row that only members of "Example Corp" tenant can access
final row = await tablesDB.createRow(
    databaseId: 'invoices_db',
    tableId: 'invoices',
    rowId: ID.unique(),
    data: {
        'title': 'Q2 Invoice',
        'amount': 2500.00,
        'customer': 'Example Customer',
        'status': 'pending',
        'tenant_id': 'example_corp'
    },
    permissions: [
        // All Example Corp team members can read
        Permission.read(Role.team('example_corp')),

        // Only admins can update
        Permission.write(Role.team('example_corp', ['admin']))
    ]
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let tablesDB = TablesDB(client)

// Create a row that only members of "Example Corp" tenant can access
let row = try await tablesDB.createRow(
    databaseId: "invoices_db",
    tableId: "invoices",
    rowId: ID.unique(),
    data: [
        "title": "Q2 Invoice",
        "amount": 2500.00,
        "customer": "Example Customer",
        "status": "pending",
        "tenant_id": "example_corp"
    ],
    permissions: [
        // All Example Corp team members can read
        Permission.read(Role.team("example_corp")),

        // Only admins can update
        Permission.write(Role.team("example_corp", ["admin"]))
    ]
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.ID
import io.clikkle.services.TablesDB
import io.clikkle.models.Permission
import io.clikkle.models.Role

val client = Client(context)
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val tablesDB = TablesDB(client)

// Create a row that only members of "Example Corp" tenant can access
val row = tablesDB.createRow(
    databaseId = "invoices_db",
    tableId = "invoices",
    rowId = ID.unique(),
    data = mapOf(
        "title" to "Q2 Invoice",
        "amount" to 2500.00,
        "customer" to "Example Customer",
        "status" to "pending",
        "tenant_id" to "example_corp"
    ),
    permissions = listOf(
        // All Example Corp team members can read
        Permission.read(Role.team("example_corp")),

        // Only admins can update
        Permission.write(Role.team("example_corp", listOf("admin")))
    )
)
```
{% /multicode %}

# Query tenant data

When querying data, users will automatically only see rows they have permission to access based on their team memberships.

{% multicode %}
```client-web
import { Client, TablesDB, Query } from "clikkle";

const client = new Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const tablesDB = new TablesDB(client);

// Current user will only see invoices they have access to
const rows = await tablesDB.listRows(
    'invoices_db',
    'invoices'
);

// For specific tenant data, you can add a query filter
const tenantDocuments = await tablesDB.listRows(
    'invoices_db',
    'invoices',
    [
        Query.equal('tenant_id', 'example_corp')
    ]
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final tablesDB = TablesDB(client);

// Current user will only see invoices they have access to
final rows = await tablesDB.listRows(
    databaseId: 'invoices_db',
    tableId: 'invoices',
);

// For specific tenant data, you can add a query filter
final tenantDocuments = await tablesDB.listRows(
    databaseId: 'invoices_db',
    tableId: 'invoices',
    queries: [
        Query.equal('tenant_id', 'example_corp')
    ]
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let tablesDB = TablesDB(client)

// Current user will only see invoices they have access to
let rows = try await tablesDB.listRows(
    databaseId: "invoices_db",
    tableId: "invoices"
)

// For specific tenant data, you can add a query filter
let tenantDocuments = try await tablesDB.listRows(
    databaseId: "invoices_db",
    tableId: "invoices",
    queries: [
        Query.equal(key: "tenant_id", value: "example_corp")
    ]
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.TablesDB
import io.clikkle.Query

val client = Client(context)
    .setEndpoint("https://<REGION>.cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val tablesDB = TablesDB(client)

// Current user will only see invoices they have access to
val rows = tablesDB.listRows(
    databaseId = "invoices_db",
    tableId = "invoices"
)

// For specific tenant data, you can add a query filter
val tenantDocuments = tablesDB.listRows(
    databaseId = "invoices_db",
    tableId = "invoices",
    queries = listOf(
        Query.equal("tenant_id", "example_corp")
    )
)
```
{% /multicode %}

{% arrow_link href="/docs/products/auth/team-invites" %}
Learn how to manage team invitations
{% /arrow_link %}

4:["$","div",null,{"className":"docs-article-title-actions-row","children":[["$","div",null,{"className":"relative flex min-w-0 flex-1 items-center","children":[null,["$","h1",null,{"className":"text-title m-0 font-aeonik-pro tracking-tight text-primary","children":"Multi-tenancy with Teams"}]]}],["$","div",null,{"className":"web-article-header-end copy-button-wrapper hidden shrink-0 lg:block","children":["$","$L2",null,{"markdown":"$b","className":"ml-0"}]}]]}]
5:["$","$Lc",null,{"className":"prose max-w-none","children":[null,["$","div",null,{"className":"docs-article-after-header pt-8","children":[null,null,["$","$Ld",null,{"isArticleLayout":true,"contentOnly":true,"children":["$","article",null,{"children":[["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Clikkle Teams provides an effective way to implement multi-tenancy in your applications. Create a team for each tenant to handle multi-tenant apps with built-in data isolation."}],["$","$Le",null,{"href":"/docs/products/auth/teams","className":"not-prose text-paragraph-lg text-primary group mt-8 flex items-center gap-1 first:mt-0","children":[["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Learn more about Teams"}],["$","span",null,{"className":"web-icon-chevron-right inline-block transition-transform duration-100 group-hover:translate-x-0.5","aria-hidden":"true"}]]}],["$","$Lf",null,{"level":1,"children":"What is multi-tenancy?"}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Multi-tenancy is a design pattern where a single instance of software serves multiple user groups (tenants). With Clikkle Teams, you can:"}],["$","ul",null,{"className":"web-pink-dots","children":[["$","li",null,{"children":"Create a team for each tenant in your application"}],["$","li",null,{"children":"Control access to resources using team-based permissions"}],["$","li",null,{"children":"Define different roles within each tenant"}],["$","li",null,{"children":"Scale to unlimited tenants without code changes"}]]}],["$","$Lf",null,{"level":1,"children":"Common use cases"}],["$","ul",null,{"className":"web-pink-dots","children":[["$","li",null,{"children":[["$","strong",null,{"children":"SaaS applications"}],": Organizations that need isolated data and users"]}],["$","li",null,{"children":[["$","strong",null,{"children":"Collaborative tools"}],": Projects with different access levels"]}],["$","li",null,{"children":[["$","strong",null,{"children":"Educational platforms"}],": Schools with teachers and students"]}],["$","li",null,{"children":[["$","strong",null,{"children":"Business software"}],": Companies with department-based access control"]}]]}],["$","$Lf",null,{"level":1,"children":"Create teams for tenants"}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"When a new tenant signs up, create a dedicated team that serves as their isolated environment."}],["$","$L10",null,{"children":[["$","$L11",null,{"language":"client-web","content":"import { Client, Teams, ID } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\n// Create team for a new tenant\nconst tenantTeam = await teams.create(\n    'example_corp', // Team ID for tenant\n    'Example Corp',   // Tenant name\n    ['owner', 'admin', 'member'] // Tenant roles\n);\n","children":"import { Client, Teams, ID } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\n// Create team for a new tenant\nconst tenantTeam = await teams.create(\n    'example_corp', // Team ID for tenant\n    'Example Corp',   // Tenant name\n    ['owner', 'admin', 'member'] // Tenant roles\n);\n"}],["$","$L11",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\n// Create team for a new tenant\nfinal tenantTeam = await teams.create(\n    teamId: 'example_corp', // Team ID for tenant\n    name: 'Example Corp',   // Tenant name\n    roles: ['owner', 'admin', 'member'] // Tenant roles\n);\n","children":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\n// Create team for a new tenant\nfinal tenantTeam = await teams.create(\n    teamId: 'example_corp', // Team ID for tenant\n    name: 'Example Corp',   // Tenant name\n    roles: ['owner', 'admin', 'member'] // Tenant roles\n);\n"}],"$L12","$L13"]}],"$L14","$L15","$L16","$L17","$L18","$L19","$L1a","$L1b","$L1c","$L1d"]}]}]]}],"$L1e"]}]
6:["$","aside",null,{"className":"web-references-menu ps-6","children":["$","$L1f",null,{"items":[{"id":"what-is-multi-tenancy","title":"What is multi-tenancy?","level":1},{"id":"common-use-cases","title":"Common use cases","level":1},{"id":"create-teams-for-tenants","title":"Create teams for tenants","level":1},{"id":"add-members-to-tenants","title":"Add members to tenants","level":1},{"id":"secure-resources-with-team-permissions","title":"Secure resources with team permissions","level":1},{"id":"query-tenant-data","title":"Query tenant data","level":1}]}]}]
7:["$","script","script-0",{"src":"/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","async":true}]
8:["$","script","script-1",{"src":"/clikkle/_next/static/chunks/10iz33s~n0gnf.js","async":true}]
9:["$","script","script-2",{"src":"/clikkle/_next/static/chunks/17akn4e9sundm.js","async":true}]
a:["$","$L20",null,{"children":["$","$21",null,{"name":"Next.MetadataOutlet","children":"$@22"}]}]
12:["$","$L11",null,{"language":"client-apple","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Create team for a new tenant\nlet tenantTeam = try await teams.create(\n    teamId: \"example_corp\", // Team ID for tenant\n    name: \"Example Corp\",   // Tenant name\n    roles: [\"owner\", \"admin\", \"member\"] // Tenant roles\n)\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Create team for a new tenant\nlet tenantTeam = try await teams.create(\n    teamId: \"example_corp\", // Team ID for tenant\n    name: \"Example Corp\",   // Tenant name\n    roles: [\"owner\", \"admin\", \"member\"] // Tenant roles\n)\n"}]
13:["$","$L11",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.ID\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Create team for a new tenant\nval tenantTeam = teams.create(\n    teamId = \"example_corp\", // Team ID for tenant\n    name = \"Example Corp\",   // Tenant name\n    roles = listOf(\"owner\", \"admin\", \"member\") // Tenant roles\n)\n","children":"import io.clikkle.Client\nimport io.clikkle.ID\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Create team for a new tenant\nval tenantTeam = teams.create(\n    teamId = \"example_corp\", // Team ID for tenant\n    name = \"Example Corp\",   // Tenant name\n    roles = listOf(\"owner\", \"admin\", \"member\") // Tenant roles\n)\n"}]
14:["$","$Lf",null,{"level":1,"children":"Add members to tenants"}]
15:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Invite users to join a tenant using team memberships. Each member can be assigned different roles for access control."}]
16:["$","$L10",null,{"children":[["$","$L11",null,{"language":"client-web","content":"import { Client, Teams } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\n// Invite a member to the tenant\nconst membership = await teams.createMembership(\n    'example_corp',      // Team/tenant ID\n    ['admin'],           // Member's role in the tenant\n    'user@example.com',  // User's email\n    undefined,           // userId (optional)\n    undefined,           // phone (optional)\n    'https://example.com/accept-invite' // Redirect URL after accepting\n);\n","children":"import { Client, Teams } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\n// Invite a member to the tenant\nconst membership = await teams.createMembership(\n    'example_corp',      // Team/tenant ID\n    ['admin'],           // Member's role in the tenant\n    'user@example.com',  // User's email\n    undefined,           // userId (optional)\n    undefined,           // phone (optional)\n    'https://example.com/accept-invite' // Redirect URL after accepting\n);\n"}],["$","$L11",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\n// Invite a member to the tenant\nfinal membership = await teams.createMembership(\n    teamId: 'example_corp',   // Team/tenant ID\n    roles: ['admin'],         // Member's role in the tenant\n    email: 'user@example.com', // User's email\n    url: 'https://example.com/accept-invite' // Redirect URL after accepting\n);\n","children":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\n// Invite a member to the tenant\nfinal membership = await teams.createMembership(\n    teamId: 'example_corp',   // Team/tenant ID\n    roles: ['admin'],         // Member's role in the tenant\n    email: 'user@example.com', // User's email\n    url: 'https://example.com/accept-invite' // Redirect URL after accepting\n);\n"}],["$","$L11",null,{"language":"client-apple","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Invite a member to the tenant\nlet membership = try await teams.createMembership(\n    teamId: \"example_corp\",    // Team/tenant ID\n    roles: [\"admin\"],          // Member's role in the tenant\n    email: \"user@example.com\", // User's email\n    url: \"https://example.com/accept-invite\" // Redirect URL after accepting\n)\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Invite a member to the tenant\nlet membership = try await teams.createMembership(\n    teamId: \"example_corp\",    // Team/tenant ID\n    roles: [\"admin\"],          // Member's role in the tenant\n    email: \"user@example.com\", // User's email\n    url: \"https://example.com/accept-invite\" // Redirect URL after accepting\n)\n"}],"$L23"]}]
17:["$","$Lf",null,{"level":1,"children":"Secure resources with team permissions"}]
18:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Control access to rows and resources using team-based permissions. This ensures data isolation between tenants."}]
19:["$","$L10",null,{"children":[["$","$L11",null,{"language":"client-web","content":"import { Client, TablesDB, ID, Permission, Role } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst tablesDB = new TablesDB(client);\n\n// Create a row that only members of \"Example Corp\" tenant can access\nconst row = await tablesDB.createRow(\n    'invoices_db',\n    'invoices',\n    ID.unique(),\n    {\n        title: 'Q2 Invoice',\n        amount: 2500.00,\n        customer: 'Example Customer',\n        status: 'pending',\n        tenant_id: 'example_corp'\n    },\n    [\n        // All Example Corp team members can read\n        Permission.read(Role.team('example_corp')),\n\n        // Only admins can update\n        Permission.write(Role.team('example_corp', ['admin']))\n    ]\n);\n","children":"import { Client, TablesDB, ID, Permission, Role } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst tablesDB = new TablesDB(client);\n\n// Create a row that only members of \"Example Corp\" tenant can access\nconst row = await tablesDB.createRow(\n    'invoices_db',\n    'invoices',\n    ID.unique(),\n    {\n        title: 'Q2 Invoice',\n        amount: 2500.00,\n        customer: 'Example Customer',\n        status: 'pending',\n        tenant_id: 'example_corp'\n    },\n    [\n        // All Example Corp team members can read\n        Permission.read(Role.team('example_corp')),\n\n        // Only admins can update\n        Permission.write(Role.team('example_corp', ['admin']))\n    ]\n);\n"}],["$","$L11",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal tablesDB = TablesDB(client);\n\n// Create a row that only members of \"Example Corp\" tenant can access\nfinal row = await tablesDB.createRow(\n    databaseId: 'invoices_db',\n    tableId: 'invoices',\n    rowId: ID.unique(),\n    data: {\n        'title': 'Q2 Invoice',\n        'amount': 2500.00,\n        'customer': 'Example Customer',\n        'status': 'pending',\n        'tenant_id': 'example_corp'\n    },\n    permissions: [\n        // All Example Corp team members can read\n        Permission.read(Role.team('example_corp')),\n\n        // Only admins can update\n        Permission.write(Role.team('example_corp', ['admin']))\n    ]\n);\n","children":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal tablesDB = TablesDB(client);\n\n// Create a row that only members of \"Example Corp\" tenant can access\nfinal row = await tablesDB.createRow(\n    databaseId: 'invoices_db',\n    tableId: 'invoices',\n    rowId: ID.unique(),\n    data: {\n        'title': 'Q2 Invoice',\n        'amount': 2500.00,\n        'customer': 'Example Customer',\n        'status': 'pending',\n        'tenant_id': 'example_corp'\n    },\n    permissions: [\n        // All Example Corp team members can read\n        Permission.read(Role.team('example_corp')),\n\n        // Only admins can update\n        Permission.write(Role.team('example_corp', ['admin']))\n    ]\n);\n"}],"$L24","$L25"]}]
1a:["$","$Lf",null,{"level":1,"children":"Query tenant data"}]
1b:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"When querying data, users will automatically only see rows they have permission to access based on their team memberships."}]
1c:["$","$L10",null,{"children":[["$","$L11",null,{"language":"client-web","content":"import { Client, TablesDB, Query } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst tablesDB = new TablesDB(client);\n\n// Current user will only see invoices they have access to\nconst rows = await tablesDB.listRows(\n    'invoices_db',\n    'invoices'\n);\n\n// For specific tenant data, you can add a query filter\nconst tenantDocuments = await tablesDB.listRows(\n    'invoices_db',\n    'invoices',\n    [\n        Query.equal('tenant_id', 'example_corp')\n    ]\n);\n","children":"import { Client, TablesDB, Query } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst tablesDB = new TablesDB(client);\n\n// Current user will only see invoices they have access to\nconst rows = await tablesDB.listRows(\n    'invoices_db',\n    'invoices'\n);\n\n// For specific tenant data, you can add a query filter\nconst tenantDocuments = await tablesDB.listRows(\n    'invoices_db',\n    'invoices',\n    [\n        Query.equal('tenant_id', 'example_corp')\n    ]\n);\n"}],["$","$L11",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal tablesDB = TablesDB(client);\n\n// Current user will only see invoices they have access to\nfinal rows = await tablesDB.listRows(\n    databaseId: 'invoices_db',\n    tableId: 'invoices',\n);\n\n// For specific tenant data, you can add a query filter\nfinal tenantDocuments = await tablesDB.listRows(\n    databaseId: 'invoices_db',\n    tableId: 'invoices',\n    queries: [\n        Query.equal('tenant_id', 'example_corp')\n    ]\n);\n","children":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://<REGION>.cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal tablesDB = TablesDB(client);\n\n// Current user will only see invoices they have access to\nfinal rows = await tablesDB.listRows(\n    databaseId: 'invoices_db',\n    tableId: 'invoices',\n);\n\n// For specific tenant data, you can add a query filter\nfinal tenantDocuments = await tablesDB.listRows(\n    databaseId: 'invoices_db',\n    tableId: 'invoices',\n    queries: [\n        Query.equal('tenant_id', 'example_corp')\n    ]\n);\n"}],["$","$L11",null,{"language":"client-apple","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet tablesDB = TablesDB(client)\n\n// Current user will only see invoices they have access to\nlet rows = try await tablesDB.listRows(\n    databaseId: \"invoices_db\",\n    tableId: \"invoices\"\n)\n\n// For specific tenant data, you can add a query filter\nlet tenantDocuments = try await tablesDB.listRows(\n    databaseId: \"invoices_db\",\n    tableId: \"invoices\",\n    queries: [\n        Query.equal(key: \"tenant_id\", value: \"example_corp\")\n    ]\n)\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet tablesDB = TablesDB(client)\n\n// Current user will only see invoices they have access to\nlet rows = try await tablesDB.listRows(\n    databaseId: \"invoices_db\",\n    tableId: \"invoices\"\n)\n\n// For specific tenant data, you can add a query filter\nlet tenantDocuments = try await tablesDB.listRows(\n    databaseId: \"invoices_db\",\n    tableId: \"invoices\",\n    queries: [\n        Query.equal(key: \"tenant_id\", value: \"example_corp\")\n    ]\n)\n"}],"$L26"]}]
1d:["$","$Le",null,{"href":"/docs/products/auth/team-invites","className":"not-prose text-paragraph-lg text-primary group mt-8 flex items-center gap-1 first:mt-0","children":[["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Learn how to manage team invitations"}],["$","span",null,{"className":"web-icon-chevron-right inline-block transition-transform duration-100 group-hover:translate-x-0.5","aria-hidden":"true"}]]}]
1e:["$","$L27",null,{}]
22:null
23:["$","$L11",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Invite a member to the tenant\nval membership = teams.createMembership(\n    teamId = \"example_corp\",    // Team/tenant ID\n    roles = listOf(\"admin\"),    // Member's role in the tenant\n    email = \"user@example.com\", // User's email\n    url = \"https://example.com/accept-invite\" // Redirect URL after accepting\n)\n","children":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Invite a member to the tenant\nval membership = teams.createMembership(\n    teamId = \"example_corp\",    // Team/tenant ID\n    roles = listOf(\"admin\"),    // Member's role in the tenant\n    email = \"user@example.com\", // User's email\n    url = \"https://example.com/accept-invite\" // Redirect URL after accepting\n)\n"}]
24:["$","$L11",null,{"language":"client-apple","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet tablesDB = TablesDB(client)\n\n// Create a row that only members of \"Example Corp\" tenant can access\nlet row = try await tablesDB.createRow(\n    databaseId: \"invoices_db\",\n    tableId: \"invoices\",\n    rowId: ID.unique(),\n    data: [\n        \"title\": \"Q2 Invoice\",\n        \"amount\": 2500.00,\n        \"customer\": \"Example Customer\",\n        \"status\": \"pending\",\n        \"tenant_id\": \"example_corp\"\n    ],\n    permissions: [\n        // All Example Corp team members can read\n        Permission.read(Role.team(\"example_corp\")),\n\n        // Only admins can update\n        Permission.write(Role.team(\"example_corp\", [\"admin\"]))\n    ]\n)\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet tablesDB = TablesDB(client)\n\n// Create a row that only members of \"Example Corp\" tenant can access\nlet row = try await tablesDB.createRow(\n    databaseId: \"invoices_db\",\n    tableId: \"invoices\",\n    rowId: ID.unique(),\n    data: [\n        \"title\": \"Q2 Invoice\",\n        \"amount\": 2500.00,\n        \"customer\": \"Example Customer\",\n        \"status\": \"pending\",\n        \"tenant_id\": \"example_corp\"\n    ],\n    permissions: [\n        // All Example Corp team members can read\n        Permission.read(Role.team(\"example_corp\")),\n\n        // Only admins can update\n        Permission.write(Role.team(\"example_corp\", [\"admin\"]))\n    ]\n)\n"}]
25:["$","$L11",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.ID\nimport io.clikkle.services.TablesDB\nimport io.clikkle.models.Permission\nimport io.clikkle.models.Role\n\nval client = Client(context)\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval tablesDB = TablesDB(client)\n\n// Create a row that only members of \"Example Corp\" tenant can access\nval row = tablesDB.createRow(\n    databaseId = \"invoices_db\",\n    tableId = \"invoices\",\n    rowId = ID.unique(),\n    data = mapOf(\n        \"title\" to \"Q2 Invoice\",\n        \"amount\" to 2500.00,\n        \"customer\" to \"Example Customer\",\n        \"status\" to \"pending\",\n        \"tenant_id\" to \"example_corp\"\n    ),\n    permissions = listOf(\n        // All Example Corp team members can read\n        Permission.read(Role.team(\"example_corp\")),\n\n        // Only admins can update\n        Permission.write(Role.team(\"example_corp\", listOf(\"admin\")))\n    )\n)\n","children":"import io.clikkle.Client\nimport io.clikkle.ID\nimport io.clikkle.services.TablesDB\nimport io.clikkle.models.Permission\nimport io.clikkle.models.Role\n\nval client = Client(context)\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval tablesDB = TablesDB(client)\n\n// Create a row that only members of \"Example Corp\" tenant can access\nval row = tablesDB.createRow(\n    databaseId = \"invoices_db\",\n    tableId = \"invoices\",\n    rowId = ID.unique(),\n    data = mapOf(\n        \"title\" to \"Q2 Invoice\",\n        \"amount\" to 2500.00,\n        \"customer\" to \"Example Customer\",\n        \"status\" to \"pending\",\n        \"tenant_id\" to \"example_corp\"\n    ),\n    permissions = listOf(\n        // All Example Corp team members can read\n        Permission.read(Role.team(\"example_corp\")),\n\n        // Only admins can update\n        Permission.write(Role.team(\"example_corp\", listOf(\"admin\")))\n    )\n)\n"}]
26:["$","$L11",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.services.TablesDB\nimport io.clikkle.Query\n\nval client = Client(context)\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval tablesDB = TablesDB(client)\n\n// Current user will only see invoices they have access to\nval rows = tablesDB.listRows(\n    databaseId = \"invoices_db\",\n    tableId = \"invoices\"\n)\n\n// For specific tenant data, you can add a query filter\nval tenantDocuments = tablesDB.listRows(\n    databaseId = \"invoices_db\",\n    tableId = \"invoices\",\n    queries = listOf(\n        Query.equal(\"tenant_id\", \"example_corp\")\n    )\n)\n","children":"import io.clikkle.Client\nimport io.clikkle.services.TablesDB\nimport io.clikkle.Query\n\nval client = Client(context)\n    .setEndpoint(\"https://<REGION>.cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval tablesDB = TablesDB(client)\n\n// Current user will only see invoices they have access to\nval rows = tablesDB.listRows(\n    databaseId = \"invoices_db\",\n    tableId = \"invoices\"\n)\n\n// For specific tenant data, you can add a query filter\nval tenantDocuments = tablesDB.listRows(\n    databaseId = \"invoices_db\",\n    tableId = \"invoices\",\n    queries = listOf(\n        Query.equal(\"tenant_id\", \"example_corp\")\n    )\n)\n"}]
