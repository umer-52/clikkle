1:"$Sreact.fragment"
2:I[2650755,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsCopyPage"]
c:I[885369,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsArticleGridContent"]
d:I[6725962,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsProse"]
e:I[4673752,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsHeading"]
f:I[155313,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"MultiCode"]
10:I[9023822,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"MarkdocFence"]
34:I[9595551,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsOnThisPage"]
35:I[3897367,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js"],"OutletBoundary"]
36:"$Sreact.suspense"
3b:I[4522016,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],""]
3c:I[5587564,["/clikkle/_next/static/chunks/0eb6pgor2p~6n.js","/clikkle/_next/static/chunks/17r0g7m.bbwki.js","/clikkle/_next/static/chunks/0q30rjhmp-mu1.js","/clikkle/_next/static/chunks/0r7ng.v1rzx53.js","/clikkle/_next/static/chunks/0nfmdhdpiedhl.js","/clikkle/_next/static/chunks/0uc~ek0j9637p.js","/clikkle/_next/static/chunks/15ze6m6dfqfrw.js","/clikkle/_next/static/chunks/155wdmir5cdcz.js","/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","/clikkle/_next/static/chunks/10iz33s~n0gnf.js","/clikkle/_next/static/chunks/17akn4e9sundm.js"],"DocsFeedback"]
3:T4b7a,---
layout: article
title: Team invites
description: Learn how to manage team invites in Clikkle. Implement both client-side email invites and server-side custom flows for team memberships.
---

Clikkle provides two approaches for adding members to teams: client-side email invites and server-side custom flows. Each approach serves different use cases and offers unique benefits.

# Invite client-side

Client-side email invites are perfect for implementing user-to-user invitations, allowing your users to invite others to join their teams, organizations, or shared resources. When creating a membership, Clikkle:
1. Creates a new user account if one doesn't exist for the email address
2. Sends an automated email invitation to the user
3. Creates a pending membership
4. Activates the membership when the user accepts

Client-side invites are ideal when you want a simple, automated process that lets your users manage their own team invitations.
Clikkle handles the email delivery with built-in templates and localization support, making it easy to implement a standard invite acceptance flow with email verification.

{% multicode %}
```client-web
import { Client, Teams } from "clikkle";

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

// Create membership with email invite
const membership = await teams.createMembership(
    '<TEAM_ID>',
    ['developer'],     // roles
    'user@example.com', // email
    undefined,         // userId (optional)
    undefined,         // phone (optional)
    'https://yourapp.com/accept-invite' // url - redirect after email click
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

// Create membership with email invite
final membership = await teams.createMembership(
    teamId: '<TEAM_ID>',
    roles: ['developer'],
    email: 'user@example.com',
    url: 'https://yourapp.com/accept-invite' // redirect after email click
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1") // Your API Endpoint
    .setProject("<PROJECT_ID>") // Your project ID

let teams = Teams(client)

// Create membership with email invite
let membership = try await teams.createMembership(
    teamId: "<TEAM_ID>",
    roles: ["developer"],
    email: "user@example.com",
    url: "https://yourapp.com/accept-invite" // redirect after email click
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1") // Your API Endpoint
    .setProject("<PROJECT_ID>") // Your project ID

val teams = Teams(client)

// Create membership with email invite
val response = teams.createMembership(
    teamId = "<TEAM_ID>",
    roles = listOf("developer"),
    email = "user@example.com",
    url = "https://yourapp.com/accept-invite" // redirect after email click
)
```
{% /multicode %}

## Accept invitations

For client-side email invites, users must accept the invitation to join the team. The acceptance flow:
1. User receives an email with an invitation link containing a secret token
2. Clicking the link redirects to your app
3. Your app calls the acceptance endpoint
4. Upon success, the user gains immediate access

{% multicode %}
```client-web
import { Client, Teams } from "clikkle";

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

// Accept the invitation using the membership ID and secret
const response = await teams.updateMembershipStatus(
    '<TEAM_ID>',
    '<MEMBERSHIP_ID>',
    '<USER_ID>',
    '<SECRET>'
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

// Accept the invitation using the membership ID and secret
final response = await teams.updateMembershipStatus(
    teamId: '<TEAM_ID>',
    membershipId: '<MEMBERSHIP_ID>',
    userId: '<USER_ID>',
    secret: '<SECRET>'
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Accept the invitation using the membership ID and secret
let response = try await teams.updateMembershipStatus(
    teamId: "<TEAM_ID>",
    membershipId: "<MEMBERSHIP_ID>",
    userId: "<USER_ID>",
    secret: "<SECRET>"
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Accept the invitation using the membership ID and secret
val response = teams.updateMembershipStatus(
    teamId = "<TEAM_ID>",
    membershipId = "<MEMBERSHIP_ID>",
    userId = "<USER_ID>",
    secret = "<SECRET>"
)
```
{% /multicode %}

# Server-side custom flows

Server-side membership creation bypasses the email invitation process, allowing direct member addition. This approach:
1. Creates an active membership immediately
2. Doesn't require user acceptance
3. Gives you complete control over the invitation workflow

This makes them perfect for scenarios requiring custom workflows, such as bulk user management, automated team assignments, or integration with external systems.
Since memberships are created directly, users gain immediate access without waiting for email acceptance.

{% multicode %}
```server-nodejs
const sdk = require('node-clikkle');

const client = new sdk.Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')
    .setKey('<API_KEY>');

const teams = new sdk.Teams(client);

// Create membership directly with userId
const membership = await teams.createMembership(
    '<TEAM_ID>',
    ['developer'],
    '<USER_ID>',
    'John Doe'  // optional name
);
```
```server-python
from clikkle.client import Client
from clikkle.services.teams import Teams

client = Client()
client.set_endpoint('https://cloud.clikkle.io/v1')
client.set_project('<PROJECT_ID>')
client.set_key('<API_KEY>')

teams = Teams(client)

# Create membership directly with userId
membership = teams.create_membership(
    team_id='<TEAM_ID>',
    roles=['developer'],
    user_id='<USER_ID>',
    name='John Doe'  # optional
)
```
```server-swift
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1") // Your API Endpoint
    .setProject("<PROJECT_ID>") // Your project ID
    .setKey("<API_KEY>") // Your secret API key

let teams = Teams(client)

// Create membership directly with userId
let membership = try await teams.createMembership(
    teamId: "<TEAM_ID>",
    roles: ["developer"],
    userId: "<USER_ID>",
    name: "John Doe" // optional
)
```
```server-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1") // Your API Endpoint
    .setProject("<PROJECT_ID>") // Your project ID
    .setKey("<API_KEY>") // Your secret API key

val teams = Teams(client)

// Create membership directly with userId
val response = teams.createMembership(
    teamId = "<TEAM_ID>",
    roles = listOf("developer"),
    userId = "<USER_ID>",
    name = "John Doe" // optional
)
```
{% /multicode %}

# Manage memberships

Once team memberships are created, you'll need to manage their lifecycle. This includes checking status, updating roles, and removing members when necessary.

## Check membership status

Before performing actions on team memberships, you often need to verify a user's current status within a team. The process differs between client-side and server-side implementations.

### Client-side
To check membership status client-side, first list the teams and then get the memberships for a specific team:

{% multicode %}
```client-web
import { Client, Teams } from "clikkle";

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

// Get list of teams the user is part of
const teamsList = await teams.list();

// For a specific team, get all memberships
const response = await teams.listMemberships({
    teamId: '<TEAM_ID>'
});

// Find membership for specific user
const userMembership = response.memberships.find(
    membership => membership.userId === '<USER_ID>'
);

if (userMembership) {
    console.log(userMembership.confirm); // false = invited, true = joined
    console.log(userMembership.roles); // ['developer', etc]
}
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

// Get list of teams the user is part of
final teamsList = await teams.list();

// For a specific team, get all memberships
final response = await teams.listMemberships(
    teamId: '<TEAM_ID>'
);

// Find membership for specific user
final userMembership = response.memberships.firstWhere(
    (membership) => membership.userId == '<USER_ID>',
    orElse: () => null
);

if (userMembership != null) {
    print(userMembership.confirm); // false = invited, true = joined
    print(userMembership.roles); // ['developer', etc]
}
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Get list of teams the user is part of
let teamsList = try await teams.list()

// For a specific team, get all memberships
let response = try await teams.listMemberships(
    teamId: "<TEAM_ID>"
)

// Find membership for specific user
if let userMembership = response.memberships.first(where: { $0.userId == "<USER_ID>" }) {
    print(userMembership.confirm) // false = invited, true = joined
    print(userMembership.roles) // ['developer', etc]
}
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Get list of teams the user is part of
val teamsList = teams.list()

// For a specific team, get all memberships
val response = teams.listMemberships(
    teamId = "team_id"
)

// Find membership for specific user
val userMembership = response.memberships.find {
    it.userId == "<USER_ID>"
}

userMembership?.let {
    println(it.confirm) // false = invited, true = joined
    println(it.roles) // ['developer', etc]
}
```
{% /multicode %}

### Server-side
Server-side requires iterating through teams and memberships since the data isn't filtered for a specific user:

{% multicode %}
```server-nodejs
const sdk = require('node-clikkle');

const client = new sdk.Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')
    .setKey('<API_KEY>');

const teams = new sdk.Teams(client);

// Get all teams
const teamsList = await teams.list();

// Iterate through teams to find memberships
for (const team of teamsList.teams) {
    const response = await teams.listMemberships({
        teamId: team.$id
    });

    // Find membership for specific user
    const userMembership = response.memberships.find(
        membership => membership.userId === '<USER_ID>'
    );

    if (userMembership) {
        console.log(`Team: ${team.name}`);
        console.log(`Joined: ${userMembership.joined}`); // null if invited, timestamp if joined
        console.log(`Roles: ${userMembership.roles}`);
    }
}
```
```server-python
from clikkle.client import Client
from clikkle.services.teams import Teams

client = Client()
client.set_endpoint('https://cloud.clikkle.io/v1')
client.set_project('<PROJECT_ID>')
client.set_key('<API_KEY>')

teams = Teams(client)

// Get all teams
teams_list = teams.list()

// Iterate through teams to find memberships
for team in teams_list['teams']:
    response = teams.list_memberships(team['$id'])

    // Find membership for specific user
    user_membership = next(
        (m for m in response['memberships'] if m['userId'] == '<USER_ID>'),
        None
    )

    if user_membership:
        print(f"Team: {team['name']}")
        print(f"Joined: {user_membership['joined']}") # null if invited, timestamp if joined
        print(f"Roles: {user_membership['roles']}")
```
```server-swift
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")
    .setKey("<API_KEY>")

let teams = Teams(client)

// Get all teams
let teamsList = try await teams.list()

// Iterate through teams to find memberships
for team in teamsList.teams {
    let response = try await teams.listMemberships(
        teamId: team.$id
    )

    // Find membership for specific user
    if let userMembership = response.memberships.first(where: { $0.userId == "<USER_ID>" }) {
        print("Team: \(team.name)")
        print("Joined: \(userMembership.joined)") # null if invited, timestamp if joined
        print("Roles: \(userMembership.roles)")
    }
}
```
```server-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")
    .setKey("<API_KEY>")

val teams = Teams(client)

// Get all teams
val teamsList = teams.list()

// Iterate through teams to find memberships
teamsList.teams.forEach { team ->
    val response = teams.listMemberships(teamId = team.$id)

    // Find membership for specific user
    val userMembership = response.memberships.find {
        it.userId == "<USER_ID>"
    }

    userMembership?.let {
        println("Team: ${team.name}")
        println("Joined: ${it.joined}") # null if invited, timestamp if joined
        println("Roles: ${it.roles}")
    }
}
```
{% /multicode %}

## Remove members

Team owners can remove members or users can leave teams:

{% multicode %}
```client-web
import { Client, Teams } from "clikkle";

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

await teams.deleteMembership(
    '<TEAM_ID>',
    '<MEMBERSHIP_ID>'
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

await teams.deleteMembership(
    teamId: '<TEAM_ID>',
    membershipId: '<MEMBERSHIP_ID>'
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

try await teams.deleteMembership(
    teamId: "<TEAM_ID>",
    membershipId: "<MEMBERSHIP_ID>"
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

teams.deleteMembership(
    teamId = "<TEAM_ID>",
    membershipId = "<MEMBERSHIP_ID>"
)
```
{% /multicode %}

# Manage team permissions

Teams in Clikkle use a role-based access control (RBAC) system. Each team member can be assigned one or more roles that define their permissions within the team.

## Update roles

You can assign roles when creating a membership or update them later. Note that only team members with the owner role can update other members' roles:

{% multicode %}
```client-web
import { Client, Teams } from "clikkle"

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')

const teams = new Teams(client)

// Update member roles
await teams.updateMembership(
    '<TEAM_ID>',
    '<MEMBERSHIP_ID>',
    ['admin', 'developer']
)
```
```client-flutter
import 'package:clikkle/clikkle.dart'

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')

final teams = Teams(client)

// Update member roles
await teams.updateMembership(
    teamId: '<TEAM_ID>',
    membershipId: '<MEMBERSHIP_ID>',
    roles: ['admin', 'developer']
)
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Update member roles
try await teams.updateMembership(
    teamId: "<TEAM_ID>",
    membershipId: "<MEMBERSHIP_ID>",
    roles: ["admin", "developer"]
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Update member roles
teams.updateMembership(
    teamId = "<TEAM_ID>",
    membershipId = "<MEMBERSHIP_ID>",
    roles = listOf("admin", "developer")
)
```
{% /multicode %}

## Check role access

You can verify if a user has specific roles:

{% multicode %}
```client-web
import { Client, Teams } from "clikkle"

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')

const teams = new Teams(client)

// Get team memberships
const response = await teams.listMemberships({
    teamId: '<TEAM_ID>'
});

// Check if user has specific role
const membership = response.memberships.find(m => m.userId === '<USER_ID>')
const isAdmin = membership?.roles.includes('admin') ?? false
```
```client-flutter
import 'package:clikkle/clikkle.dart'

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')

final teams = Teams(client)

// Get team memberships
final response = await teams.listMemberships(
    teamId: '<TEAM_ID>'
)

// Check if user has specific role
final membership = response.memberships.firstWhere(
    (m) => m.userId == '<USER_ID>',
    orElse: () => null
)
final isAdmin = membership?.roles.contains('admin') ?? false
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Get team memberships
let response = try await teams.listMemberships(
    teamId: "<TEAM_ID>"
)

// Check if user has specific role
let membership = response.memberships.first { $0.userId == "<USER_ID>" }
let isAdmin = membership?.roles.contains("admin") ?? false
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Get team memberships
val response = teams.listMemberships(
    teamId = "<TEAM_ID>"
)

// Check if user has specific role
val membership = response.memberships.find { it.userId == "<USER_ID>" }
val isAdmin = membership?.roles?.contains("admin") ?: false
```
{% /multicode %}

See how to grant row and file access to team roles in the [permissions](/docs/advanced/platform/permissions#example-2-team-roles) guide.

{% arrow_link href="/docs/products/auth/teams" %}
Learn more about team management
{% /arrow_link %}

0:{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"contents","children":["$","article",null,{"className":"web-article contents","children":[["$","header",null,{"className":"web-article-header docs-article-header-stack","children":[["$","div",null,{"className":"docs-article-mobile-header-row mb-2 flex w-full items-center lg:hidden","children":[null,["$","div",null,{"className":"copy-button-wrapper-mobile ml-auto","children":["$","$L2",null,{"markdown":"$3","className":"ml-0"}]}]]}],null,"$L4"]}],"$L5","$L6"]}]}],["$L7","$L8","$L9"],"$La"]}],"isPartial":false,"staleTime":300,"varyParams":null,"buildId":"siKNZ69LNgpmcXavwmUTs"}
b:T4b7a,---
layout: article
title: Team invites
description: Learn how to manage team invites in Clikkle. Implement both client-side email invites and server-side custom flows for team memberships.
---

Clikkle provides two approaches for adding members to teams: client-side email invites and server-side custom flows. Each approach serves different use cases and offers unique benefits.

# Invite client-side

Client-side email invites are perfect for implementing user-to-user invitations, allowing your users to invite others to join their teams, organizations, or shared resources. When creating a membership, Clikkle:
1. Creates a new user account if one doesn't exist for the email address
2. Sends an automated email invitation to the user
3. Creates a pending membership
4. Activates the membership when the user accepts

Client-side invites are ideal when you want a simple, automated process that lets your users manage their own team invitations.
Clikkle handles the email delivery with built-in templates and localization support, making it easy to implement a standard invite acceptance flow with email verification.

{% multicode %}
```client-web
import { Client, Teams } from "clikkle";

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

// Create membership with email invite
const membership = await teams.createMembership(
    '<TEAM_ID>',
    ['developer'],     // roles
    'user@example.com', // email
    undefined,         // userId (optional)
    undefined,         // phone (optional)
    'https://yourapp.com/accept-invite' // url - redirect after email click
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

// Create membership with email invite
final membership = await teams.createMembership(
    teamId: '<TEAM_ID>',
    roles: ['developer'],
    email: 'user@example.com',
    url: 'https://yourapp.com/accept-invite' // redirect after email click
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1") // Your API Endpoint
    .setProject("<PROJECT_ID>") // Your project ID

let teams = Teams(client)

// Create membership with email invite
let membership = try await teams.createMembership(
    teamId: "<TEAM_ID>",
    roles: ["developer"],
    email: "user@example.com",
    url: "https://yourapp.com/accept-invite" // redirect after email click
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1") // Your API Endpoint
    .setProject("<PROJECT_ID>") // Your project ID

val teams = Teams(client)

// Create membership with email invite
val response = teams.createMembership(
    teamId = "<TEAM_ID>",
    roles = listOf("developer"),
    email = "user@example.com",
    url = "https://yourapp.com/accept-invite" // redirect after email click
)
```
{% /multicode %}

## Accept invitations

For client-side email invites, users must accept the invitation to join the team. The acceptance flow:
1. User receives an email with an invitation link containing a secret token
2. Clicking the link redirects to your app
3. Your app calls the acceptance endpoint
4. Upon success, the user gains immediate access

{% multicode %}
```client-web
import { Client, Teams } from "clikkle";

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

// Accept the invitation using the membership ID and secret
const response = await teams.updateMembershipStatus(
    '<TEAM_ID>',
    '<MEMBERSHIP_ID>',
    '<USER_ID>',
    '<SECRET>'
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

// Accept the invitation using the membership ID and secret
final response = await teams.updateMembershipStatus(
    teamId: '<TEAM_ID>',
    membershipId: '<MEMBERSHIP_ID>',
    userId: '<USER_ID>',
    secret: '<SECRET>'
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Accept the invitation using the membership ID and secret
let response = try await teams.updateMembershipStatus(
    teamId: "<TEAM_ID>",
    membershipId: "<MEMBERSHIP_ID>",
    userId: "<USER_ID>",
    secret: "<SECRET>"
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Accept the invitation using the membership ID and secret
val response = teams.updateMembershipStatus(
    teamId = "<TEAM_ID>",
    membershipId = "<MEMBERSHIP_ID>",
    userId = "<USER_ID>",
    secret = "<SECRET>"
)
```
{% /multicode %}

# Server-side custom flows

Server-side membership creation bypasses the email invitation process, allowing direct member addition. This approach:
1. Creates an active membership immediately
2. Doesn't require user acceptance
3. Gives you complete control over the invitation workflow

This makes them perfect for scenarios requiring custom workflows, such as bulk user management, automated team assignments, or integration with external systems.
Since memberships are created directly, users gain immediate access without waiting for email acceptance.

{% multicode %}
```server-nodejs
const sdk = require('node-clikkle');

const client = new sdk.Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')
    .setKey('<API_KEY>');

const teams = new sdk.Teams(client);

// Create membership directly with userId
const membership = await teams.createMembership(
    '<TEAM_ID>',
    ['developer'],
    '<USER_ID>',
    'John Doe'  // optional name
);
```
```server-python
from clikkle.client import Client
from clikkle.services.teams import Teams

client = Client()
client.set_endpoint('https://cloud.clikkle.io/v1')
client.set_project('<PROJECT_ID>')
client.set_key('<API_KEY>')

teams = Teams(client)

# Create membership directly with userId
membership = teams.create_membership(
    team_id='<TEAM_ID>',
    roles=['developer'],
    user_id='<USER_ID>',
    name='John Doe'  # optional
)
```
```server-swift
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1") // Your API Endpoint
    .setProject("<PROJECT_ID>") // Your project ID
    .setKey("<API_KEY>") // Your secret API key

let teams = Teams(client)

// Create membership directly with userId
let membership = try await teams.createMembership(
    teamId: "<TEAM_ID>",
    roles: ["developer"],
    userId: "<USER_ID>",
    name: "John Doe" // optional
)
```
```server-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1") // Your API Endpoint
    .setProject("<PROJECT_ID>") // Your project ID
    .setKey("<API_KEY>") // Your secret API key

val teams = Teams(client)

// Create membership directly with userId
val response = teams.createMembership(
    teamId = "<TEAM_ID>",
    roles = listOf("developer"),
    userId = "<USER_ID>",
    name = "John Doe" // optional
)
```
{% /multicode %}

# Manage memberships

Once team memberships are created, you'll need to manage their lifecycle. This includes checking status, updating roles, and removing members when necessary.

## Check membership status

Before performing actions on team memberships, you often need to verify a user's current status within a team. The process differs between client-side and server-side implementations.

### Client-side
To check membership status client-side, first list the teams and then get the memberships for a specific team:

{% multicode %}
```client-web
import { Client, Teams } from "clikkle";

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

// Get list of teams the user is part of
const teamsList = await teams.list();

// For a specific team, get all memberships
const response = await teams.listMemberships({
    teamId: '<TEAM_ID>'
});

// Find membership for specific user
const userMembership = response.memberships.find(
    membership => membership.userId === '<USER_ID>'
);

if (userMembership) {
    console.log(userMembership.confirm); // false = invited, true = joined
    console.log(userMembership.roles); // ['developer', etc]
}
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

// Get list of teams the user is part of
final teamsList = await teams.list();

// For a specific team, get all memberships
final response = await teams.listMemberships(
    teamId: '<TEAM_ID>'
);

// Find membership for specific user
final userMembership = response.memberships.firstWhere(
    (membership) => membership.userId == '<USER_ID>',
    orElse: () => null
);

if (userMembership != null) {
    print(userMembership.confirm); // false = invited, true = joined
    print(userMembership.roles); // ['developer', etc]
}
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Get list of teams the user is part of
let teamsList = try await teams.list()

// For a specific team, get all memberships
let response = try await teams.listMemberships(
    teamId: "<TEAM_ID>"
)

// Find membership for specific user
if let userMembership = response.memberships.first(where: { $0.userId == "<USER_ID>" }) {
    print(userMembership.confirm) // false = invited, true = joined
    print(userMembership.roles) // ['developer', etc]
}
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Get list of teams the user is part of
val teamsList = teams.list()

// For a specific team, get all memberships
val response = teams.listMemberships(
    teamId = "team_id"
)

// Find membership for specific user
val userMembership = response.memberships.find {
    it.userId == "<USER_ID>"
}

userMembership?.let {
    println(it.confirm) // false = invited, true = joined
    println(it.roles) // ['developer', etc]
}
```
{% /multicode %}

### Server-side
Server-side requires iterating through teams and memberships since the data isn't filtered for a specific user:

{% multicode %}
```server-nodejs
const sdk = require('node-clikkle');

const client = new sdk.Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')
    .setKey('<API_KEY>');

const teams = new sdk.Teams(client);

// Get all teams
const teamsList = await teams.list();

// Iterate through teams to find memberships
for (const team of teamsList.teams) {
    const response = await teams.listMemberships({
        teamId: team.$id
    });

    // Find membership for specific user
    const userMembership = response.memberships.find(
        membership => membership.userId === '<USER_ID>'
    );

    if (userMembership) {
        console.log(`Team: ${team.name}`);
        console.log(`Joined: ${userMembership.joined}`); // null if invited, timestamp if joined
        console.log(`Roles: ${userMembership.roles}`);
    }
}
```
```server-python
from clikkle.client import Client
from clikkle.services.teams import Teams

client = Client()
client.set_endpoint('https://cloud.clikkle.io/v1')
client.set_project('<PROJECT_ID>')
client.set_key('<API_KEY>')

teams = Teams(client)

// Get all teams
teams_list = teams.list()

// Iterate through teams to find memberships
for team in teams_list['teams']:
    response = teams.list_memberships(team['$id'])

    // Find membership for specific user
    user_membership = next(
        (m for m in response['memberships'] if m['userId'] == '<USER_ID>'),
        None
    )

    if user_membership:
        print(f"Team: {team['name']}")
        print(f"Joined: {user_membership['joined']}") # null if invited, timestamp if joined
        print(f"Roles: {user_membership['roles']}")
```
```server-swift
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")
    .setKey("<API_KEY>")

let teams = Teams(client)

// Get all teams
let teamsList = try await teams.list()

// Iterate through teams to find memberships
for team in teamsList.teams {
    let response = try await teams.listMemberships(
        teamId: team.$id
    )

    // Find membership for specific user
    if let userMembership = response.memberships.first(where: { $0.userId == "<USER_ID>" }) {
        print("Team: \(team.name)")
        print("Joined: \(userMembership.joined)") # null if invited, timestamp if joined
        print("Roles: \(userMembership.roles)")
    }
}
```
```server-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")
    .setKey("<API_KEY>")

val teams = Teams(client)

// Get all teams
val teamsList = teams.list()

// Iterate through teams to find memberships
teamsList.teams.forEach { team ->
    val response = teams.listMemberships(teamId = team.$id)

    // Find membership for specific user
    val userMembership = response.memberships.find {
        it.userId == "<USER_ID>"
    }

    userMembership?.let {
        println("Team: ${team.name}")
        println("Joined: ${it.joined}") # null if invited, timestamp if joined
        println("Roles: ${it.roles}")
    }
}
```
{% /multicode %}

## Remove members

Team owners can remove members or users can leave teams:

{% multicode %}
```client-web
import { Client, Teams } from "clikkle";

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

const teams = new Teams(client);

await teams.deleteMembership(
    '<TEAM_ID>',
    '<MEMBERSHIP_ID>'
);
```
```client-flutter
import 'package:clikkle/clikkle.dart';

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>');

final teams = Teams(client);

await teams.deleteMembership(
    teamId: '<TEAM_ID>',
    membershipId: '<MEMBERSHIP_ID>'
);
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

try await teams.deleteMembership(
    teamId: "<TEAM_ID>",
    membershipId: "<MEMBERSHIP_ID>"
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

teams.deleteMembership(
    teamId = "<TEAM_ID>",
    membershipId = "<MEMBERSHIP_ID>"
)
```
{% /multicode %}

# Manage team permissions

Teams in Clikkle use a role-based access control (RBAC) system. Each team member can be assigned one or more roles that define their permissions within the team.

## Update roles

You can assign roles when creating a membership or update them later. Note that only team members with the owner role can update other members' roles:

{% multicode %}
```client-web
import { Client, Teams } from "clikkle"

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')

const teams = new Teams(client)

// Update member roles
await teams.updateMembership(
    '<TEAM_ID>',
    '<MEMBERSHIP_ID>',
    ['admin', 'developer']
)
```
```client-flutter
import 'package:clikkle/clikkle.dart'

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')

final teams = Teams(client)

// Update member roles
await teams.updateMembership(
    teamId: '<TEAM_ID>',
    membershipId: '<MEMBERSHIP_ID>',
    roles: ['admin', 'developer']
)
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Update member roles
try await teams.updateMembership(
    teamId: "<TEAM_ID>",
    membershipId: "<MEMBERSHIP_ID>",
    roles: ["admin", "developer"]
)
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Update member roles
teams.updateMembership(
    teamId = "<TEAM_ID>",
    membershipId = "<MEMBERSHIP_ID>",
    roles = listOf("admin", "developer")
)
```
{% /multicode %}

## Check role access

You can verify if a user has specific roles:

{% multicode %}
```client-web
import { Client, Teams } from "clikkle"

const client = new Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')

const teams = new Teams(client)

// Get team memberships
const response = await teams.listMemberships({
    teamId: '<TEAM_ID>'
});

// Check if user has specific role
const membership = response.memberships.find(m => m.userId === '<USER_ID>')
const isAdmin = membership?.roles.includes('admin') ?? false
```
```client-flutter
import 'package:clikkle/clikkle.dart'

final client = Client()
    .setEndpoint('https://cloud.clikkle.io/v1')
    .setProject('<PROJECT_ID>')

final teams = Teams(client)

// Get team memberships
final response = await teams.listMemberships(
    teamId: '<TEAM_ID>'
)

// Check if user has specific role
final membership = response.memberships.firstWhere(
    (m) => m.userId == '<USER_ID>',
    orElse: () => null
)
final isAdmin = membership?.roles.contains('admin') ?? false
```
```client-apple
import Clikkle

let client = Client()
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

let teams = Teams(client)

// Get team memberships
let response = try await teams.listMemberships(
    teamId: "<TEAM_ID>"
)

// Check if user has specific role
let membership = response.memberships.first { $0.userId == "<USER_ID>" }
let isAdmin = membership?.roles.contains("admin") ?? false
```
```client-android-kotlin
import io.clikkle.Client
import io.clikkle.services.Teams

val client = Client(context)
    .setEndpoint("https://cloud.clikkle.io/v1")
    .setProject("<PROJECT_ID>")

val teams = Teams(client)

// Get team memberships
val response = teams.listMemberships(
    teamId = "<TEAM_ID>"
)

// Check if user has specific role
val membership = response.memberships.find { it.userId == "<USER_ID>" }
val isAdmin = membership?.roles?.contains("admin") ?: false
```
{% /multicode %}

See how to grant row and file access to team roles in the [permissions](/docs/advanced/platform/permissions#example-2-team-roles) guide.

{% arrow_link href="/docs/products/auth/teams" %}
Learn more about team management
{% /arrow_link %}

4:["$","div",null,{"className":"docs-article-title-actions-row","children":[["$","div",null,{"className":"relative flex min-w-0 flex-1 items-center","children":[null,["$","h1",null,{"className":"text-title m-0 font-aeonik-pro tracking-tight text-primary","children":"Team invites"}]]}],["$","div",null,{"className":"web-article-header-end copy-button-wrapper hidden shrink-0 lg:block","children":["$","$L2",null,{"markdown":"$b","className":"ml-0"}]}]]}]
5:["$","$Lc",null,{"className":"prose max-w-none","children":[null,["$","div",null,{"className":"docs-article-after-header pt-8","children":[null,null,["$","$Ld",null,{"isArticleLayout":true,"contentOnly":true,"children":["$","article",null,{"children":[["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Clikkle provides two approaches for adding members to teams: client-side email invites and server-side custom flows. Each approach serves different use cases and offers unique benefits."}],["$","$Le",null,{"level":1,"children":"Invite client-side"}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Client-side email invites are perfect for implementing user-to-user invitations, allowing your users to invite others to join their teams, organizations, or shared resources. When creating a membership, Clikkle:"}],["$","ol",null,{"className":"web-numeric-list","children":[["$","li",null,{"children":"Creates a new user account if one doesn't exist for the email address"}],["$","li",null,{"children":"Sends an automated email invitation to the user"}],["$","li",null,{"children":"Creates a pending membership"}],["$","li",null,{"children":"Activates the membership when the user accepts"}]]}],["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["Client-side invites are ideal when you want a simple, automated process that lets your users manage their own team invitations."," ","Clikkle handles the email delivery with built-in templates and localization support, making it easy to implement a standard invite acceptance flow with email verification."]}],["$","$Lf",null,{"children":[["$","$L10",null,{"language":"client-web","content":"import { Client, Teams } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\n// Create membership with email invite\nconst membership = await teams.createMembership(\n    '<TEAM_ID>',\n    ['developer'],     // roles\n    'user@example.com', // email\n    undefined,         // userId (optional)\n    undefined,         // phone (optional)\n    'https://yourapp.com/accept-invite' // url - redirect after email click\n);\n","children":"import { Client, Teams } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\n// Create membership with email invite\nconst membership = await teams.createMembership(\n    '<TEAM_ID>',\n    ['developer'],     // roles\n    'user@example.com', // email\n    undefined,         // userId (optional)\n    undefined,         // phone (optional)\n    'https://yourapp.com/accept-invite' // url - redirect after email click\n);\n"}],["$","$L10",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\n// Create membership with email invite\nfinal membership = await teams.createMembership(\n    teamId: '<TEAM_ID>',\n    roles: ['developer'],\n    email: 'user@example.com',\n    url: 'https://yourapp.com/accept-invite' // redirect after email click\n);\n","children":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\n// Create membership with email invite\nfinal membership = await teams.createMembership(\n    teamId: '<TEAM_ID>',\n    roles: ['developer'],\n    email: 'user@example.com',\n    url: 'https://yourapp.com/accept-invite' // redirect after email click\n);\n"}],"$L11","$L12"]}],"$L13","$L14","$L15","$L16","$L17","$L18","$L19","$L1a","$L1b","$L1c","$L1d","$L1e","$L1f","$L20","$L21","$L22","$L23","$L24","$L25","$L26","$L27","$L28","$L29","$L2a","$L2b","$L2c","$L2d","$L2e","$L2f","$L30","$L31","$L32"]}]}]]}],"$L33"]}]
6:["$","aside",null,{"className":"web-references-menu ps-6","children":["$","$L34",null,{"items":[{"id":"invite-client-side","title":"Invite client-side","level":1},{"id":"accept-invitations","title":"Accept invitations","level":2},{"id":"server-side-custom-flows","title":"Server-side custom flows","level":1},{"id":"manage-memberships","title":"Manage memberships","level":1},{"id":"check-membership-status","title":"Check membership status","level":2},{"id":"client-side","title":"Client-side","level":3},{"id":"server-side","title":"Server-side","level":3},{"id":"remove-members","title":"Remove members","level":2},{"id":"manage-team-permissions","title":"Manage team permissions","level":1},{"id":"update-roles","title":"Update roles","level":2},{"id":"check-role-access","title":"Check role access","level":2}]}]}]
7:["$","script","script-0",{"src":"/clikkle/_next/static/chunks/0nm9skf~8e5e2.js","async":true}]
8:["$","script","script-1",{"src":"/clikkle/_next/static/chunks/10iz33s~n0gnf.js","async":true}]
9:["$","script","script-2",{"src":"/clikkle/_next/static/chunks/17akn4e9sundm.js","async":true}]
a:["$","$L35",null,{"children":["$","$36",null,{"name":"Next.MetadataOutlet","children":"$@37"}]}]
11:["$","$L10",null,{"language":"client-apple","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\") // Your API Endpoint\n    .setProject(\"<PROJECT_ID>\") // Your project ID\n\nlet teams = Teams(client)\n\n// Create membership with email invite\nlet membership = try await teams.createMembership(\n    teamId: \"<TEAM_ID>\",\n    roles: [\"developer\"],\n    email: \"user@example.com\",\n    url: \"https://yourapp.com/accept-invite\" // redirect after email click\n)\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\") // Your API Endpoint\n    .setProject(\"<PROJECT_ID>\") // Your project ID\n\nlet teams = Teams(client)\n\n// Create membership with email invite\nlet membership = try await teams.createMembership(\n    teamId: \"<TEAM_ID>\",\n    roles: [\"developer\"],\n    email: \"user@example.com\",\n    url: \"https://yourapp.com/accept-invite\" // redirect after email click\n)\n"}]
12:["$","$L10",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\") // Your API Endpoint\n    .setProject(\"<PROJECT_ID>\") // Your project ID\n\nval teams = Teams(client)\n\n// Create membership with email invite\nval response = teams.createMembership(\n    teamId = \"<TEAM_ID>\",\n    roles = listOf(\"developer\"),\n    email = \"user@example.com\",\n    url = \"https://yourapp.com/accept-invite\" // redirect after email click\n)\n","children":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\") // Your API Endpoint\n    .setProject(\"<PROJECT_ID>\") // Your project ID\n\nval teams = Teams(client)\n\n// Create membership with email invite\nval response = teams.createMembership(\n    teamId = \"<TEAM_ID>\",\n    roles = listOf(\"developer\"),\n    email = \"user@example.com\",\n    url = \"https://yourapp.com/accept-invite\" // redirect after email click\n)\n"}]
13:["$","$Le",null,{"level":2,"children":"Accept invitations"}]
14:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"For client-side email invites, users must accept the invitation to join the team. The acceptance flow:"}]
15:["$","ol",null,{"className":"web-numeric-list","children":[["$","li",null,{"children":"User receives an email with an invitation link containing a secret token"}],["$","li",null,{"children":"Clicking the link redirects to your app"}],["$","li",null,{"children":"Your app calls the acceptance endpoint"}],["$","li",null,{"children":"Upon success, the user gains immediate access"}]]}]
16:["$","$Lf",null,{"children":[["$","$L10",null,{"language":"client-web","content":"import { Client, Teams } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\n// Accept the invitation using the membership ID and secret\nconst response = await teams.updateMembershipStatus(\n    '<TEAM_ID>',\n    '<MEMBERSHIP_ID>',\n    '<USER_ID>',\n    '<SECRET>'\n);\n","children":"import { Client, Teams } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\n// Accept the invitation using the membership ID and secret\nconst response = await teams.updateMembershipStatus(\n    '<TEAM_ID>',\n    '<MEMBERSHIP_ID>',\n    '<USER_ID>',\n    '<SECRET>'\n);\n"}],["$","$L10",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\n// Accept the invitation using the membership ID and secret\nfinal response = await teams.updateMembershipStatus(\n    teamId: '<TEAM_ID>',\n    membershipId: '<MEMBERSHIP_ID>',\n    userId: '<USER_ID>',\n    secret: '<SECRET>'\n);\n","children":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\n// Accept the invitation using the membership ID and secret\nfinal response = await teams.updateMembershipStatus(\n    teamId: '<TEAM_ID>',\n    membershipId: '<MEMBERSHIP_ID>',\n    userId: '<USER_ID>',\n    secret: '<SECRET>'\n);\n"}],["$","$L10",null,{"language":"client-apple","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Accept the invitation using the membership ID and secret\nlet response = try await teams.updateMembershipStatus(\n    teamId: \"<TEAM_ID>\",\n    membershipId: \"<MEMBERSHIP_ID>\",\n    userId: \"<USER_ID>\",\n    secret: \"<SECRET>\"\n)\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Accept the invitation using the membership ID and secret\nlet response = try await teams.updateMembershipStatus(\n    teamId: \"<TEAM_ID>\",\n    membershipId: \"<MEMBERSHIP_ID>\",\n    userId: \"<USER_ID>\",\n    secret: \"<SECRET>\"\n)\n"}],["$","$L10",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Accept the invitation using the membership ID and secret\nval response = teams.updateMembershipStatus(\n    teamId = \"<TEAM_ID>\",\n    membershipId = \"<MEMBERSHIP_ID>\",\n    userId = \"<USER_ID>\",\n    secret = \"<SECRET>\"\n)\n","children":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Accept the invitation using the membership ID and secret\nval response = teams.updateMembershipStatus(\n    teamId = \"<TEAM_ID>\",\n    membershipId = \"<MEMBERSHIP_ID>\",\n    userId = \"<USER_ID>\",\n    secret = \"<SECRET>\"\n)\n"}]]}]
17:["$","$Le",null,{"level":1,"children":"Server-side custom flows"}]
18:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Server-side membership creation bypasses the email invitation process, allowing direct member addition. This approach:"}]
19:["$","ol",null,{"className":"web-numeric-list","children":[["$","li",null,{"children":"Creates an active membership immediately"}],["$","li",null,{"children":"Doesn't require user acceptance"}],["$","li",null,{"children":"Gives you complete control over the invitation workflow"}]]}]
1a:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["This makes them perfect for scenarios requiring custom workflows, such as bulk user management, automated team assignments, or integration with external systems."," ","Since memberships are created directly, users gain immediate access without waiting for email acceptance."]}]
1b:["$","$Lf",null,{"children":[["$","$L10",null,{"language":"server-nodejs","content":"const sdk = require('node-clikkle');\n\nconst client = new sdk.Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n    .setKey('<API_KEY>');\n\nconst teams = new sdk.Teams(client);\n\n// Create membership directly with userId\nconst membership = await teams.createMembership(\n    '<TEAM_ID>',\n    ['developer'],\n    '<USER_ID>',\n    'John Doe'  // optional name\n);\n","children":"const sdk = require('node-clikkle');\n\nconst client = new sdk.Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n    .setKey('<API_KEY>');\n\nconst teams = new sdk.Teams(client);\n\n// Create membership directly with userId\nconst membership = await teams.createMembership(\n    '<TEAM_ID>',\n    ['developer'],\n    '<USER_ID>',\n    'John Doe'  // optional name\n);\n"}],["$","$L10",null,{"language":"server-python","content":"from clikkle.client import Client\nfrom clikkle.services.teams import Teams\n\nclient = Client()\nclient.set_endpoint('https://cloud.clikkle.io/v1')\nclient.set_project('<PROJECT_ID>')\nclient.set_key('<API_KEY>')\n\nteams = Teams(client)\n\n# Create membership directly with userId\nmembership = teams.create_membership(\n    team_id='<TEAM_ID>',\n    roles=['developer'],\n    user_id='<USER_ID>',\n    name='John Doe'  # optional\n)\n","children":"from clikkle.client import Client\nfrom clikkle.services.teams import Teams\n\nclient = Client()\nclient.set_endpoint('https://cloud.clikkle.io/v1')\nclient.set_project('<PROJECT_ID>')\nclient.set_key('<API_KEY>')\n\nteams = Teams(client)\n\n# Create membership directly with userId\nmembership = teams.create_membership(\n    team_id='<TEAM_ID>',\n    roles=['developer'],\n    user_id='<USER_ID>',\n    name='John Doe'  # optional\n)\n"}],["$","$L10",null,{"language":"server-swift","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\") // Your API Endpoint\n    .setProject(\"<PROJECT_ID>\") // Your project ID\n    .setKey(\"<API_KEY>\") // Your secret API key\n\nlet teams = Teams(client)\n\n// Create membership directly with userId\nlet membership = try await teams.createMembership(\n    teamId: \"<TEAM_ID>\",\n    roles: [\"developer\"],\n    userId: \"<USER_ID>\",\n    name: \"John Doe\" // optional\n)\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\") // Your API Endpoint\n    .setProject(\"<PROJECT_ID>\") // Your project ID\n    .setKey(\"<API_KEY>\") // Your secret API key\n\nlet teams = Teams(client)\n\n// Create membership directly with userId\nlet membership = try await teams.createMembership(\n    teamId: \"<TEAM_ID>\",\n    roles: [\"developer\"],\n    userId: \"<USER_ID>\",\n    name: \"John Doe\" // optional\n)\n"}],["$","$L10",null,{"language":"server-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\") // Your API Endpoint\n    .setProject(\"<PROJECT_ID>\") // Your project ID\n    .setKey(\"<API_KEY>\") // Your secret API key\n\nval teams = Teams(client)\n\n// Create membership directly with userId\nval response = teams.createMembership(\n    teamId = \"<TEAM_ID>\",\n    roles = listOf(\"developer\"),\n    userId = \"<USER_ID>\",\n    name = \"John Doe\" // optional\n)\n","children":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\") // Your API Endpoint\n    .setProject(\"<PROJECT_ID>\") // Your project ID\n    .setKey(\"<API_KEY>\") // Your secret API key\n\nval teams = Teams(client)\n\n// Create membership directly with userId\nval response = teams.createMembership(\n    teamId = \"<TEAM_ID>\",\n    roles = listOf(\"developer\"),\n    userId = \"<USER_ID>\",\n    name = \"John Doe\" // optional\n)\n"}]]}]
1c:["$","$Le",null,{"level":1,"children":"Manage memberships"}]
1d:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Once team memberships are created, you'll need to manage their lifecycle. This includes checking status, updating roles, and removing members when necessary."}]
1e:["$","$Le",null,{"level":2,"children":"Check membership status"}]
1f:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Before performing actions on team memberships, you often need to verify a user's current status within a team. The process differs between client-side and server-side implementations."}]
20:["$","$Le",null,{"level":3,"children":"Client-side"}]
21:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"To check membership status client-side, first list the teams and then get the memberships for a specific team:"}]
22:["$","$Lf",null,{"children":[["$","$L10",null,{"language":"client-web","content":"import { Client, Teams } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\n// Get list of teams the user is part of\nconst teamsList = await teams.list();\n\n// For a specific team, get all memberships\nconst response = await teams.listMemberships({\n    teamId: '<TEAM_ID>'\n});\n\n// Find membership for specific user\nconst userMembership = response.memberships.find(\n    membership => membership.userId === '<USER_ID>'\n);\n\nif (userMembership) {\n    console.log(userMembership.confirm); // false = invited, true = joined\n    console.log(userMembership.roles); // ['developer', etc]\n}\n","children":"import { Client, Teams } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\n// Get list of teams the user is part of\nconst teamsList = await teams.list();\n\n// For a specific team, get all memberships\nconst response = await teams.listMemberships({\n    teamId: '<TEAM_ID>'\n});\n\n// Find membership for specific user\nconst userMembership = response.memberships.find(\n    membership => membership.userId === '<USER_ID>'\n);\n\nif (userMembership) {\n    console.log(userMembership.confirm); // false = invited, true = joined\n    console.log(userMembership.roles); // ['developer', etc]\n}\n"}],["$","$L10",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\n// Get list of teams the user is part of\nfinal teamsList = await teams.list();\n\n// For a specific team, get all memberships\nfinal response = await teams.listMemberships(\n    teamId: '<TEAM_ID>'\n);\n\n// Find membership for specific user\nfinal userMembership = response.memberships.firstWhere(\n    (membership) => membership.userId == '<USER_ID>',\n    orElse: () => null\n);\n\nif (userMembership != null) {\n    print(userMembership.confirm); // false = invited, true = joined\n    print(userMembership.roles); // ['developer', etc]\n}\n","children":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\n// Get list of teams the user is part of\nfinal teamsList = await teams.list();\n\n// For a specific team, get all memberships\nfinal response = await teams.listMemberships(\n    teamId: '<TEAM_ID>'\n);\n\n// Find membership for specific user\nfinal userMembership = response.memberships.firstWhere(\n    (membership) => membership.userId == '<USER_ID>',\n    orElse: () => null\n);\n\nif (userMembership != null) {\n    print(userMembership.confirm); // false = invited, true = joined\n    print(userMembership.roles); // ['developer', etc]\n}\n"}],["$","$L10",null,{"language":"client-apple","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Get list of teams the user is part of\nlet teamsList = try await teams.list()\n\n// For a specific team, get all memberships\nlet response = try await teams.listMemberships(\n    teamId: \"<TEAM_ID>\"\n)\n\n// Find membership for specific user\nif let userMembership = response.memberships.first(where: { $0.userId == \"<USER_ID>\" }) {\n    print(userMembership.confirm) // false = invited, true = joined\n    print(userMembership.roles) // ['developer', etc]\n}\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Get list of teams the user is part of\nlet teamsList = try await teams.list()\n\n// For a specific team, get all memberships\nlet response = try await teams.listMemberships(\n    teamId: \"<TEAM_ID>\"\n)\n\n// Find membership for specific user\nif let userMembership = response.memberships.first(where: { $0.userId == \"<USER_ID>\" }) {\n    print(userMembership.confirm) // false = invited, true = joined\n    print(userMembership.roles) // ['developer', etc]\n}\n"}],"$L38"]}]
23:["$","$Le",null,{"level":3,"children":"Server-side"}]
24:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Server-side requires iterating through teams and memberships since the data isn't filtered for a specific user:"}]
25:["$","$Lf",null,{"children":[["$","$L10",null,{"language":"server-nodejs","content":"const sdk = require('node-clikkle');\n\nconst client = new sdk.Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n    .setKey('<API_KEY>');\n\nconst teams = new sdk.Teams(client);\n\n// Get all teams\nconst teamsList = await teams.list();\n\n// Iterate through teams to find memberships\nfor (const team of teamsList.teams) {\n    const response = await teams.listMemberships({\n        teamId: team.$id\n    });\n\n    // Find membership for specific user\n    const userMembership = response.memberships.find(\n        membership => membership.userId === '<USER_ID>'\n    );\n\n    if (userMembership) {\n        console.log(`Team: ${team.name}`);\n        console.log(`Joined: ${userMembership.joined}`); // null if invited, timestamp if joined\n        console.log(`Roles: ${userMembership.roles}`);\n    }\n}\n","children":"const sdk = require('node-clikkle');\n\nconst client = new sdk.Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n    .setKey('<API_KEY>');\n\nconst teams = new sdk.Teams(client);\n\n// Get all teams\nconst teamsList = await teams.list();\n\n// Iterate through teams to find memberships\nfor (const team of teamsList.teams) {\n    const response = await teams.listMemberships({\n        teamId: team.$id\n    });\n\n    // Find membership for specific user\n    const userMembership = response.memberships.find(\n        membership => membership.userId === '<USER_ID>'\n    );\n\n    if (userMembership) {\n        console.log(`Team: ${team.name}`);\n        console.log(`Joined: ${userMembership.joined}`); // null if invited, timestamp if joined\n        console.log(`Roles: ${userMembership.roles}`);\n    }\n}\n"}],["$","$L10",null,{"language":"server-python","content":"from clikkle.client import Client\nfrom clikkle.services.teams import Teams\n\nclient = Client()\nclient.set_endpoint('https://cloud.clikkle.io/v1')\nclient.set_project('<PROJECT_ID>')\nclient.set_key('<API_KEY>')\n\nteams = Teams(client)\n\n// Get all teams\nteams_list = teams.list()\n\n// Iterate through teams to find memberships\nfor team in teams_list['teams']:\n    response = teams.list_memberships(team['$id'])\n\n    // Find membership for specific user\n    user_membership = next(\n        (m for m in response['memberships'] if m['userId'] == '<USER_ID>'),\n        None\n    )\n\n    if user_membership:\n        print(f\"Team: {team['name']}\")\n        print(f\"Joined: {user_membership['joined']}\") # null if invited, timestamp if joined\n        print(f\"Roles: {user_membership['roles']}\")\n","children":"from clikkle.client import Client\nfrom clikkle.services.teams import Teams\n\nclient = Client()\nclient.set_endpoint('https://cloud.clikkle.io/v1')\nclient.set_project('<PROJECT_ID>')\nclient.set_key('<API_KEY>')\n\nteams = Teams(client)\n\n// Get all teams\nteams_list = teams.list()\n\n// Iterate through teams to find memberships\nfor team in teams_list['teams']:\n    response = teams.list_memberships(team['$id'])\n\n    // Find membership for specific user\n    user_membership = next(\n        (m for m in response['memberships'] if m['userId'] == '<USER_ID>'),\n        None\n    )\n\n    if user_membership:\n        print(f\"Team: {team['name']}\")\n        print(f\"Joined: {user_membership['joined']}\") # null if invited, timestamp if joined\n        print(f\"Roles: {user_membership['roles']}\")\n"}],"$L39","$L3a"]}]
26:["$","$Le",null,{"level":2,"children":"Remove members"}]
27:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Team owners can remove members or users can leave teams:"}]
28:["$","$Lf",null,{"children":[["$","$L10",null,{"language":"client-web","content":"import { Client, Teams } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\nawait teams.deleteMembership(\n    '<TEAM_ID>',\n    '<MEMBERSHIP_ID>'\n);\n","children":"import { Client, Teams } from \"clikkle\";\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nconst teams = new Teams(client);\n\nawait teams.deleteMembership(\n    '<TEAM_ID>',\n    '<MEMBERSHIP_ID>'\n);\n"}],["$","$L10",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\nawait teams.deleteMembership(\n    teamId: '<TEAM_ID>',\n    membershipId: '<MEMBERSHIP_ID>'\n);\n","children":"import 'package:clikkle/clikkle.dart';\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>');\n\nfinal teams = Teams(client);\n\nawait teams.deleteMembership(\n    teamId: '<TEAM_ID>',\n    membershipId: '<MEMBERSHIP_ID>'\n);\n"}],["$","$L10",null,{"language":"client-apple","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\ntry await teams.deleteMembership(\n    teamId: \"<TEAM_ID>\",\n    membershipId: \"<MEMBERSHIP_ID>\"\n)\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\ntry await teams.deleteMembership(\n    teamId: \"<TEAM_ID>\",\n    membershipId: \"<MEMBERSHIP_ID>\"\n)\n"}],["$","$L10",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\nteams.deleteMembership(\n    teamId = \"<TEAM_ID>\",\n    membershipId = \"<MEMBERSHIP_ID>\"\n)\n","children":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\nteams.deleteMembership(\n    teamId = \"<TEAM_ID>\",\n    membershipId = \"<MEMBERSHIP_ID>\"\n)\n"}]]}]
29:["$","$Le",null,{"level":1,"children":"Manage team permissions"}]
2a:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Teams in Clikkle use a role-based access control (RBAC) system. Each team member can be assigned one or more roles that define their permissions within the team."}]
2b:["$","$Le",null,{"level":2,"children":"Update roles"}]
2c:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"You can assign roles when creating a membership or update them later. Note that only team members with the owner role can update other members' roles:"}]
2d:["$","$Lf",null,{"children":[["$","$L10",null,{"language":"client-web","content":"import { Client, Teams } from \"clikkle\"\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n\nconst teams = new Teams(client)\n\n// Update member roles\nawait teams.updateMembership(\n    '<TEAM_ID>',\n    '<MEMBERSHIP_ID>',\n    ['admin', 'developer']\n)\n","children":"import { Client, Teams } from \"clikkle\"\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n\nconst teams = new Teams(client)\n\n// Update member roles\nawait teams.updateMembership(\n    '<TEAM_ID>',\n    '<MEMBERSHIP_ID>',\n    ['admin', 'developer']\n)\n"}],["$","$L10",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart'\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n\nfinal teams = Teams(client)\n\n// Update member roles\nawait teams.updateMembership(\n    teamId: '<TEAM_ID>',\n    membershipId: '<MEMBERSHIP_ID>',\n    roles: ['admin', 'developer']\n)\n","children":"import 'package:clikkle/clikkle.dart'\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n\nfinal teams = Teams(client)\n\n// Update member roles\nawait teams.updateMembership(\n    teamId: '<TEAM_ID>',\n    membershipId: '<MEMBERSHIP_ID>',\n    roles: ['admin', 'developer']\n)\n"}],["$","$L10",null,{"language":"client-apple","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Update member roles\ntry await teams.updateMembership(\n    teamId: \"<TEAM_ID>\",\n    membershipId: \"<MEMBERSHIP_ID>\",\n    roles: [\"admin\", \"developer\"]\n)\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Update member roles\ntry await teams.updateMembership(\n    teamId: \"<TEAM_ID>\",\n    membershipId: \"<MEMBERSHIP_ID>\",\n    roles: [\"admin\", \"developer\"]\n)\n"}],["$","$L10",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Update member roles\nteams.updateMembership(\n    teamId = \"<TEAM_ID>\",\n    membershipId = \"<MEMBERSHIP_ID>\",\n    roles = listOf(\"admin\", \"developer\")\n)\n","children":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Update member roles\nteams.updateMembership(\n    teamId = \"<TEAM_ID>\",\n    membershipId = \"<MEMBERSHIP_ID>\",\n    roles = listOf(\"admin\", \"developer\")\n)\n"}]]}]
2e:["$","$Le",null,{"level":2,"children":"Check role access"}]
2f:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"You can verify if a user has specific roles:"}]
30:["$","$Lf",null,{"children":[["$","$L10",null,{"language":"client-web","content":"import { Client, Teams } from \"clikkle\"\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n\nconst teams = new Teams(client)\n\n// Get team memberships\nconst response = await teams.listMemberships({\n    teamId: '<TEAM_ID>'\n});\n\n// Check if user has specific role\nconst membership = response.memberships.find(m => m.userId === '<USER_ID>')\nconst isAdmin = membership?.roles.includes('admin') ?? false\n","children":"import { Client, Teams } from \"clikkle\"\n\nconst client = new Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n\nconst teams = new Teams(client)\n\n// Get team memberships\nconst response = await teams.listMemberships({\n    teamId: '<TEAM_ID>'\n});\n\n// Check if user has specific role\nconst membership = response.memberships.find(m => m.userId === '<USER_ID>')\nconst isAdmin = membership?.roles.includes('admin') ?? false\n"}],["$","$L10",null,{"language":"client-flutter","content":"import 'package:clikkle/clikkle.dart'\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n\nfinal teams = Teams(client)\n\n// Get team memberships\nfinal response = await teams.listMemberships(\n    teamId: '<TEAM_ID>'\n)\n\n// Check if user has specific role\nfinal membership = response.memberships.firstWhere(\n    (m) => m.userId == '<USER_ID>',\n    orElse: () => null\n)\nfinal isAdmin = membership?.roles.contains('admin') ?? false\n","children":"import 'package:clikkle/clikkle.dart'\n\nfinal client = Client()\n    .setEndpoint('https://cloud.clikkle.io/v1')\n    .setProject('<PROJECT_ID>')\n\nfinal teams = Teams(client)\n\n// Get team memberships\nfinal response = await teams.listMemberships(\n    teamId: '<TEAM_ID>'\n)\n\n// Check if user has specific role\nfinal membership = response.memberships.firstWhere(\n    (m) => m.userId == '<USER_ID>',\n    orElse: () => null\n)\nfinal isAdmin = membership?.roles.contains('admin') ?? false\n"}],["$","$L10",null,{"language":"client-apple","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Get team memberships\nlet response = try await teams.listMemberships(\n    teamId: \"<TEAM_ID>\"\n)\n\n// Check if user has specific role\nlet membership = response.memberships.first { $0.userId == \"<USER_ID>\" }\nlet isAdmin = membership?.roles.contains(\"admin\") ?? false\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nlet teams = Teams(client)\n\n// Get team memberships\nlet response = try await teams.listMemberships(\n    teamId: \"<TEAM_ID>\"\n)\n\n// Check if user has specific role\nlet membership = response.memberships.first { $0.userId == \"<USER_ID>\" }\nlet isAdmin = membership?.roles.contains(\"admin\") ?? false\n"}],["$","$L10",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Get team memberships\nval response = teams.listMemberships(\n    teamId = \"<TEAM_ID>\"\n)\n\n// Check if user has specific role\nval membership = response.memberships.find { it.userId == \"<USER_ID>\" }\nval isAdmin = membership?.roles?.contains(\"admin\") ?: false\n","children":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Get team memberships\nval response = teams.listMemberships(\n    teamId = \"<TEAM_ID>\"\n)\n\n// Check if user has specific role\nval membership = response.memberships.find { it.userId == \"<USER_ID>\" }\nval isAdmin = membership?.roles?.contains(\"admin\") ?: false\n"}]]}]
31:["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":["See how to grant row and file access to team roles in the ",["$","$L3b",null,{"href":"/docs/advanced/platform/permissions#example-2-team-roles","children":"permissions"}]," guide."]}]
32:["$","$L3b",null,{"href":"/docs/products/auth/teams","className":"not-prose text-paragraph-lg text-primary group mt-8 flex items-center gap-1 first:mt-0","children":[["$","p",null,{"className":"mb-4 leading-7 text-[var(--color-text-secondary)] dark:text-white/70","children":"Learn more about team management"}],["$","span",null,{"className":"web-icon-chevron-right inline-block transition-transform duration-100 group-hover:translate-x-0.5","aria-hidden":"true"}]]}]
33:["$","$L3c",null,{}]
37:null
38:["$","$L10",null,{"language":"client-android-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Get list of teams the user is part of\nval teamsList = teams.list()\n\n// For a specific team, get all memberships\nval response = teams.listMemberships(\n    teamId = \"team_id\"\n)\n\n// Find membership for specific user\nval userMembership = response.memberships.find {\n    it.userId == \"<USER_ID>\"\n}\n\nuserMembership?.let {\n    println(it.confirm) // false = invited, true = joined\n    println(it.roles) // ['developer', etc]\n}\n","children":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client(context)\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n\nval teams = Teams(client)\n\n// Get list of teams the user is part of\nval teamsList = teams.list()\n\n// For a specific team, get all memberships\nval response = teams.listMemberships(\n    teamId = \"team_id\"\n)\n\n// Find membership for specific user\nval userMembership = response.memberships.find {\n    it.userId == \"<USER_ID>\"\n}\n\nuserMembership?.let {\n    println(it.confirm) // false = invited, true = joined\n    println(it.roles) // ['developer', etc]\n}\n"}]
39:["$","$L10",null,{"language":"server-swift","content":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n    .setKey(\"<API_KEY>\")\n\nlet teams = Teams(client)\n\n// Get all teams\nlet teamsList = try await teams.list()\n\n// Iterate through teams to find memberships\nfor team in teamsList.teams {\n    let response = try await teams.listMemberships(\n        teamId: team.$id\n    )\n\n    // Find membership for specific user\n    if let userMembership = response.memberships.first(where: { $0.userId == \"<USER_ID>\" }) {\n        print(\"Team: \\(team.name)\")\n        print(\"Joined: \\(userMembership.joined)\") # null if invited, timestamp if joined\n        print(\"Roles: \\(userMembership.roles)\")\n    }\n}\n","children":"import Clikkle\n\nlet client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n    .setKey(\"<API_KEY>\")\n\nlet teams = Teams(client)\n\n// Get all teams\nlet teamsList = try await teams.list()\n\n// Iterate through teams to find memberships\nfor team in teamsList.teams {\n    let response = try await teams.listMemberships(\n        teamId: team.$id\n    )\n\n    // Find membership for specific user\n    if let userMembership = response.memberships.first(where: { $0.userId == \"<USER_ID>\" }) {\n        print(\"Team: \\(team.name)\")\n        print(\"Joined: \\(userMembership.joined)\") # null if invited, timestamp if joined\n        print(\"Roles: \\(userMembership.roles)\")\n    }\n}\n"}]
3a:["$","$L10",null,{"language":"server-kotlin","content":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n    .setKey(\"<API_KEY>\")\n\nval teams = Teams(client)\n\n// Get all teams\nval teamsList = teams.list()\n\n// Iterate through teams to find memberships\nteamsList.teams.forEach { team ->\n    val response = teams.listMemberships(teamId = team.$id)\n\n    // Find membership for specific user\n    val userMembership = response.memberships.find {\n        it.userId == \"<USER_ID>\"\n    }\n\n    userMembership?.let {\n        println(\"Team: ${team.name}\")\n        println(\"Joined: ${it.joined}\") # null if invited, timestamp if joined\n        println(\"Roles: ${it.roles}\")\n    }\n}\n","children":"import io.clikkle.Client\nimport io.clikkle.services.Teams\n\nval client = Client()\n    .setEndpoint(\"https://cloud.clikkle.io/v1\")\n    .setProject(\"<PROJECT_ID>\")\n    .setKey(\"<API_KEY>\")\n\nval teams = Teams(client)\n\n// Get all teams\nval teamsList = teams.list()\n\n// Iterate through teams to find memberships\nteamsList.teams.forEach { team ->\n    val response = teams.listMemberships(teamId = team.$id)\n\n    // Find membership for specific user\n    val userMembership = response.memberships.find {\n        it.userId == \"<USER_ID>\"\n    }\n\n    userMembership?.let {\n        println(\"Team: ${team.name}\")\n        println(\"Joined: ${it.joined}\") # null if invited, timestamp if joined\n        println(\"Roles: ${it.roles}\")\n    }\n}\n"}]
