# Parent monorepo `.npmrc` sets `engine-strict=true`, which npm still reads when
# installing from this directory. That can fail `npm ci` on Vercel for transitive
# packages whose `engines` do not match the builder. Scope engine checks off here.
engine-strict=false
